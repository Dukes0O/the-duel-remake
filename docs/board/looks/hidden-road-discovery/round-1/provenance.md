# EGG-04 discovery — round 1

Source and review are recorded in `captures.json` and the change note. The
private memory-only harness used port 5431 and retained eight actual images,
with zero browser warnings or errors. No live server or real save was used.

The completion counts five and ten are explicit synthetic profile fixtures.
The actual invitation marks discovery; the actual Turn back control is used.
Private reload, named-player switching, safe menu visit with unchanged career,
scenic automatic passage, flag-off presentation and paused dust cleanup all
passed. Direct visits did not create a race run or alter the profile. Saved
race settings and persistence details also have separate independent tests.

Dust views use the documented inspection camera in `captures.json` to show the
actual entrance and effect in context. One reused pool has 48 points and one
material, with no simulation RNG or collision. Each quality recorded 120 ordered
RAF intervals: p95 was 18.1 ms High and 18.2 ms Performance. This is a bounded
frame observation, not an attributed overhead or whole-course performance test.
The full scene reported 487 draws/1,867,343 triangles High and
243 draws/721,162 triangles Performance, including its existing scene passes.

The contact sheet combines retained PNGs. Builder inspection finds the garage
tip below the initial visible garage area, a small WASTELAND menu action, and a
subtle soft dust spiral. Director scoring and the next refinement remain
independent; no self-approved fidelity score is claimed.
