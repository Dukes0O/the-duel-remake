# Five-merge full check — `731a075`

**Release decision: hold.** The 160-suite code gate, 11 available browser
scenarios, save budget, and short frame comparison passed. The combat balance
gate failed its UFO time-gain and Medium/Hard CPU-hit targets. The art-intake
script named by the playbook is not present; this cycle added no image assets.

## Fix proposals for the Director

1. Resolve BUG-04's conflict between the exact 80 m + 30 m/level warp and
   24 s recharge numbers and the maximum 4 s two-lap gain. A visible
   post-swap recovery is one candidate; the gameplay tradeoff is awaiting
   Kyle's choice. Keep the current measured failure visible until resolved.
2. Continue BUG-06/BUG-07 together. The standard Pacific Canyon race has too
   little front-facing contact for the Medium/Hard CPU-hit bands. Test AI
   shield reaction and crossbow accuracy together before changing race pace.
3. Add the missing art-intake command and a supported all-scenarios runner to
   FND-12's remaining tooling work. Neither is a reason to weaken the
   balance gate.

## Gates on the exact integration source

| Gate | Result | Evidence |
| --- | --- | --- |
| Locked dependencies | Pass | `npm ci --offline`; 16 packages installed, zero reported vulnerabilities. |
| Full code suite | Pass | `node tools/run-tests.mjs --tier full --jobs 10`: **160/160** suites in 327.67 s. All campaign shards, 162 replay checks, input, save and rendering suites passed. |
| Combat balance | **Fail** | `node tools/combat-balance.mjs --check`: 21 policy runs and 30 baseline races in 61.8 s; measured misses below. |
| Browser smoke | Pass | High and Performance menu/race, four screenshots, zero warnings or errors, private port 9330. |
| Browser scenarios | Pass | All 11 available scenarios passed individually with private ports and memory-only saves: experimental, weapon-bar, vehicle-socket-poses, ghost-lifecycle, crash-counter, career-tab-isolation, career-origin-missing, career-backup, career-backup-failure, career-archive and career-archive-missing. |
| Career storage | Pass | 64-player fixture: 5,515,688 raw UTF-16 bytes, 230 physical origin bytes after migration; maximum ghost journal 2,500,604 of 4,000,000 bytes. |
| Frame pacing comparison | Pass as short regression check | Same-machine private Chrome samples against master `479d857`, 240 steady driving frames per quality. Details below. |
| Art intake | Not available | `tools/check-art-intake.mjs` does not exist. No new raster/image asset was added in this five-card cycle. |

### Combat balance details

| Measure | Easy | Medium | Hard | Target |
| --- | ---: | ---: | ---: | --- |
| Standard autopilot wins | 10/10 | 10/10 | 2/10 | 80–95% / 45–65% / 20–40% |
| Stock UFO two-lap gain | 8.57 s | 8.58 s | 8.17 s | ≤4 s each |
| Maximum UFO two-lap gain | 25.29 s | 25.30 s | 24.89 s | ≤4 s each |
| CPU hits on player | 1 | 1 | 1 | 0–3 / 2–6 / 4–10 |

The moving-rival crossbow probe hit 13 of 26 shots (50%, within 35–60%).
Own bombs removed at most 4.53% of speed (within 15%). Easy and Medium win
rates are above their targets; Hard is within its range. The check's nonzero
exit was caused by the six UFO rows and the Medium/Hard CPU-hit rows.

### Frame pacing against the live master code

Both builds were served from disposable worktrees on private ports; neither
used the live game's port or real saves. Each sample used Pacific Canyon Time
Trial with the QA autopilot at about 164–170 km/h and route distance 81–87 m.
These are delivered frame intervals, not GPU time. One short sample per
quality can catch a large regression but cannot establish every-course
performance.

| Quality | Master p95 | Integration p95 | Frames over 33 ms | Draw calls master → integration |
| --- | ---: | ---: | --- | ---: |
| High | 18.1 ms | 18.2 ms | 0 → 0 | 681 → 683 |
| Performance | 18.2 ms | 18.2 ms | 0 → 0 | 425 → 321 |

Integration [High screenshot](<C:/Users/kyleb/.codex/worktrees/wasteland-integration/the-duel-remake/.qa-dist/browser-output/frame-pacing-check-2026-09-23T07-40-26-379Z/frame-high.png>) and [Performance screenshot](<C:/Users/kyleb/.codex/worktrees/wasteland-integration/the-duel-remake/.qa-dist/browser-output/frame-pacing-check-2026-09-23T07-40-26-379Z/frame-performance.png>); master [High screenshot](<C:/Users/kyleb/.codex/worktrees/wasteland-frame-baseline/the-duel-remake/.qa-dist/browser-output/frame-pacing-check-2026-09-23T07-39-53-874Z/frame-high.png>) and [Performance screenshot](<C:/Users/kyleb/.codex/worktrees/wasteland-frame-baseline/the-duel-remake/.qa-dist/browser-output/frame-pacing-check-2026-09-23T07-39-53-874Z/frame-performance.png>). The temporary comparison scenario was removed from both worktrees after these runs.

No live `master`, live build, or real player save was changed.
