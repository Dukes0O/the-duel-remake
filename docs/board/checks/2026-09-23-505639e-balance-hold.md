# CPU pickup integration hold — `505639e`

**Release decision: hold.** The physical CPU pickup fix passed the exact merge
gate and private browser checks. Combat balance still fails all six UFO
time-gain limits. The live `master`, game server and real saves were not
changed.

| Check | Result |
| --- | --- |
| Changed lane | 168/168 suites on the CPU-pickup branch; production build passed. |
| Exact integration merge gate | 160/160 suites in 212.50 s. |
| Production build | Passed; the existing large render-chunk advisory remains. |
| Private High/Performance smoke | Four screenshots, zero warnings and zero errors. |
| Browser scenarios | 11/11 passed, 13 screenshots, zero warnings and zero errors. Archive: `C:\Users\kyleb\AppData\Local\Temp\duel-browser-suite-q5NL9l\summary.json`. |
| Art intake | Passed. All seven Batch A images remain planned and absent. |
| Standalone full code tier | 167/167 suites passed on the immediately preceding gameplay source (`b48e3cf`). The new pickup branch passed 168/168 lane suites; no composite full check was claimed. |
| Combat balance | **Fail:** six UFO gain rows. The other measured targets below pass. |

The exact integration balance run completed 21 policy races and 30 baseline
races in 55.65 s. The CPU now collects real road pickups on Medium and Hard,
then uses shields or scheduled attacks without extra shots. The ten-seed trace
collected 17 pickups on Medium and four on Hard; eight and four were used.
Easy collected and used none. CPU UFO pickup use remains open.

| Measure | Easy | Medium | Hard | Target |
| --- | ---: | ---: | ---: | --- |
| No-weapon player wins | 8/10 | 6/10 | 2/10 | 80–95% / 45–65% / 20–40% |
| CPU hits on player | 1 | 2 | 10 | 0–3 / 2–6 / 4–10 |
| Stock UFO two-lap gain | 9.59 s | 45.52 s | 57.87 s | ≤4 s each |
| Max UFO two-lap gain | 26.31 s | 62.24 s | 76.18 s | ≤4 s each |

The moving-rival crossbow probe hit 13/26 shots (50%, target 35–60%). Own
bombs caused at most 4.53% speed loss (target ≤15%). All 162 stored replay
comparisons remain unchanged. The earlier exact-distance UFO branch and a
target-only short-range prototype are held separately: both miss the original
four-second test. The target-only prototype also raises Medium player wins to
10/10. No balance limit was relaxed.

The Hard stock UFO gain increased from 22.11 s before CPU pickups to 57.87 s.
A trace of both exact integration sources found why: the no-weapon race is
unchanged at 155.44 s with one 30-second traffic crash. In the UFO-policy
race, the new rival pickup replaces a crossbow at about 50 s with a physical
bomb. The old crossbow caused a second crash and penalty; the bomb did not.
This is a real combat-path change, not a pickup collision error. A narrower
bomb-use window is being tested in isolation; the merged behavior remains
under review while the UFO gate is red.

The previous composite full check was red. This report is a partial check,
not a second composite full check or a live release.
