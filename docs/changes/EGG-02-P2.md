---
task: EGG-02-P2
flag: hidden-road
status: merged
---

# Rustwall salvage depth and canyon strata

EGG-02-P2 merged at 78cfffe as a measured development improvement. Director and Crew scored the final wall and wash likeness **3/5**, while the scoped frame gate scored **4/5** with the worst required A1/B/A2 ratio at 1.0833 against 1.10. Lane tests passed 256/256 and the build passed. The target likeness four remains open in EGG-02-P3; this card makes no beta or finished-art claim.

## Starting point and design choice before code

The merged EGG-02-P1 wall and wash are development assets. The round-10 independent review scored wall likeness 3/5, wash likeness 3/5, readability 4/5, grounding 3/5 and scene consistency 3/5. The wall is 7,777,248 bytes, 58,914 triangles and 13 draws; the wash is 3,898,584 bytes. The wall has a 35 m structural core spanning over 400 m and a real 9 by 7 m movable gate. Its facing is still based on 30 regular bays, six rows of panels and many repeated rivets. The wash uses a joined ribbon with three fixed cross-slope bands, leaving broad, repeated stripes. These are the two modeling methods to replace.

Build a nonperiodic facade from an explicit table of roughly 10–12 unequal salvage sections over the existing wall span. Each section sets its own roof crown, foreground depth, car-stack placement, large brace and material region. Spend geometry on a few distinct tall mass shapes and grounded load paths, while leaving sky breaks between them. Reclaim triangles from the repeated rivets, interior tile faces, duplicated ladders and small flat filler before adding macro geometry. The structural core, wall placement, physical course and 9 by 7 m clear gate do not change. No decorative piece may block the opening in its closed or fully raised state.

The prominent salvage uses the proven **offline** welded-car method from `tools/blender/scrapdome-yard.py` as a read-only recipe. Import the existing Falcone F42 and Banshee production GLBs in Blender; weld coincident vertices at 0.1 mm before decimation; preserve a connected body with hood, cabin and trunk, a separate dark glass region and source-measured wheel/ring hollows. Crush and tilt copies with repeatable authored section transforms, stacking each visible lower body on the wall foot or another measured car surface. Batch the result into one new GLB mesh named `welded-car-hulks` using the existing `hulks` atlas/material; this adds at most one draw. Its exported node extras record each source GLB path and SHA-256, the 0.0001 m weld threshold, pre/post weld connected-component counts, per-template body/glass/wheel triangle counts and actual placed hulk count. Tests must inspect the exported mesh geometry and bounds as well as the extras, so metadata alone cannot pass. The existing `hulk-stacks` mesh may retain only small distant filler. Runtime loads no source car for the wall. Material paint uses broad connected oxide, dark steel, soot and worn-edge regions rather than the current checker rhythm. The same three 512-square wall atlas sets, at most 60,000 actual GLB triangles and 24 actual material draws remain hard contracts. Record the per-object triangle inventory **before** editing the generator and remeasure after every export; do not raise a bound to fit art.

The wash remains one joined runtime ribbon per side, no more than 30,000 rendered triangles and two draws, using the same single 1024-square rock atlas. Keep the current two segments per physical bank and three cross-slope strips per segment. At each **shared** station, deterministically shift the two interior contour levels from the fixed 0.38/0.72 ratios within a narrow ordered range; use the same world station and side to produce identical XYZ and UV values on both sides of a join. Map atlas V by normalized distance along the actual slope/height, with a bounded low-frequency warp, so dark erosion seams bend with the wash instead of forming broad horizontal stripes. Paint broken rock strata and weathered colors into the existing atlas. All preparation happens once when the scene loads, never in a frame update. Every rendered vertex and triangle centroid stays inside the 2 mm union of the original oriented physical bank boxes on Routes A, B and C. Joined edges must match exactly, normals face daylight on both sides, and the original collision objects, route, RNG and replay fingerprints stay unchanged.

The wash visual target is the **background canyon context**, not a close geology study, in the approved `public/assets/reference/wasteland-art-direction.png`. Use its top-right crop at about x1010–1470, y15–310 in the 1774×887 image, retaining the 460:295 aspect and labeling that scope on every P2 comparison sheet. The existing `rustwall-gate.png` remains the wall/gate reference. A reviewer scores actual High and Performance game views; no geometry count or texture check stands in for likeness.

## Ownership and measurable acceptance

### P2 review path and comparison contract, settled before helper code

`node tools/fidelity-sheet.mjs --rustwall-p2-round N` selects only the new `rustwall-p2` family for N=1..10. `--paths-only` returns absolute `directory`, `output`, `manifest` and `summary` paths without writing; the default directory is `.evidence/<local-date>/rustwall-p2/round-N`, or the existing `DUEL_EVIDENCE_DIR` override. The raw output is `sheet.png` with `sheet.json`; the published immutable review copy is `docs/board/looks/rustwall-p2/round-N.jpg`. The old `--rustwall-round` family and every old output path remain unchanged. Blender's existing `--p2 --round N` selects the same raw folder. Browser uses existing `EGG_RUSTWALL_ROUND=N` and the private harness's explicit `--output-dir` set to that exact folder; no new browser flag or shared harness edit is needed.

The P2 `sheet.json` has six `rows`: five wall rows (`front`, `depth`, `driver-approach`, `open-gate`, `full-span`) with `comparisonScope:'numeric-camera-match'`, unchanged Rustwall gate reference crops, and exact Blender/High/Performance camera, gate and asset-hash checks. The sixth row is `wash-module` with `comparisonScope:'source-module-vs-runtime-bank'`, `label:'WASH SOURCE MODULE AND GAME JOINED BANK NO CAMERA MATCH'`, and `columnLabels:['REFERENCE ENVIRONMENT CONTEXT','BLENDER SOURCE MODULE','HIGH GAME JOINED BANK','PERFORMANCE GAME JOINED BANK']`. Its `reference` is `public/assets/reference/wasteland-art-direction.png` with crop `[1010,15,1470,310]`, preserving that background canyon crop's 460:295 aspect. Its `blender` is the isolated source wash module; `high` and `performance` are the **actual joined runtime bank** at Route A wash inspection. No wash numeric-camera-match assertion or geological close-up claim is made.

At top level `sheet.json` retains the wall `reference` provenance and adds `contextReference:{path,sha256,crop,scope:'environment-context'}` for the approved canyon context. It retains `sources` hashes for every used image and GLB. It adds `placement` copied from the six validated `captures.json` A/B/C × High/Performance placement records, and `routeCaptures` holding their six approach image records (`route`, `quality`, `view`, `path`, `sha256`). The helper rejects a missing or duplicate route/quality, bad image or source hash, mismatched wall pose, missing actual joined-bank capture, or changed asset hash. The PNG/JPG displays the column labels on the wash row, and `sheet.json` records that row's context scope and crop; the file itself is an evidence composite, not a fidelity pass. Independent tests cover the new family/path, context/provenance, six placements and old-family stability before helper implementation.

### Whole-scene frame comparison planned before measurement

The baseline is lane start `5a994ad`, which still uses the merged EGG-02-P1 GLBs and joined wash. The candidate is the eventual exact P2 lane commit. Build them in separate private QA folders; use memory-only storage and never port 5174. For each quality (High, Performance), capture the same stopped seed-1989 Route A wash view at progress 350 and gate-approach view at progress 960, with identical 1280×720 pixels, device scale, camera matrices, no UI panels, same simulation snapshot, and the complete production renderer/composer/shadow path. Use ordered A1/B/A2 samples, 600 RAF intervals and whole `renderFrame()` CPU submissions per view after 20 warm frames, one production render per RAF. Record raw ordered RAF/CPU arrays, draw calls, triangles, actual dimensions and source/camera hashes, with mirror refreshed and reused strata; compare candidate CPU p50/p95 and RAF p95 against **both** baselines at <=1.10. Means and unstable/unfavorable samples remain visible diagnostics. Check A1/A2 stability before a pass; no GPU claim follows from CPU submission. Measure only in a Director-approved quiet window after candidate assets, review and builds are frozen. No frame number is currently accepted for P2.

This lane owns `tools/blender/rustwall.py`, `public/assets/models/wasteland/rustwall/wall.glb` and `wash.glb`, this note, and one compressed sheet plus review note per round in `docs/board/looks/rustwall-p2/`. Named hooks are `src/rustwall-scene.js` limited to `joinedWash` contour/UV preparation, `tools/scenarios/rustwall.mjs`, and the fidelity-sheet tools only as needed for the new immutable P2 folder. Crew owns independent tests in `tools/test-rustwall-assets.mjs`, `tools/test-rustwall-scene.mjs` and `tools/test-rustwall-p2.mjs`. Do not edit those assertions without independent review. No implementation starts until Crew shows the first failure on the unchanged source.

Independent checks should measure source-car import provenance and welded connected silhouettes, actual wall depth/roofline and grounded braces at sampled sections, gate geometry/clearance and unchanged atlas/triangle/draw budgets. Wash checks should measure contour-level variation and slope-following UVs, exact shared seam XYZ/UV, triangle winding, deterministic repeat builds, all-route collision-box containment for both vertices and centroids, and disposal without touching simulation state. Keep the prior P1 safety tests. Capture the same wall/approach/wash views in Blender and private memory-only High and Performance game runs, then a continuous approach/open-gate sequence. Each round gets a new immutable comparison sheet under 500 KB and an independent five-part visual score. Hidden Road stays `dev` until every SPEC 0.3 score reaches at least 4 and a quiet whole-scene baseline comparison holds both RAF p95 and render-CPU p50/p95 within 1.10; retain unfavorable samples and mirror-refresh strata. CPU submission time is not GPU time.

## Reversal and evidence

If the new macroforms reduce gate clarity, lose grounding, exceed budgets or fail the frame comparison, restore the last merged wall/wash GLBs and only this card's generator and `joinedWash` contour/UV changes. The course, physics, collision boxes, gate timing and saves have no P2 changes to reverse. Keep failed captures and numbers until the verdict is committed. The prior P1 art remains the development fallback; no live folder or real save is involved.

## Tests and checks

Independent `node --test tools/test-rustwall-p2.mjs` is red on the unchanged P1 source: 1/5 passes. Four intended failures cover only five distinct wash contour levels against the new twelve-level requirement, atlas V that does not follow physical slope height, the missing `welded-car-hulks` source-derived mesh/extras, and fewer than six distinct wall skyline heights. The same spatial test will require varied facade depth and six grounded upper supports after the skyline check passes. The existing all-route seam XYZ/UV check passes. Crew owns these assertions; no builder runtime or GLB change preceded this red run.

The first builder step is an **isolated ignored preflight**, not a whole-wall export. New `--p2-hulk-probe` imports actual F42 and Banshee meshes, welds at 0.0001 m, decimates body/trim/glass and reconstructs hollow tire rings from source wheel bounds, then exports one `welded-car-hulks` mesh using the final hulks atlas and one named `Authored material islands` UV layer. The generated `art-build/rustwall-p2/hulk-probe.glb`, `.blend`, two clay/material renders and manifest are ignored. Actual probe GLB is 2,437 triangles, one material draw, with source SHA-256 and the agreed extras keys. F42 original/welded connected-component totals are 29,288/85 and body/glass/wheel triangles 979/8/256; Banshee totals are 27,739/486 and 930/8/256. The first probe used tile 6 for glass/rubber and looked tan; rerendered proof uses existing dark tile 8 for glass and tile 3 for rubber, with no extra atlas or draw. Front and three-quarter views now retain recognizable hoods, cabins, trunk mass, dark windows and hollow wheels. The runtime wall/wash GLBs are unchanged while the Director reviews this proof and the next representative facade section.

Independent red acceptance: pending. Read-only starting GLB accessor inventory: `wall-body` 6,400 triangles, `scaffold-steel` 28,680, `hulk-stacks` 19,776, `wall-details` 1,698, `gate-panel` 120 and eight guards at 244 each, or 58,626 actual exported triangles across 13 draws. P1's 58,914-triangle figure was its pre-export builder count; keep both distinct. The regular 30 bay × six row × three column × six-rivet pattern creates about 3,240 four-triangle rivets, roughly 12,960 scaffold triangles available for removal before adding source-car silhouettes. Build, matched captures, frame comparison, lane tier and replay fingerprints: pending.

The next ignored section proof uses one roughly 34 m wide representative span, with a continuous 35 m structural back, front salvage planes stepping 1.8–4.5 m toward the road, five actual-car hulks and large diagonal steel braces reaching the ground. Ground-level cars sit at y=0; upper cars sit at y=12, 15.9 and 28.6 m on authored shelves. The section has a nonlevel roof crown and no tiny rivet grid. It uses only the existing steel and hulk materials, and leaves the gate outside the probe. Its three exported objects total 5,011 actual triangles: 112 mass, 228 supports and 4,671 source-car hulks. One hulk batch retains the single `Authored material islands` UV layer. Front and quarter proof renders are in ignored `art-build/rustwall-p2/section-probe-*.png`. Visual self-review found that the shelves are too thin and the large rust planes still dominate. Repeating the five-car section twelve times would exceed the 60,000-triangle full-wall limit, so the final wall must spend fewer hero cars per section and use visibly substantial connected support and more varied foreground depth. No runtime GLB has been replaced. Director review is pending before full export.

### Section revision before further source work

The Director rejected the first 34 m section proof. Its giant peaked sheets and isolated cars on narrow shelves lose the reference's dense stacked-car bays between tall steel framing. The revised proof keeps a 35 m rectangular structural back, two narrow full-height uprights around an inset bay, and an adjacent subdivided rectangular steel panel. Fill the inset bay with roughly six staggered rows of touching F42/Banshee shapes, each lower car meeting the wall foot or the layer below; tie the bay to the ground with prominent diagonal braces. Unequal full-wall sections will vary bay width, depth, roof accessories and stack profile within this underlying framed construction, rather than form zigzag mountains.

Triangle allocation before code: target eight inset salvage bays, each about twenty cheap source-derived car silhouettes. The welded 120-body-triangle F42/Banshee variants have dark glass and two visible source-measured wheel rings; the actual templates average 175.5 triangles, so eight twenty-car bays cost about 28,080 triangles. Reserve up to 10,000 triangles for distinct 500–1,200-triangle heroes near the gate/foot, retain the 6,400-triangle structural core, target at most 9,000 for framing/panels and 2,000 for guards/details. The summed upper plan is 55,480 triangles, under the unchanged 60,000 limit. Reuse the same one hulk batch and material/UV path. The twenty-car representative section uses 4,753 car triangles including one 1,243-triangle grounded hero; mass and grounded supports use 108 and 288, for 5,149 triangles total. At 2.9 m row spacing, adjacent 3.4 m-tall cheap cars physically overlap about 0.5 m; horizontal centers are 5.6 m apart for roughly touching 5.35 m bodies after stagger. The remaining concern is visible two-column repetition; vary section bay widths, car offset/tilt/color and roof steel rather than repeating this probe verbatim. The earlier failed section renders and 5,011-triangle count stay in ignored preflight evidence and this note. No runtime GLB has been replaced.

Director review accepted the framed steel and car-bay structure but found the 120-body-triangle cars still read as pointed angular boxes with weak tire circles. Before a full export, the probe will try roughly 200 body triangles per cheap car, preserve side-on car profiles for most rows, add a few quarter turns, and alternate crushed height/body atlas colors. Seat successive rows with a small actual overlap rather than identical open roof gaps. To fund the larger cheap silhouette, revise the plan to **six dense twenty-car bays** (about 6 × 20 × 260 = 31,200 triangles), up to seven detailed heroes spread across other sections (about 8,400), 6,400 core, 9,000 frames/panels, 2,000 guards/details: 57,000 projected triangles. This retains at least seven wall sections with actual car geometry and the hard 60,000 limit. The 200-triangle method and count must be measured in the updated ignored section before selecting it for the full wall. No assertion or budget changes are authorized.

The updated ignored proof now has 20 source-derived cheap bodies with actual mean 255.5 triangles, a detailed 1,243-triangle grounded F42, and 396 triangles of bay mass/support. The exported section totals 6,749 triangles, three existing-material draws and one named hulk UV layer. Most car bodies show the broad source side, while a few use ±0.3 rad yaw; paint alternates across five atlas regions. The 2.72 m row step puts the next compressed car into the one below. This is a single-method proof, not a fidelity score; Director review is pending before the full runtime wall export. In parallel, the deterministic wash contour/height-UV change passes all three new wash checks and all 23 existing scene checks; the two full-wall P2 tests remain red while the wall GLB is untouched.

The Director's section review found the cheap cars' wheel circles look like thin vertical spikes. Source tire bounds confirm a 0.32–0.34 m axle thickness and 0.8–0.86 m circular Y/Z diameter. The current normalization scales source up by 2.35 m and then each cheap placement by about 1.28–1.48, while longitudinal scale is only 5.35 m over the full body. This distorts the rings vertically. Crew initially suspected the near cap's winding, then retracted that claim after accounting for the face reversal in `stamp_welded_car`; actual exported normals must be measured before changing winding. The source-side selection is restored to its prior min-X choice. Root asked Crew for an independent failing wheel-facing/scale assertion on the current ignored proof GLB. Hold further generator edits until that red evidence exists, then correct the cheap template's vertical scale and row contact from actual bounds. Keep the hard full-wall budget and physical/collision contracts.

Crew now has red proof on freshly exported geometry: near wheel projected height/width is 2.10 (allowed 0.65–1.45), and only 3/6 measured near-cap triangles face out. First scale correction will use a 1.65 m cheap-template normalized body height, 1.05–1.15 placement height scale and approximately 1.65 m row step, leaving about 0.1–0.25 m contact overlap. This should put the wheel aspect near 1.2–1.3; the exact export decides. Use about twelve two-car rows in each of five dense bays, with 150 source-body triangles plus dark glass/two wheel rings per cheap car, reserving upper steel/panel/scaffold silhouette. Five bays × 24 cars × roughly 205 triangles is about 24,600. With seven detailed heroes up to 8,400, 6,400 core, 9,000 frames/panels and 2,000 details, the projected upper sum is 50,400, leaving measured headroom under 60,000. This revises the prior six-bay/tall-body estimate before further code; actual section counts and visual review determine the final placement. Check exported near cap normals after scale. Do not change winding unless the remaining red result proves it.

The first wheel regression measured multiple touching rows as one wheel after the row-height change, yielding a misleading 3.10 combined aspect. Root approved an **ignored probe-only fixture** instead: export exactly one current 150-body-triangle cheap F42 through the same `welded_car_template` and `stamp_welded_car` path, same hulks atlas and UV layer, to `art-build/rustwall-p2/wheel-probe.glb` with manifest and front/quarter renders. Crew will measure the isolated actual exported tire aspect and outward normal with a new red assertion before any further wheel-source correction. This proof adds no runtime asset or draw. The scale-corrected section image currently leaves too much blank upper bay; fill that at realistic car scale after the wheel method is settled.

The isolated actual-GLB regression is now red only on facing order: wheel height/width 1.36 passes, nearby body side lies at front depth 6.109 m, and the ring caps lie about 60 mm outside it. Yet the outward-facing cap averages depth 6.169 m, slightly **behind** the inward-facing cap at 6.173 m. The near wheel therefore shows its back face or sidewalls. Correct the cap triangle winding in the offline template, leaving the ring sidewalls, physical measurements, UV tile and triangle count fixed; rerun Crew's unchanged regression and inspect front/quarter renders. No arbitrary wheel offset is needed because the annulus already protrudes beyond the body.

The cap diagnostic found `bmesh.ops.recalc_face_normals` changed directions on disconnected annulus triangles. The offline hulk batch now preserves explicitly opposite x0/x1 cap winding and skips that recalculation, leaving other geometry builders unchanged. Crew independently corrected a false test assumption that both the exterior and hidden interior caps should be visible in front of the body. The corrected actual-export regression passes 1/1: 1.36 height/width, at least four outward triangles and no inward triangles in the camera-facing exterior band, with at least 10 mm local clearance from the nearby body. The probe camera maps to glTF -Z, matching the in-game wall-facing test vector. The front render still looks triangular because the cheap rings use only three segments. Before final acceptance, raise **only the cheap visible two rings** to six segments; this adds 48 triangles per cheap car. For five twenty-four-car bays that adds 5,760 triangles to the 50,400 projection, or 56,160 under the 60,000 hard cap. Rerun the same test and front/quarter proof before checkpointing source.

The scale-corrected bodies occupy the lower 19–20 m of a 35 m bay. Fill the upper 15 m with two or three offset steel salvage panels and cross-frame bays **behind** the car stack, not another smooth front sheet; stagger depths 1–3 m, crown each section differently, and attach tall vertical support to y=0. The steel shares the existing steel material draw, adds no collision, and leaves the gate corridor clear. A few detailed source-car heroes can interrupt selected upper frames, but should not float on thin shelves. The lower stack remains car-on-car at its measured row step. This uses the reserved frame/panel triangle pool rather than stretching tires to cover empty height.

The independent wheel curvature test was red with three outer-ring directions and green after six, with unchanged scale/facing/body-clearance assertions. The isolated cheap F42 now exports 254 triangles (150 body, 8 glass, 96 wheel) and the front/quarter renders show visible dark tire holes. Director approved that cheap silhouette as a stack foundation, while noting its intact tall spoiler must not repeat like a comb. In the section/full-wall stack, deterministically fold the aft upper spoiler/roof vertices down on most cheap F42 copies while leaving some uncrushed source shape for recognition; use no new vertices, material or draw. Preserve the approved isolated one-car wheel proof unchanged. Then re-export the representative section, verify actual triangle count and contact, and freeze code for a text-only checkpoint before full runtime asset export.

## First full candidate and round 1

The first P2 export replaces the previous runtime GLBs in this isolated lane. It has twelve unequal steel-framed sections, five dense lower car bays containing 120 cheap source-derived F42/Banshee copies, and seven detailed source-car heroes. The exported wall measured **58,573 triangles and 13 draws**: structural wall body 1,308; scaffold steel 14,222; welded car hulks 38,985; details 1,986; gate 120; eight guards 244 each. The source wash module remains 144 triangles and one draw; the runtime joined ribbons keep their existing two-draw limit. These are actual exported counts, distinct from the earlier proposed allocation and ignored section counts. The atlases remain three 512-square wall sets and one 1024-square wash atlas.

The first export failed Crew's unchanged skyline assertion because existing watchtower tops masked the varied section crowns in most 35 m inspection bins. The measured roof-bin maxima repeated after 0.25 m rounding. P2 now gives its eight physically supported towers varied heights while keeping their bases and the clear gate fixed. The re-export adds 72 steel triangles and passes the same roof/depth/support assertion. No test bound or assertion changed. Focused evidence: `node --test tools/test-rustwall-p2.mjs tools/test-rustwall-assets.mjs tools/test-rustwall-scene.mjs` passed P2 5/5, P1 assets 9/9 and scene 23/23. Crew's new sheet negative fixtures first caught a missing-hash and loose joined-bank-scope acceptance; the helper now requires a valid expected SHA-256 for all frozen images/assets and exact joined-bank scope, while computing the newly introduced canyon context hash directly. Its independent suite passes 6/6. The old P1 review path is unchanged.

Round 1 is immutable under `.evidence/2026-09-24/rustwall-p2/round-1` and `docs/board/looks/rustwall-p2/round-1.jpg`. Blender made six source views; the private memory-only browser on port 23953 made twelve High/Performance wall/wash views, eight course views, and six A/B/C placements with zero warnings and zero errors. The sheet checks source hashes and five numeric wall camera matches; its wash row labels the approved canyon crop as environment context and distinguishes the Blender source module from actual game joined banks. The 120-frame within-bundle visibility ratios in `captures.json` are diagnostics and are **not** the approved A1/B/A2 P2 frame comparison. Visual score and further art direction await independent review; a passing geometry test is not a likeness verdict.

Director's independent round-1 score: wall likeness 2/5, readability 3/5, grounding 3/5, consistency 3/5; wash 3/3/3/3, frame unmeasured. The wall reads as a broad corrugated fence because the five 13 m car bays occupy only about 65 m of its 420 m span and only the lower roughly 20 m of its 35 m core. Their nominal projection covers 1,300 m² of about 14,700 m² facade, under 9%. Wide horizontal steel panels, peaked crowns and small tower caps dominate. The wash game view still has a plain upper terrain mass above the striated joined bank. This candidate is not approved as likeness polish. Keep round 1 and its scores immutable; change the method before round 2.

## Revised round-2 method, decided before implementation

Use **source-car-derived 2.5D salvage relief** in larger recessed bays, alongside the existing true 3D welded cars. Render actual welded F42/Banshee stacks in Blender from a fixed side-facing orthographic camera into reserved regions of the existing 512-square `hulks` color and normal atlases. Each region must retain readable hood/cabin/trunk, dark glass and two tire hollows, with varied row angles and rust masks. Transfer that color plus aligned height/normal relief to a shallow broken panel mesh in the `welded-car-hulks` material batch; it must be clearly behind real 3D cars, not printed on the wall's front plane. This is authored offline, seeded and deterministic, with no runtime source-car load, new atlas, draw or dependency. Keep the actual source-car metadata and the 120 cheap/7 detailed geometry until the probe shows where to reallocate. Reuse the relief with different section widths, flips and partial steel occlusion; do not tile one identical car image across the wall.

The atlas has sixteen actual 128-square tiles after the generator's 1024-to-512 downsample. Actual 3D cars use body tiles **0,2,4,5,13**, wheel **3**, glass **8** and trim **9**. Those eight tiles and their current paint/UV contracts remain untouched, including the independent wheel test's tile-3 selector. The only contiguous 256-square free block is tiles **6,7,10,11**: atlas pixels x256..511, y128..383. Reserve its inner x260..507/y132..379 (4 px padding on every edge) for a primary 248-square source-stack image with matching color/normal/roughness pixels. Four additional free 128-square tiles **1,12,14,15** can hold distinct local car-stack patches, each with a 4 px inset; no actual 3D car points into them. This corrects the earlier unworkable four-256-square idea before atlas code. The first one-bay probe must show whether the 248-square primary at game scale reads well enough; if not, stop and choose a different within-budget method rather than overwriting wheel/glass/body tiles. The exported relief mesh carries its UV region IDs and source-render SHA in node extras; an independent test checks all relief UVs are inside these padded regions and actual 3D car UVs stay inside the eight original tiles. It also checks the exported atlas dimensions and actual image pixels, since metadata alone cannot prove packing.

Target eight recessed vertical salvage zones spanning about 240 m total, with baked relief from y1 to y32. This covers roughly 7,440 m², about half the facade, while retaining substantive steel regions and the 9 by 7 m gate. Within that, keep actual welded car layers at selected foot, near-gate and skyline edges so 3D silhouette, wheel hollows and car-on-car contact remain visible in game. Reallocate triangles by removing the five upper blank steel panel stacks and unneeded flat horizontal sheets first. The first probe is one 24–30 m wide bay at the same wall camera scale, with one baked relief variant, one 3D car layer and grounded steel straps. Measure pixel readability, actual atlas resolution after GLB export, UV seam/no-repeat behavior, panel depth, and triangles before multiplying it. The second probe should place that bay beside a plain reinforced panel and a stepped upper salvage edge to judge rhythm. No full-wall export until Director review of these probes.

Prospective triangle ledger from the measured 58,573-triangle round-1 export: reduce cheap 3D cars from 120 to about 96 across eight zones, saving **6,096** triangles at 254 each; keep the seven detailed heroes but reposition several to the near-gate and upper breaks rather than add seven more. Eight shallow relief grids at no more than 250 triangles each add at most **2,000**, yielding about **54,477** before frame reshaping. Reserve the remaining roughly 5,500 under the unchanged 60,000 cap for thicker connected uprights, stepped steel salvage and any required visible 3D car silhouettes. The one-bay proof must measure its actual grid and image cost before this allocation is used. If the relief cannot carry full-size bay readability, this plan is rejected; geometry counts alone do not justify it.

Make tall vertical reinforced bays, not roof triangles. Reuse the twelve uneven section edges but group some into eight wider salvage zones. Keep steel uprights and large diagonals physically grounded; vary their width/depth. Build two or three thicker lookout/scaffold clusters with real connected supports and let upper car/steel piles make a stepped skyline within the existing vertical envelope. Remove the repeated pointed crown sheets and distribute their saved triangles to selected 3D silhouette cars along the roof and near the gate. Keep the original core, gate clearance, 60,000 triangles, 24 draws, three 512-square atlas sets, all-route collision/containment, and deterministic behavior unchanged. This is a prospective geometry/paint plan; the previous 58,573-triangle export is the measured starting allocation, and actual removals/new triangles will be counted in the probe before committing to a full round.

The first ignored `--p2-relief-probe` export passes Crew's independent actual GLB/atlas contract 1/1: the source PNG hash is real, two car source hashes match, UVs stay in the padded 248-square region, all eight 3D car palette tiles have pixel-identical color/surface/normal data to round 1, and the shallow relief shares the hulk material. Its front/quarter proof is **visually rejected**. The staged cars form a clean five-column, fifteen-row showroom grid with light grey gaps; they read as icons printed on a wall, not a compressed salvage pile. This is a test green but art red. Do not copy the first source render into the full wall.

Second probe approach, written before altering the source staging: compress about six columns by twenty uneven rows into the same 30 m bay image, with adjacent hood/roof contact at roughly 1.55 m vertical and 5.1 m horizontal steps. Vary each car's yaw/lean/crush and depth by deterministic row/column transforms; fold most tall spoilers, show a few tire/end/quarter faces, and let small rusty real geometry occlude residual holes. Put a dark interior surface behind the stack and use ambient occlusion plus restrained side light so overlap reads as load rather than tidy gaps. Keep the exact source cars, palette/UV/texture size, color/normal packing and independent atlas test. The updated ignored proof includes visible grounded steel uprights, a front 3D car layer, a close front/quarter and a wall-scale in-frame view at the game comparison's car-pixel density. Director reviews that entire bay before any full-wall export.

The revised ignored proof now has 120 source-render-only overlapping car copies over a dark recess, while its export contains a 128-triangle shallow mapped relief, two real front cars (508 triangles) and grounded steel uprights. The atlas/UV/source independent test remains 1/1 green with all protected 3D car pixels unchanged. The recorded source render SHA-256 is `d28f8e6a38e0a1d3b26831adb1bb18ecc3c4445b7ef070e0b12dbe6c02f91a3b`. Its close image is much denser than the rejected grid; the 960×720 wall-scale proof shows a dark salvage texture with less distinct individual cars. This proof has been accepted as a method foundation, not a scored game view.

Director accepts the denser proof as a foundation for round 2, without a likeness pass. Full-wall allocation before code: use eight unequal section interiors (indices 0,1,3,5,6,8,10,11) totaling about 282 m of the 420 m span, with each relief behind connected front steel. Reduce actual cheap cars to **twelve per salvage zone (96 total)**: six touching near the foot, three supported by a cross-frame near y14 and three breaking the upper plane near y27. Cross-frames join uprights that run to y0; the cars are seated on those frames rather than floating. Keep seven detailed cars, four near the foot and three at prominent higher/near-gate silhouette breaks, with real shelves/load paths. Remove the five lower-car-only 13 m bay subdivisions and broad six-row steel sheets in those eight zones. Replace the pointed crown profiles with stepped steel and uneven car/scrap edges, retaining at least six distinct skyline heights. Reuse one exported relief mesh and one hulk atlas material batch; actual saved/additional triangles must be reported from the exported GLB before any round-2 score. Crew will add a red full-asset relief/coverage test before this runtime export. The visual risk is repetitive dark wallpaper across eight zones; the matched game sheet must decide that, and another authored paint method may be needed if the 3D interruptions do not resolve it.

The first full relief export measures **53,180 actual wall triangles / 14 draws**: welded cars 32,938, relief 1,024, scaffold steel 14,608, wall body 552, details 1,986, gate 120, guards eight ×244. This is 5,393 triangles below round 1 and 6,820 below the hard 60,000 cap. Crew's new full-wall relief test was red on the old GLB and green after export; the complete focused P2/probe/P1-scene/assets tests pass unchanged. The immutable round-2 Blender and private High/Performance game captures are in `.evidence/2026-09-24/rustwall-p2/round-2`; private port 26482 reported twenty screenshots, zero warnings and zero errors, and the published sheet is `docs/board/looks/rustwall-p2/round-2.jpg`.

**Round-2 visual failure found before a likeness score:** Blender shows the source-car relief but the game shows blocky dark structural material behind it. The embedded wall and probe hulks color PNG bytes have identical SHA-256, so the painted atlas was not overwritten. The relief quads were authored bottom-left → bottom-right → top-right → top-left in glTF X-right/Y-up coordinates, giving +Z normals, away from the game inspection camera at -Z. Blender displayed the backs; the game's front-face culling exposes the core instead. Preserve the failed round-2 sheet. Crew is adding a focused actual-GLB outward-winding red check for all eight zones before correction; reverse the relief face order in both offline probe and full wall, rerun unchanged safety tests, and make a new immutable round-3 capture. Do not silently recapture over round 2 or claim its appearance as the intended art.

Crew's exported winding check was red on the old full wall (0% front-facing area in zone 0) and green after reversing relief quad order in both full wall and offline probe. All existing P2, P1 scene/assets, UV and wheel checks stayed green. Round 3 was captured immutably with twenty private-browser screenshots, no warnings/errors, but the game still showed the palette blocks. A decoded embedded GLB PNG crop at x260..507/y132..379 contains the **old checker palette**, while the on-disk generated `art-build/rustwall/hulks-color.png` crop contains the car-source image and Blender's in-memory preview shows that image. The packed Blender image attached to the exported material is stale even though its edited pixels were saved on disk. Crew is adding an independent actual embedded-GLB-versus-authored-atlas red check for color/surface/normal relief pixels before the material is unpacked/repacked. This corrects the earlier inference that the matching wall/probe GLB atlas hashes proved the bake survived export; both matching hashes were the same **old** atlas. Round 3 is a second failed technical capture, not a likeness verdict.

Crew's exact exported-pixel test was red: the probe GLB's reserved color region differed from the generated authored PNG in **182,446 RGBA channel samples**. The generator had packed the initial palette image in `material()`, then changed and saved image pixels without replacing the packed bytes that glTF embeds. P2 now discards the old pack before editing, saves each atlas PNG and explicitly packs its exact encoded bytes. A subsequent 1–3 channel mismatch came from comparing the wall to a **later separately rendered** probe's overwritten shared atlas file, rather than either GLB's own source output; EEVEE varied those few one-unit samples even with dither disabled. Root forbade a duplicate non-runtime PNG under `public`. The generator now writes ignored per-build source renders and atlas snapshots with SHA-256 in GLB extras and probe manifest. Crew's independent test rebuilds the full wall and probe into separate ignored directories, checks each GLB's embedded color/surface/normal pixels against **its own** pre-export snapshots with zero differences, and verifies the committed runtime wall/wash hashes remain unchanged. It passes 2/2. The new isolated `--output-dir` CLI was initially wired before independent path tests; no false red-first claim is made for that CLI. Crew then verified its Blender-free path planning, public/outside rejection and isolated output. The source-car render method itself can have minor cross-run pixel variation; the committed runtime GLB and each test build have recorded exact provenance.

The actual candidate was rebuilt with the corrected packed atlas and captured as immutable round 4 in `.evidence/2026-09-24/rustwall-p2/round-4` and `docs/board/looks/rustwall-p2/round-4.jpg`. A private memory-only browser on port 12340 made twenty screenshots with zero warnings and zero errors. The full-size High front now shows the source-car rows in the recessed bays, as Blender does; the old checker palette is gone. Director scored wall likeness **2/5**, readability **3/5**, grounding **3/5**, consistency **3/5**; wash **3/3/3/3**, frame unmeasured. The wall reads as warehouse shelving: repeated rectangular bays and dominant X braces, blue/orange strip panels, a thin straight upper edge and a small gate relative to the whole scene. This is not a polish acceptance. The wall is still 53,180 exported triangles / 14 draws and wash source module 144 / 1; these counts and a passing content check are not a likeness or whole-frame pass.

## Next composition change, before code

Preserve the 420 m structural span, 35 m core, exact 9 by 7 m gate opening and moving panel, and all all-route collision/containment contracts. Change the **central approximately 60 m** first (x -30..30, with the clear x -4.5..4.5 passage): make a dark, armored gate tower centered above the existing opening, with a 25–32 m wide upper silhouette that rises from y35 to about y46 and two grounded flanking supports. Use the current steel/hulks atlas, reassigning existing dark steel/rust islands rather than adding a fourth set. Put real 3D welded cars and broken steel at three distinct depths (front roughly z -4 to -6, recessed relief z around -1, core behind at z2.5), with stepped/sloped salvage edges that overlap the neighboring zones instead of ending at one rectangular frame. The central gate itself remains fully clear and operable; no prop crosses its 9 m width below y7. Make the central dark mass cover perhaps 45–55 m of horizontal span at the upper levels so the opening is visually framed rather than isolated in an even fence.

In the central test section, remove at least half of the long crossing X braces visible across x -30..30 and replace their load with thicker near-vertical posts, short diagonals tied to y0 and overlapping crushed-car stacks on one side. Leave some brace rhythm elsewhere for structure, but avoid an eight-bay repeated poster treatment. Break the top edge with irregular stacked body/plate profiles at heights about y35,38,42,46, varying by side; the structural core remains y35. Subdue large blue/orange sheet blocks by reusing darker mixed iron/rust atlas regions and limit bright plates to small interruptions. Reallocate the measured 6,820-triangle headroom primarily to the central silhouettes and deeper support; remove existing unproductive crown boxes and wide sheet faces first so the hard 60,000-triangle /24-draw/three-512-atlas ceiling remains. Make a preflight full export of this central change, inspect actual counts/clearance and matched Blender/High/Performance central images, then obtain Director score **before** applying the language to outer sections. The already recorded round4 GLBs/sheet and review remain the reversal point. This is a new macro composition method, not a small color tweak.

## Central portal proof, round 5

Crew first proved the old exported portal red: the right upper mass and center lintel had only 38 and 22 m² of projected area, and four central long diagonal braces remained. The unchanged geometry test requires more than 60 m² in each upper zone, grounded flanks, less than 8 m² in the fixed clear 9 by 7 m opening, and no more than two long central braces. The first new export met the mass/opening checks but still had four long spans. The source then removed the two old gate X pairs for P2 only and shortened the new central load paths. Crew's unchanged exported portal test now passes. The final central proof has 57,072 exported wall triangles / 14 material draws; wash source module remains 144 / 1. No collision, course or gate dimensions changed.

Crew also corrected one independent full-wall relief assertion after the reviewed per-build atlas source split. The old path literal was `art-build/rustwall-p2/relief-source.png`; the actual full-wall source is `art-build/rustwall-p2/wall-relief-source.png`. The assertion still checks that path, file existence and SHA-256. The focused full-wall relief test remains green. No coverage or threshold was removed.

The central-only candidate was captured as round 5 in `.evidence/2026-09-24/rustwall-p2/round-5` and `docs/board/looks/rustwall-p2/round-5.jpg`. A private memory-only browser on port 50910 made twenty High/Performance screenshots with zero warnings or errors; the sheet includes the actual joined wash and six all-route placements. The subsequent unchanged P1 asset test found that the added crown had increased the `wall-body` node's height from its fixed 35 m core to 46 m. The three crown profiles moved to the existing `scaffold-steel` node, retaining identical vertices and one steel draw; `wall-body` is again exactly 35 m. The regenerated export is still 57,072 triangles / 14 draws, and unchanged asset 9/9, scene 23/23 and P2 9/9 checks pass. The round 5 sheet shows the identical geometry but predates this node grouping correction; a later final capture must use the corrected export. This is a geometry and visual proof pending independent Director score, not a frame or likeness pass. No outer section composition was propagated.

## Next central method, before code

Director scored round 5 wall likeness 2, readability 3, grounding 3, consistency 3; Crew independently scored likeness 2, readability 3, grounding 2, consistency 2. Both saw a thin crowned sign above two regular shelf grids, with cars perched on top. The wash remains 3/3/3/3. Do not spread round 5's crown to the outer wall. Keep the sheet as a rejected visual proof; its capture preceded the later node-only core fix.

Round 6 will replace the **central shelf frame** rather than decorate its edge. At the existing x ±5..15 gate flanks, make two broad inward-facing battered steel pylons, each about 8–10 m across at the foot, 35 m structural height with an irregular 38–43 m steel/salvage crown, and 2–4 m actual depth. Show real side faces in the depth camera, not a flat front polygon. Stagger overlapping vertical plates at several depths, widen the base and step/taper the inward edge above the 9 m passage. Their front faces use the existing dark iron/rust steel atlas at a visibly different value from the recessed car relief, with narrow weathered seams and local dents; remove the seven broad alternating blue/orange horizontal gate-side slabs for P2. Use existing `scaffold-steel` for parts above 35 m so the named `wall-body` remains exactly 35 m high. A stout, deep lintel will connect the pylons over y7..12 and carry an uneven upper armored load at y35..44; its lower edge stays above the exact clear 9 by 7 m physical opening. The lintel should cast a real recess shadow, not form a flat sign.

Keep the source-car relief in the two central adjacent bays as a deep backing, but remove their regular two long shelves and row-like exposed hero displays. Place a smaller number of actual welded bodies at staggered height/angle in front of and behind the pylons, with contact to steel or adjacent crushed cars and no floating top row. Replace thin isolated gate rods with a few thick short load paths tied to the ground; preserve the existing tower height envelope. Keep x ±4.5 clear below y7, the moving panel, route/collision hashes, 420 m span, 60,000 triangles, 24 draws and three 512 atlases. Reclaim triangles from removed gate-side slab faces, central shelves and perched cars first. Use Crew's unchanged structural and P1 contracts plus a new independent exported-depth/pylon/lintel red test before the source change. Prove the central 60 m in the next matched full game round before changing outer bays. Round 4 remains the reversal point if this method does not improve likeness.

## Deep-pylon proof, round 6

Crew's new actual-export pylon test was red on round 5: left front metal area by grounded height band was `[0,36.9,64.5,40.2]` m² and right `[7.8,81.5,97.7,40.2]` m², against greater than 65 m² in each band; seven dominant horizontal levels remained. The round 6 source replaced P2-only striped flank slabs with two deep broad piers and a connected lintel, removed both central upper shelf rails, the six old rear rails and the displayed top cars, while keeping the original panel and core. The first pylon export still failed the long-brace and shelf checks because wide face triangulation and rear rails appeared as long lines. Subdividing each continuous pylon/lintel face into adjacent narrow steel strips and removing only the obsolete rear rails made Crew's unchanged portal and pylon regressions pass. This is a geometry change, not a test threshold change.

The exported round 6 wall has **48,223 triangles / 14 material draws** and wash source module 144 / 1, within all unchanged caps. Named `wall-body` remains exactly 35 m high. Matched Blender/High/Performance evidence is in `.evidence/2026-09-24/rustwall-p2/round-6` and the immutable review sheet `docs/board/looks/rustwall-p2/round-6.jpg`. The private memory-only browser on port 17704 captured twenty screenshots with zero warnings or errors, including six all-route placements. Visual score is pending independent review; the dark piers read stronger but the broad facade remains regular. Do not treat structural green as a likeness or frame pass.

## Pylon surface method, before code

Director scored round 6 wall likeness 2, readability 3, grounding 3, consistency 3: the two piers are grounded but read as blank dark strips with a cap over car-grid posters. Do not copy them along the wall. The first Blender UV inventory incorrectly called tiles 10/11/14/15 free because it used Blender bottom-origin V as exported PNG top-origin V. Crew independently audited actual glTF UV consumers: exported PNG lower-right tiles 10/11/14/15 are used by crown/rails, wall body and gate. The true unused contiguous 256 by 256 pixel block in the exported 512-square steel color/surface/normal maps is **upper-right tiles 2, 3, 6 and 7** (x256..511, PNG y0..255). All other twelve exported PNG tiles remain protected byte-for-byte. The paint region is x260..507, PNG y4..251 with a four-pixel outer gutter. No fourth texture set, new draw or material is needed.

Paint three adjacent full-height weathered armored-steel fields inside that free region, each **72 by 232 pixels** with bleed/gutters: x264..335, x344..415, x424..495, and exported PNG y12..243 in the 512-square embedded atlas. Blender raw image rows y268..499 and original Blender UV V268..499 become PNG/glTF y12..243 after vertical conversion. Map one strip to each roughly 10 by 35 m pylon face, giving about 7.2 horizontal and 6.6 vertical texels per metre so rivets remain nearly round. Use the third strip rotated for the roughly 29 by 6 m lintel: 232 pixels over 29 m horizontally and 72 over 6 m vertically. Include broad rubbed iron versus rust patches, uneven vertical plate seams/rivet lines, dents with aligned normal variation, and downward oxide runoff; no uniform color noise. Every pylon front band gets UVs from its full physical x/y position, continuous across the narrow structural strips and four height bands, rather than resetting one tiny tile on each piece. Its side faces and 2–4 m thick structural boxes use existing darker tiles, producing depth and a different value from inset salvage. Paint/normal/surface pixels come from a SHA-verified explicit generated trial input, or a labeled deterministic procedural fixture when no trial input is selected; they are packed into the existing steel GLB material. Runtime reads only the GLB. Keep the complete physical gate, wall-body35m, 420m span, 60k/24draw/three512, and all unchanged pylon/portal tests. Crew will first add an independent actual-export red test for protected atlas bytes, nonflat painted region/UV use, physical-aspect mapping and full-height front coverage, then the builder will make one central-only proof and matched game round. Use the measured **11,777-triangle headroom** for a few real seams/beams and dent silhouettes rather than more broad flat sheets. If this remains likeness2, restore the pre-P2 runtime GLBs from baseline 5a994ad and re-slice unfinished art instead of spreading the failed method. The round4 checkpoint is an engineering reversal for relief, not a visually accepted runtime wall.

## Removed

The replaced regular facade tiles/rivets and uniform wash-band recipe will be removed in the same generator/runtime edit when the new assets pass. The prior runtime GLBs are replaced in place; older binary history is retained unless Kyle separately approves compaction.


## Generated steel source for the strip trial

Director used the built-in image tool for a diffuse paint source. Ignored local input: `art-build/rustwall-p2/steel-paint-source-1.png`; SHA-256 `817aedd97c42b69f57736e308e302c47ea8d5940512b0ad72e6519a8ea23c2ed`. It contains three adjacent weathered vertical variants. Treat this as optional trial source, verify bytes before use, preserve atlas allocation and use deterministic synthetic inputs in tests. Existing raw source stays ignored; no source PNG belongs in public. The exact generation prompt is:

```text
Create a production diffuse/albedo texture source for original gritty post-apocalyptic game steel gate towers. One SQUARE image, divided invisibly into THREE adjacent equal-width full-height vertical texture strips. Each strip depicts one different continuous column of battered overlapping reclaimed steel plates, approximately 9 metres wide by 35 metres tall in physical appearance. The three strips fill the image edge to edge; no gaps, borders, captions, numbers or labels. Each individual strip is narrow and tall, with correctly proportioned mostly round small rivet heads and bolt holes, broad staggered vertical steel panels, occasional horizontal overlapping seams, scraped paint, small impact dents, scaly rusty edges, rain runoff and abrasion. Ensure details are physically coherent within each narrow strip, not an image of an architectural tower. Palette: dusty weathered grey steel, warm muted brown rust and faded desaturated olive paint, readable midtones, not large flat black areas or bright orange. Roughly 5 to 8 broad plate courses vertically, asymmetrical seams, worn fasteners tracing plate edges. The three variants should be related materials but have different plate arrangements and wear. The left strip is grey iron with patchy brown rust, the middle strip faded olive with bare metal and rust, the right strip warmer brown iron with grey scraped edges. It must read as heavy scavenged plate construction after reduction to roughly 72 by 232 pixels per strip, so emphasize broad material variation and seam organization instead of microscopic noise. Perfect straight-on orthographic surface texture with no perspective, scene, sky, ground, shadows from other objects, scaffolding, cars or people. Flat neutral diffuse illumination: no baked directional highlights or directional cast shadows; the game supplies lighting. Fully opaque edge-to-edge texture, no watermark, logos or text.
```

The generated source is selected only by explicit `--steel-paint art-build/rustwall-p2/steel-paint-source-1.png --steel-paint-sha256 817aedd97c42b69f57736e308e302c47ea8d5940512b0ad72e6519a8ea23c2ed`. Validation of an existing PNG inside the ignored P2 art-build tree and its exact hash occurs before Blender import or output-directory creation, including `--paths-only`; a path or hash alone, public path and outside path are rejected. Without both options the generator uses a clearly labeled deterministic procedural fixture, never silently the nearby source file. The steel GLB node records selected input kind, root-relative source path/hash when supplied, and the three actual exported PNG UV rectangles. Crew's independent Blender-free CLI red and isolated actual-export provenance red precede implementation. The generated source stays ignored; if the painted asset is eventually approved, the committed GLB carries its embedded atlas and a later default rebuild must extract it or require an explicit source, rather than claiming the ignored PNG survives clean checkout.

Round 7 is **failed UV-placement evidence**, not a scored likeness round. It loaded the SHA-verified generated source and embedded color pixels that visually matched an independent downsample, yet the High view showed old green/rust blocks on the pylons and paint spilling onto crown/rails. Winding was independently checked and already faced the glTF -Z camera. The earlier steel test had reconstructed PNG row as `1 - exportedUv.y`; GLTFLoader with `flipY=false` actually samples embedded PNG row `exportedUv.y`. Crew changed that coordinate assertion, retained the same protected-pixel and projected-area thresholds, and reproduced zero painted pylon coverage. An actual consumer audit found that PNG lower-right tiles 10/11/14/15 were occupied; the upper-right tiles 2/3/6/7 are unused by the **current round 6 pylon geometry** after its old flank slabs were removed. The generator now writes Blender bottom-origin rows 268..499, which export to PNG top-origin y12..243, matching the existing glTF pylon UVs. The first corrected export missed the y0..8 foot because its UV endpoint rounded to PNG row244 outside the padded interval; a 2-pixel UV inset now covers the full 35 m pylon. Crew's unchanged corrected atlas test passes protected used-tile hashes, actual camera-facing pylon/lintel coverage, source aspect and variation. This is a mapping fix, not a new paint pass.

Director's round 8 score after the corrected explicit generated input is wall **3/3/3/3** and wash **3/3/3/3**, frame unmeasured. The weathered grey/rust/olive plate seams now appear in the actual High and Performance pylons; the prior green/blue blocks and paint spill are gone. This is an improvement from likeness 2 but remains below the required 4. The wall still has a giant zigzag brown cap and repeated flat car posters beside the gate. Private browser round 8 on port 12136 captured twenty views with zero warnings/errors; wall 48,307 triangles / 14 draws. Crew's isolated source-to-embedded pixel comparison is green on test-owned input (per-strip RGB mean absolute error below three), as are explicit path/hash/atlas-rect provenance and protected used-tile hashes.

## Round 9 composition choice, before code

Spend the remaining two review rounds on **massing**, keeping the accepted painted atlas. Remove the three wide zigzag cap profiles above the central gate and replace them with a deep plated lintel across roughly x -15..15, y35..42, and two distinct grounded upper tower tops at x about -11 and +10. Each top grows from the existing full-height 2–4 m deep pylon, rises to about y44–46 within the current tower envelope and has a small scaffold platform and short practical brace; neither floats or becomes a wide brown roof sign. Use the painted third strip across the deep lintel face and side steel/iron tiles on its visible depth. Keep the 9 by 7 m opening and moving panel untouched. Crew's unchanged upper-mass/grounded-flank/low-X test still applies; add a red actual-export check for two separated tower tops, deep center lintel and no wide triangular cap before source edits.

In the two adjacent source-car relief zones (indices 5 and 6, x about -38..-4.5 and 4.5..39.5), interrupt the flat car rows at several heights with 3D welded F42/Banshee bodies at unequal x/z/y and lean. Give each body contact to a short grounded vertical steel load path, a touching lower body or a local bracket connected to an upright; avoid continuous showroom shelves. Extend selected weathered-steel full-height strips to two farther non-relief vertical masses, while replacing the worst six-band blue/orange sheet faces with staggered tall panels and deep shadow gaps. Keep these additional masses within x±210, the existing total height envelope, source-car provenance, relief material/UV, <=60,000 wall triangles, <=24 draws and three 512 atlases. Reclaim old cap and broad stripe triangles before adding welded silhouettes; record actual export allocation. The first round 9 proof remains central plus adjacent bays; only if independently improved should its panel rhythm extend farther. If round 10 still misses likeness 4, re-slice residual art and retain the last visually approved runtime wall rather than claim a polish pass.

## Round 9 candidate and measured checks

The immutable `round-9.jpg` sheet compares the reference, Blender, High and Performance. Private memory-only browser port 55829 captured 20 images with zero warnings or errors. The first central proof replaced the three zigzag cap meshes with a deep bridge and two attached tower tops. Six welded source cars interrupt the adjacent relief bays at different heights; short perches meet grounded uprights. The accepted painted steel atlas and wash are unchanged. Export inventory is 55,866 wall triangles and 14 draws (scaffold 15,636; welded cars 34,764; wall core 384; relief 1,024; remaining details and guards 4,058), below 60,000 and 24. Wash module is 144 triangles and one draw.

The new tower assertion was red on the round 8 asset. The first round 9 export then failed the central mass and top-zone checks because their triangle-centroid sampling excluded a single large bridge face at x +/-5 and a tall tower face whose centroids fell below y43, although the actual surfaces crossed both zones. The bridge was split into six touching x segments and each tower into two touching height bands without changing its outside surface or gate opening. Crew then corrected the new tower test to clipped triangle area in the same spatial bands and added a two-triangulation invariant; thresholds were not lowered. Its original round 8 failure is not retained under this revised sampling, so the first red is evidence for the earlier assertion only. The full focused suite passes 24/24 in 85.72 seconds, including asset, scene, source, atlas, sheet and wash checks. Director and Crew independently scored wall 3/3/3/3 and wash 3/3/3/3; frame remains unmeasured. The bridge improves the gate silhouette, but dark regular car recesses and wide blue/orange bands still hold likeness below four.

## Round 10 outer-panel choice, before code

Keep the round 9 gate, car layers, wash, 9 by 7 m opening and explicit steel source identity. Replace the dominant repeated blue/orange bands in the four non-relief facade sections (indices 2, 4, 7 and 9) with staggered weathered steel plates and dark recessed joints. Reuse the three already embedded 72 by 232 pixel steel strips; do not create a fourth material or atlas. A strip paints one physical panel about 9–11 m wide and 30–35 m tall, so its UV density and width/height aspect remain close to the central pylons. Split each outer section into three unequal vertical masses, with only two painted continuously and one broken by staggered low rust/iron sheets and shadow. Set adjacent fronts at different depths by roughly 0.5–1.5 m and vary the strip source variant. Keep some original sheet edge/rust material as relief, but reduce the uninterrupted bright horizontal rail line. Use deep joints and existing source-car silhouettes to break the plate edges; do not add another grid of braces. At this stage the steel source PNG is already baked into the runtime atlas. New facade UV consumers are intentional and must be checked against protected palette pixels and source provenance, while the original atlas texels stay unchanged. The expected export remains under 60,000 wall triangles, 24 draws and three 512 textures. Crew will first add an actual-export test for painted outer coverage, sane physical UV aspect, dark breaks and preserved gate/budgets. Then build only the final round 10 candidate and review matched Blender/High/Performance. If likeness remains three, record residual art as a next polish card rather than mark this card likeness complete.

## Round 10 final art verdict

Crew's actual-export outer-panel acceptance was red on round 9 and is green on round 10 without changed thresholds. Eight physically scaled painted plates in four outer sections reuse the existing three steel atlas strips. Dark iron/rust sheets and joints interrupt them. The generated source PNG is unchanged; only new UV consumers use the existing embedded pixels. Exported wall is 56,122 triangles / 14 draws; wash module remains 144 / 1. A private memory-only browser on port 6631 captured twenty views across High, Performance and six route placements with zero warnings or errors. All focused art/scene/source/atlas/sheet tests passed **25/25 in 50.31 seconds**. `round-10.jpg` and its individual review note retain the matched evidence.

Director and Crew independently score the final wall and wash **3/3/3/3** for likeness/readability/grounding/scene consistency; frame is not yet scored. The added paint improves the outer rhythm, but repeated rectangular bays, straight gate columns, thin perched cars and artificial wash strata still fall short of likeness four. The ten-round wave stops here. EGG-02-P3 should explore denser interlocked 3D salvage and irregular grounded tower silhouettes, with more natural joined strata. P2 can be reviewed as a measured development improvement only after the frame and lane/build gates pass; it must not be described as completed likeness polish.

## Frame helper and changed comparison assertion

The new QA-only `tools/scenarios/rustwall-frame.mjs` compares pinned pre-card baseline `5a994ad` with this frozen P2 asset through the complete production renderer. Crew first proved the helper missing with two red unit tests. The helper now records 20 warm and 600 ordered RAF/full-render CPU samples for High and Performance at stopped Route A wash progress 350 and gate approach 960, including actual state, camera matrices, canvas, source/asset hashes, draw/triangle counts and mirror strata. The independent validator and two-baseline comparator tests pass 2/2. A private candidate functional dry run passed on port 50936; baseline dry A1/A2 passed on ports 58285 and 41429, all with memory-only storage and zero warnings/errors. Their timing values are **diagnostics only** because another art browser capture overlapped.

The first comparator required the exact Boolean mirror-refresh sequence to match frame by frame. Actual native baseline A/A dry runs showed refreshed counts of 300/300, 300/300, 300/300 and 300/299 across the four quality/views, but positions differed in 64, 0, 600 and 445 of the 600 ordered frames. The exact-sequence assertion therefore rejected comparable workloads due to phase drift. Crew added a red phase-inverted 300/300 fixture and retained a red 1/599 workload mismatch. The reviewed correction permits at most six refreshed-count differences out of 600 across A1/B/A2, requires at least 240 samples in each stratum, and exposes refreshed/reused CPU p50/p95 ratios against **both** baselines. The unchanged 1.10 cost limit applies to aggregate CPU p50/p95, RAF p95 and each mirror CPU p50/p95. The correction does not alter the production clock or erase unfavorable dry measurements. A pure comparison of those dry reports succeeds, but baseline High-wash CPU p95 drifted 23.5% while other work ran; no P2 frame verdict is drawn from them. The separately controlled final A1/B/A2 results follow below.

## Final quiet frame and lane verdict

Director granted a quiet window after all other heavy jobs stopped. Final A1/B/A2 used pinned baseline `5a994ad1b99f6a90734f960cb89dbd5eb26e6675`, exact candidate `d0e26a1fb55f4f532155aa52752ca79d0e73513c`, and byte-identical QA helpers SHA-256 `4aa070ae87a9996583ad4db4704a31d56a69620226500931bf1b2934e3f117a9`. Private memory-only browser ports 21018, 27318 and 32333 completed with zero warnings/errors. Each report has 600 ordered samples at High/Performance × wash/approach; raw A1/A2 reports are under the baseline's ignored `.evidence/2026-09-24/rustwall-p2/frame-final-a1` and `frame-final-a2`, while B and `comparison.json` are under the candidate's `frame-final-b`.

| View | CPU p95 B/A1, B/A2 | RAF p95 B/A1, B/A2 | Mirror CPU p95 refreshed | Mirror CPU p95 reused |
| --- | --- | --- | --- | --- |
| High wash | 1.034, 1.034 | 1.006, 1.006 | 1.033, 1.033 | 1.000, 1.000 |
| High approach | 1.000, 1.000 | 0.995, 0.995 | 1.000, 1.000 | 1.048, 1.000 |
| Performance wash | 1.045, 1.045 | 1.005, 1.000 | 1.043, 1.000 | 1.067, 1.067 |
| Performance approach | 1.000, 0.950 | 0.995, 0.995 | 1.050, 1.000 | 0.933, 1.000 |

The worst required ratio, including aggregate and mirror CPU p50 as well as the displayed p95 values, is **1.0833** (Performance wash reused-mirror CPU p50 versus A1). CPU p95 baseline A2/A1 drift is at most **1.053**. Native refreshed counts are 300/300/300, 300/300/300, 299/299/299 and 299/300/300 across those rows. Mean CPU B/A1 and B/A2, retained as diagnostics rather than a substituted gate, are High wash 1.025/1.041, High approach 1.015/0.989, Performance wash 1.033/0.993 and Performance approach 1.004/1.013. Draw median deltas are +1..+3; triangle medians decrease by 2,792..8,376. Director independently re-read all three raw reports, reproduced `comparison.json` exactly, and approved the **scoped frame score four** against the unchanged 1.10 CPU/RAF limit. This is full render submission CPU and delivered RAF timing, not GPU time or a visual likeness score.

After timing, the lane merged integration metadata without changing renderer, world or art. Required lane tier passed **256/256 in 375.19 seconds** and `npm run build` passed. `git diff --check` and focused review-evidence checks passed; race rules/saves were outside this card. Final wall likeness remains **three**, and EGG-02-P3 owns the unmet target four.

## Selected paint recovery

The generated 1254-square source is retained at `C:\Users\kyleb\.codex\generated_images\01a0d53e-3baa-7c12-883d-38d2395c6099\exec-3e9ad6d1-13e3-49dd-90bc-5b59ba3eb5d6.png`, independently verified SHA-256 `817aedd97c42b69f57736e308e302c47ea8d5940512b0ad72e6519a8ea23c2ed`. The ignored lane copy may be swept; janitor must preserve the original generated-image file. The committed runtime `wall.glb` embeds the selected baked paint. In an isolated checkout, `git restore --source=d0e26a1 -- public/assets/models/wasteland/rustwall/wall.glb` restores that exact retained asset. This is **asset recovery, not generator reproduction**. The generator's no-input `--p2` path is a labeled procedural test fixture and does not reproduce the selected image. EGG-02-P3 needs an explicit, hashed embedded-atlas extraction/reapplication recipe or a verified copy of the original source before changing this paint. No duplicate source image is committed or placed under `public`.
