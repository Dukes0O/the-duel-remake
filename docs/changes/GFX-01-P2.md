---
task: GFX-01-P2
status: design
kind: art
flag: wasteland2
player_facing: yes
---

# Rook likeness: authoring contract before code

## Starting evidence and design choice

The round-10 Rook trial scored likeness 2 and was rejected. Its generator used rings, patches, a voxel union, subdivision and automated folds. Connected topology, padded UVs and a triangle budget passed, but the head stayed small above a long exposed neck, the vest looked like inflated columns, the rear pack floated, the trousers and boots were simple, and the face was weak in game. The reviewed round-3 runtime Rook scores 3/3/3/4/4 for likeness/readability/grounding/scene/cost. Its GLB stays byte-identical during this trial. The seven other crew GLBs and the current `tools/blender/crew-fighters.py` also stay unchanged.

Use a **committed, explicitly shaped mesh input**. A Blender artist may edit an ignored working `.blend`, but that file is not the source of record. Export its named control meshes, vertex positions, face indices, UVs, garment roles and bone weights into `tools/blender/rook-p2-source.json`, then rebuild the ignored Blender scene and candidate GLB from that JSON on a clean checkout. The rebuild script must not synthesize Rook's proportions from the old procedural rings or copy the rejected trial mesh. Scripted subdivision and export are allowed after the authored control geometry has fixed the silhouette. Paint uses authored garment regions and local wear, not full-view photo projection.

The reference is `public/assets/reference/wasteland-crew-1.png`, with Rook in the first front, side and back triplet. Before rigging or texture work, trace a small reviewed landmark input in `tools/blender/rook-p2-landmarks.json`: crown, hair width, jaw/chin, neck base, scarf hem, shoulder tips, elbow/wrist, vest opening and hem, large pocket base, belt, crotch, knees, boot shaft/toe/heel, and pack/strap anchors. Store each point in image-pixel coordinates for its view, plus the reference image SHA-256 and the calibration from floor to crown (the approved sheet has Rook floor near y650 and crown near y62; the rebuild must record exact chosen pixels). Render the neutral control mesh at matching full-size front/profile/back views and overlay it on the approved reference and round-3 baseline. A separate reviewer must accept the silhouette before the candidate stage may attach weights or bake paint. Do not infer likeness from vertex count, UV occupancy or a passing rig test.

The authored geometry has an integrated head/jaw/neck body core; separate, thin jacket, open vest panels and pockets, scarf wrapped under the chin, loose cargo trousers, tall narrower boot shafts with a separate toe and sole, and a curved pack with shoulder and belt contact. The sculpted surface must carry these features in front and side views. The pack must not be a flat board behind the torso. Each garment's near and far mesh has stable names so tests can measure its silhouette and contact. The current 1.83 m height, glTF axis convention, existing skeleton and twelve clip names remain the runtime contract.

## Clean-build input and output API

The first implementation slice ends at the **neutral silhouette**. It does not import the runtime rig, paint an atlas, export a candidate GLB or change any runtime file. Those steps need a reviewed neutral silhouette and a second red-test contract.

- **Committed source schema:** `tools/blender/rook-p2-source.json` has `{version:1, units:'metres', coordinates:'blender-z-up', height:1.83, meshes:[...]}`. Every mesh has `{name, role, lod:'near'|'far', vertices:[[x,y,z],...], faces:[[vertexIndex,...],...]}`. The required near roles are `body-core`, `jacket`, `vest-left`, `vest-right`, `scarf`, `trousers`, `boots` and `pack`. Names are stable; faces have valid indices and consistent winding. The body core connects the head and neck through shared mesh vertices, with no detached head or neck shell. Near control triangles total at most 8,000; far control triangles total at most 2,000. These are first-slice ceilings; actual subdivided and exported triangle budgets are checked in the later candidate slice. Later candidate fields may add UVs, material regions and bone weights without changing this neutral geometry. The JSON contains no binary images or copied reference pixels. An ignored artist `.blend` may be serialized into this committed JSON, but the clean rebuild reads only JSON.
- **Committed landmark schema:** `tools/blender/rook-p2-landmarks.json` has `{version:1, reference:{path,sha256,width,height}, height:1.83, views:{front:{crop:[x0,y0,x1,y1],crown,floor,points:{...}},side:{...},back:{...}}}`. Each view has observed image-pixel points for `crown`, `chin`, `neck`, `shoulder`, `elbow`, `wrist`, `scarf`, `vest`, `belt`, `crotch`, `knee`, `boot` and `pack`. A point can be a named pair or more precise left/right subpoints where the view supports it. Coordinates must lie in the stated crop and reflect the actual Rook reference; filling keys with arbitrary points fails the visual review. The first Rook triplet is about x112/287/441 for front/side/back, with crown near y62 and floor near y650 on the approved 2067×761 sheet; the committed data records inspected exact positions. The reference SHA-256 is verified before rendering. Landmarks guide comparison; they do not automatically score likeness.
- **Neutral build command:** `blender -b --python tools/blender/rook-p2.py -- --root REPO --stage neutral --source tools/blender/rook-p2-source.json --landmarks tools/blender/rook-p2-landmarks.json --output-dir art-build/crew/rook-p2`. `--output-dir` defaults to `art-build/crew/rook-p2`. The script resolves it against `--root` and allows outputs only inside that lane's ignored `art-build/` or `.evidence/` trees. It rejects `..` traversal, absolute paths outside those trees, and any route to `public/`, `src/`, reference files or the live checkout before making a directory. The neutral stage rebuilds an ignored `.blend`, produces full-size front/profile/back clay renders and a manifest, and produces **no GLB or textures**.
- **Read-only path query:** `--paths-only` validates the same resolved paths and prints one JSON object `{blend:[...],glb:[],textures:[],evidence:[frontRender,sideRender,backRender,manifest]}` before importing `bpy` or writing any file. This permits the safe-path tests to run without Blender. It must not create an output directory.
- **Neutral manifest:** records the source, landmark/reference and exporter hashes; Blender version; output hashes; coordinate system and height; per-level control-mesh triangle totals; exact camera/crop calibration; and render paths. The clean checkout must reproduce the same structural data and matched silhouettes. Candidate rig, UV, atlas, clips and GLB manifest fields are specified and tested only after neutral silhouette approval.
- **Later candidate stage:** after the silhouette gate, extend the same script with `--stage candidate`. It may import the existing reviewed Rook rig and clips from `public/assets/models/wasteland/crew/rook.glb`, bind authored near/far meshes and weights, bake the one-material 1024-square color/surface atlas set, and export `rook-p2.glb` only under ignored `art-build/` or `.evidence/`. It never modifies a production GLB. The later tests check actual exported triangle, UV, material and clip contracts.
- **Review loading:** `createRiggedFighterFigures({loadAsset})` in `src/rigged-fighter.js` already accepts `(crewId) => Promise<{scene, animations}>`. Structural tests can inject the candidate through that contract. A private browser candidate hook may copy `rook-p2.glb` into the ignored QA bundle and supply it to Rook only; the normal loader path remains `/assets/models/wasteland/crew/rook.glb`. The exact private hook is a later, separately owned slice before game capture.

The ignored `art-build/` and `.evidence/` directories hold intermediate `.blend`, later candidate GLB, atlases and raw captures. Only the script, JSON inputs, tests, compact review sheet and written verdict enter Git. This follows SPEC 0.7 and allows a clean checkout to reconstruct the candidate without a stored `.blend`.

## Acceptance sequence

1. Independent tests start red on absent neutral inputs/build script. Validate schemas, observed reference coordinates and hash, `--paths-only` with zero writes, output path isolation, unchanged baseline and other-seven hashes, required authored mesh roles, connected body core and control triangle ceilings. The independent tester adds rig/UV/atlas/clip and exported triangle checks only after the neutral silhouette gate. Tests check structure and safety; they do not assign a likeness score.
2. Review neutral full-size front/profile/back overlays before rigging or paint. Record visible differences from the reference and round-3 baseline. Adjust authored vertices and landmark decisions, then repeat the neutral review. The first approved neutral silhouette is the candidate for binding and painting.
3. Compare candidate Rook in matched High and Performance game views, idle and motion/contact views, with the round-3 runtime and rejected round-10 sheet. Check jaw/scarf/neck seams, vest opening/pockets, pack contact, trouser/boot shape, painted face and UV seams in front, side and back. Exercise all twelve clips, especially walk, fire, knockdown and get-up support contact.
4. Independent art review must score likeness at least 4, with readability, grounding, scene and cost at least 4, before replacing runtime Rook or building the other seven. Use a new immutable polish-wave review folder and its ten-round limit. Twelve-fighter crowd/frame measurements in both qualities must show no more than the SPEC 0.3 cost allowance; the Rook-only screenshot run does not prove that. First-person hands remain blocked until this Rook gate passes.

If likeness remains below four, keep the reviewed round-3 runtime GLB and document the next changed approach. No race rules, saves or simulation state change in this card.

## Neutral blockout checkpoint (unscored)

The independent neutral source tests began red 3/3 on absent files. The first committed control-mesh JSON, actual reference landmark JSON and safe path planner now pass 3/3. Blender 4.5.13 rebuilt an ignored `.blend` and front/profile/back clay renders; the current explicit control cage has 1,872 near and 184 far triangles, with no GLB or textures. `--paths-only` runs without Blender or writes and rejects paths outside ignored review output. The runtime Rook and other crew assets were not changed.

The ignored `art-build/crew/rook-p2/neutral-blockout-a.jpg` preflight uses the reference's 761-pixel height with the same crown/floor calibration in each clay view. It is **not accepted** and has no SPEC 0.3 score. I traced front and side torso, sleeve, trouser and boot outlines into the landmark JSON and narrowed the control mesh accordingly. The profile is closer in depth, and the back has the triangular scarf and lower waist bag. The model still reads as a simplified block figure: the vest lacks tailored cloth volume despite being a thin open surface, the scarf and neck remain geometric, trouser folds are too sparse, boots are straight shafts with a wedge toe, and the skull/jaw/hair detail is too angular. Structural tests cannot approve this silhouette. No rig, UV, atlas or candidate export should start from this blockout.

The next neutral edit must replace broad tube sections with individually shaped garment and anatomical surfaces traced to the reference's front **and** profile contours, especially the scarf shoulder slope, vest rib contact and lower pockets, trouser knees/cargo folds, and boot ankle/toe/sole. Show another matched unscored blockout before requesting independent silhouette approval. The first scored polish round begins only after this neutral shape gate.

## Changed neutral method before the next edit

The first blockout tuned symmetric width/depth sections and then added detail to them. That did not follow the reference's garment edges. The next source revision uses a **paired contour cage**: for each garment and anatomical region, authored pixel points at several heights give the front left/right silhouette and the profile front/back silhouette. The authoring workbench maps those points through the recorded crown/floor calibration into metre-scale x, y and z and emits explicit near/far vertices and faces in the committed JSON. Each section therefore gets its width, depth and centre from the observed image, including asymmetry; added vertices only interpolate between those controls. The clean Blender rebuild still reads committed explicit JSON and does not read an ignored workbench or reference raster to invent the shape.

The contour B render showed that upright boot sections plus a toe wedge cannot reproduce the reference's foot shape. It also exposed a mirrored review camera: the reference profile faces right, but the neutral side camera at +X projected the model's physical front (−Y) left. Before any further full-figure review, set the side camera at −X and author each boot as separate shaft and horizontal foot volumes. The foot uses explicit rounded sections from heel through arch and ball to toe cap, with an ankle constriction, cuff and distinct sole/heel block. The shaft and foot meet at the ankle, and the toe points forward (−Y) on both feet. Compare an isolated boot pair in front, matched right-facing profile and three-quarter views at the same height scale. This is a geometry change; do not use a profile flip alone to make the wedge pass.

Trace jacket shoulder, upper sleeve, elbow, rolled cuff and exposed forearm on both sides; left and right trouser waist, cargo thigh, knee fold, calf and cuff; the boot shaft, ankle, rounded toe and sole in profile; scarf at jaw, clavicle and back triangle; thin open vest at ribs, folded edge and lower pocket contact; and the small lower-back waist bag. The neutral render must show these changes in the same front/profile/back camera calibration. If the two references disagree because the stylized views are not exact orthographic photos, record the chosen point rather than averaging silently. No rig, paint or candidate GLB proceeds until the neutral silhouette is independently accepted.

## Isolated boot correction

The source now contains two connected rounded foot and tapered shaft control meshes, each with a separate fitted outsole, lower heel block, cuff, tongue and thin crossed laces. The physical front is `-Y`; the side review camera is at `-X`, so Rook faces right as in the approved profile. An early boot render had thick horizontal lace bars and an unattached-looking sole. The revised ignored `art-build/crew/rook-p2/boot-{front,side,three-quarter}.png` views show a forward rounded toe, narrower ankle, raised rear heel and crossed laces. The boot is a usable neutral shape, not a scored likeness or final leather material. The whole figure still needs head, scarf, vest and trouser work before neutral acceptance. `node --test tools/test-rook-p2-source.mjs` passes 3/3 after the boot edit; near control triangles are 3,312 and far 184. No runtime asset changed.

## Next head and scarf contour pass

The full clay view still has a blank angular egg head and a stiff scarf that projects as a pointed chest block. The approved reference has a rounded but uneven skull under loose hair, a narrower jaw with a visible brow/nose/chin profile, and scarf fabric touching the jaw before widening over the clavicles. I will reshape the existing connected body-core head section from observed front and profile face levels, then add small surface planes for nose, brows and hair locks. The scarf will use several uneven cloth rings at jaw, neck and shoulder height plus a short draped front edge, with no rigid straight shoulder plateau. The ignored authoring workbench will serialize those controls into the committed explicit JSON; the clean rebuild still reads JSON only. I will review the result at the same camera scale before changing the vest or trousers. The isolated boot shape was accepted by the Director for this next whole-body pass; it is not whole-figure likeness approval.

The subsequent ignored matched clay views show a narrower jaw and cheek, asymmetric side hair, a jaw-height scarf wrap with two overlapping front folds, and thin open vest cloth now visible in front of the jacket. The old vest panels were behind the jacket, exposing only large pockets. This is still an unscored structural pass: hair volume is uniform in profile, face planes are spare, vest panels are plain, and the trouser legs lack the reference's creases and cargo folds. The source contract tests pass 3/3. The neutral control source now has 3,744 near and 184 far triangles, below the first-slice ceilings. No rig, textures, candidate GLB or runtime asset changed.

The next ignored clay render adds separate asymmetrical cargo thigh flaps and angled knee folds while retaining the traced trouser outer contours. The pair now has 3,824 near and 184 far control triangles; the source tests still pass 3/3. These added cloth forms remain unscored and are not evidence of likeness by themselves. The Director has only accepted the isolated boot shape so far.

The remaining square shoulder shelf comes from the jacket torso mesh ending at the broad observed shoulder row without narrowing toward the neck. Before the next edit, extend that connected jacket surface upward through two measured neck opening levels, tapering from the shoulder row under the scarf. Keep the sleeve sections and physical body core unchanged. Review the front/profile/back clay for a sloped shoulder line; the scarf must remain in contact rather than float over a gap.

The latest ignored front/profile/back set includes the sloped jacket neck opening and keeps the scarf in contact. I also corrected the cargo flap and knee crease depths against the traced profile: the first side render exposed floating cloth strips several centimetres ahead of the leg, so I moved the forms back to within roughly one centimetre of the trouser front. Two repeated fold strips looked artificial and were removed; the remaining single knee crease and cargo flap are the reviewed neutral geometry. The current source is 3,832 near and 184 far control triangles. `node --test tools/test-rook-p2-source.mjs` passes 3/3. The full silhouette remains for Director review; no likeness score has been claimed.

## Independent neutral review and next contact pass

The Director rejected rig readiness. The main defect is garment contact: two broad vest boards float ahead of the torso, and the lower body core shows as a horizontal pelvis block. The lower-back bag is too large; knee-crease strips appear detached in profile. The next edit computes each vest cloth vertex from the jacket's elliptical section at that same height and width, with only 1–2 cm stand-off and a curved open front edge. It narrows the body core below the jacket so it stays inside the trouser waist, adds only a thin belt, softens and shrinks the lower-back bag, and removes the knee strips. A dedicated front/profile torso crop will show these contacts before another full-figure comparison. No subdivision is necessary for this pass; the actual control/evaluated budget remains below 8,000 triangles.

The ignored `torso-front.png` and `torso-side.png` closeups now show this contact pass. Vest rows have five width samples fitted to the traced jacket section at each height, with 1–1.7 cm offset; pocket fronts are fitted to that surface too. The top inner edges open into a measured V, and the hem rises toward the open edge. The body core no longer extends below the jacket/trouser overlap, eliminating the exposed pelvis block. The bag is smaller and lower; detached knee strips are removed. The vest still looks plain in unpainted clay, so the Director is reviewing the actual closeups before another whole-figure verdict. The source is 3,900 near / 184 far control triangles; no subdivision is applied. `node --test tools/test-rook-p2-source.mjs` passes 3/3; no runtime file changed.

The Director accepted the rib contact in these closeups, but the neutral silhouette is still not ready to rig. The next controlled edit will subdivide only jacket, vest and trouser surfaces once, with support rows around openings and hems so the garment boundaries stay in place. The smoothed result will be baked into explicit committed mesh vertices/faces, not left as a renderer-only modifier. Both source and actual evaluated near triangles must remain below 8,000; independent source tests and matched front/profile renders will check the result. The scarf will replace two pointed bowtie flaps with layered continuous fabric bands. Their front edges dip in a U below the jaw while the sides rise to the neck, and the lower band follows the clavicles. This keeps fabric contact without a separate central spike. These are neutral geometry changes only; no rig, texture or runtime export starts until an independent silhouette review accepts them. Work is paused for the Director's yard timing quiet window before either edit is built.

After the quiet window, the vest gained close support rows and width samples. One Catmull-Clark level was baked into the explicit JSON for jacket, vest and trousers only; there is no runtime modifier. The actual near source is 6,424 triangles and far remains 184, below the limits. The two pointed scarf flaps were replaced by continuous layered bands whose front edge dips under the jaw. Ignored `torso-front.png` and `torso-side.png` show smoother shoulders and leg tops with the new wrap. The bowtie spike is gone, but the front cloth still reads as a broad smooth collar, so this remains unscored. `node --test tools/test-rook-p2-source.mjs` passes 3/3. The future paint/export recipe must normalize one active and render UV layer name before joining parts; this was identified in the separate kit export review, and no UV or paint work begins before the neutral gate.

The Director's closeup review confirms the scarf is still a rigid clavicle disk. After the yard timing quiet window, the next scarf-only edit will narrow the jaw and shoulder radii, move the upper band into neck contact, and use three continuous nested folds with front-centre dips that descend in steps to the upper chest. A sharper curve concentrated near the front centre will taper the lowest fold rather than create another wide shelf or detached triangle. Jacket, vest, trousers, boots, pack and head remain fixed for that comparison. The next review will use full matched front/profile/back renders; no further torso-only polish loop precedes the whole-figure verdict.

The matched ignored `neutral-front.png`, `neutral-side.png` and `neutral-back.png` now show three nested U-shaped scarf bands close to the jaw, with a tapered centre dip to the upper chest. The broad bowtie/disk is gone. The first full render also showed two front thigh flaps as knee pads and a thin floating edge in profile; I removed those invented forms and rebuilt the exact source and views. The final neutral source at this checkpoint is 6,376 near / 184 far triangles, including the baked single cloth smoothing level. `node --test tools/test-rook-p2-source.mjs` passes 3/3. This is ready for the Director's whole-figure neutral silhouette verdict, not yet a likeness approval or authorization for rig/UV/paint. Runtime crew assets remain unchanged.

## Second whole-figure silhouette review

The Director rejected rig readiness again. The approved back view shows a short shoulder scarf point around reference y≈190–215, shaggy hair to the nape around y≈140–150, and a compact waist bag. The current clay has a broad triangle to z=1.20, a bare rear skull and a flat jacket hem. Front and back shoulder/waist/knee/ankle widths and scarf/hem heights are now recorded as approximate inspected pixel pairs in `rook-p2-landmarks.json`; they are visual controls, not an automatic likeness score. One bounded edit will shorten and narrow the back scarf panel to upper/mid back, add irregular rear hair locks down to nape, taper and raise the jacket hem to belt height, and add measured knee/ankle levels to the trousers before the existing one-level smoothing bake. No face detail, materials, rig or runtime asset changes. The next evidence is one matched reference/clay whole-body comparison, with a review verdict before any other edit.

The bounded source pass is complete. The back scarf tip now reaches about z=1.39, near the measured reference y≈203. Six irregular back hair locks reach the nape around z=1.61–1.65. The jacket hem is raised and tapered to belt height. The trouser outlines include measured knee, calf and ankle levels before the baked one-level cloth smoothing. The lower-back bag is now a centre pouch with two smaller side pouches. The ignored `neutral-measured-comparison.png` places the approved reference and current clay side by side at the same vertical pixel scale; the component views and manifest remain beside it. Baked explicit source totals 7,392 near / 184 far triangles. `node --test tools/test-rook-p2-source.mjs` passes 3/3. My visual judgment remains negative for overall likeness: clay trousers are still smooth tubes, hair locks are visibly angular, and the plain vest/jacket mass is broader and stiffer than the reference. This evidence is for an independent review verdict, not a rig or paint gate. Runtime GLBs and old generator remain untouched.

## Joined trouser source contract before implementation

Independent review also rejected the neutral silhouette. The next source method first replaces the two disconnected leg tubes with **one** `rook-near-trousers` mesh (`role:'trousers', lod:'near'`). Its indexed faces must form one connected component from a shared waist/pelvis surface, across a modelled crotch bridge, and down both legs. At a waist band near reference front y327 (z≈0.994 m) the mesh spans both hips and the centre; the crotch branches near front y362 (z≈0.883 m). Below that branch, the legs separate visibly. A superficial vertex or degenerate face linking two otherwise independent tubes does not satisfy the design: the waist and crotch must have a substantial surface and no non-manifold pinched connection. The existing world axes stay Blender Z-up, left/right as the approved front crop, and physical front `-Y`. Source geometry remains explicit vertices/faces in `rook-p2-source.json`; no modifier is required at clean rebuild.

Add `views.front.trouserBands`, `views.back.trouserBands` and `views.side.trouserBands` to the committed landmark JSON before generating the mesh. Each band has a reference image `y`, a named level (`waist`, `hips`, `crotch`, `thigh`, `knee`, `calf`, `cuff`), and observed edge pairs. Front/back records have `left:[x0,x1]` and `right:[x0,x1]` where each leg exists; the waist/hips records additionally have `whole:[x0,x1]`. The side records have `backX` and `frontX`. The front and back image x axes are opposite in world space: front x maps through centre 112 as `(pixel-112)*1.83/578`, back x maps through centre 439 as `(439-pixel)*1.83/578`; all levels map `z=(641-y)*1.83/578`. Side pixels map physical depth with front negative Y as `(270-pixel)*1.83/578`. These observed bands guide a noncircular pelvis, thigh, knee, calf and cuff shape; an independent test can check source bounds against each band within a small image-pixel tolerance without prescribing exact triangles. The back widths must shape the back cloth separately rather than be discarded by an elliptical front-only ring.

The trouser mesh must narrow unevenly from thigh through knee and calf, gather above the already accepted boot cuffs, and preserve the approved front/side leg spacing. The source test should require one connected trouser component, real central pelvis/crotch surface at the named heights, and bounds at the landmark bands; the matched front/profile/back clay comparison still decides likeness. One bounded smoothing level may be baked to explicit JSON, with actual near triangles <=8,000 and far <=2,000. Detail budget can be moved from removable decorative panels; limits do not rise. Jacket/vest back-contour separation and curved rear hair clumps follow as separately reviewed neutral edits. No UV, rig, paint, candidate GLB or runtime swap starts from the current rejected silhouette.

Rustwall's independent test was red first: the previous `rook-near-trousers` had two face components. The new authored source has one edge-connected pelvis/crotch/leg surface, 952 vertices, 1,792 faces and 1,900 triangles. It passed 4/4 focused tests, with no non-manifold edges, 0.0423 m² central pelvis cloth versus a 0.008 m² minimum, separation below crotch, and all measured front/back/side bands at 10-pixel tolerance. Total near source is 7,468 triangles. The refreshed ignored `neutral-measured-comparison.png` gives a negative visual verdict: its joined pants read as wide flat front panels with a hard rectangular crotch and an overly thin side profile. This green structural test does not clear likeness. I am retaining the failing visual evidence and refining the joined cloth shape rather than moving to head/torso, rig, paint or runtime.

The comparison scale was audited before changing geometry. The ignored compositor crops the original 2067×761 reference at front x0–212, side x214–333 and back x339–530, then centres each unchanged crop in a 300×761 tile. It never stretches a view; each clay image is native 300×761. Reference scale is 578px/1.83m=315.85px/m; clay is 761px/2.37m=321.10px/m, a 1.66% scale difference. The ignored `side-trouser-band-overlay.png` draws the committed bands on native reference pixels. Knee, calf and cuff endpoints follow the trouser silhouette. The thigh band crosses a large side cargo pouch, so that band may measure an outer pouch edge rather than only the underlying trouser cloth. No landmark or test tolerance was changed during this audit.

Closer native-pixel review corrected that first impression: the original side endpoints were **inside** visible cloth at knee, calf and cuff, and the upper bands were obscured by hand, hip bag, holster or cargo pouch. The Director reviewed the ignored native-pixel `side-contour-proposal.png` and approved these corrected backX..frontX pairs: waist 242..298 (old 245..290, inferred under hand/hip bag), hips 241..300 (old 244..289, holster), crotch 240..301 (old 243..289, holster), thigh 240..300 (old 247..287, cargo pouch), knee 242..307 (old 251..284, visible cloth), calf 240..294 (old 251..282, visible cloth), cuff 245..291 (old 254..280, visible cloth). This is a calibration correction to the observed reference, not a test tolerance change. Update the landmark data, first prove the existing source fails the unchanged independent band test, then rebuild the joined cross-sections with those depths and review a new matched triplet. No other silhouette parts change in that pass.

The corrected JSON made the unchanged independent test red on `hips front trouser depth`, proving the old mesh was too thin. Before the next render, the Director also approved exact full-body camera calibration: orthographic vertical span `761*1.83/578 ≈ 2.40950 m` and target Z `(641-761/2)*1.83/578 ≈ 0.82477 m`. This maps the reference floor at pixel 641 and crown at 63 to the same native render rows and removes the previous 1.66% comparison scale discrepancy. Boot and torso detail cameras stay as they are. This changes review evidence only; it does not change runtime geometry, thresholds or tests.

The joined trouser workbench now rounds each front/back side turn, extends the pelvis into a tapered central crotch surface, and gathers the cuffs. Rebuilt against the corrected side bands, the same independent source test passes 4/4 with 7,468 near /184 far total triangles. The refreshed ignored `neutral-measured-comparison.png` uses the exact calibrated full-body camera. The side leg has visibly more depth, and the crotch is less square, but front and back legs still read as broad faceted columns; no neutral silhouette approval is claimed. This is the scoped trouser review checkpoint before any jacket/hair edit.

Checkpoint `b9a6770` preserved the accepted connected trouser and boot foundation. The next neutral edit uses separately observed front and back jacket edges at six height levels, plus the approved side-depth contour, rather than a symmetric front-derived ellipse. A closed rounded torso cloth shell will join both fitted sleeve shells across overlapping shoulder seams into one surface; it will taper toward the belt without a flat rectangular hem. The open vest keeps its 1–2 cm contact over the jacket but gets unequal front hems and a narrower shoulder attachment. Rear hair will replace six flat pointed panels with curved overlapping clumps whose width, depth and lateral centre vary through several nape levels. The explicit source remains the clean-build input, and the scored decision remains a matched whole-figure visual review. The near limit stays 8,000 triangles, so this pass will exchange prior jacket/hair faces for new shape rather than increase the budget.

The next ignored exact-scale `neutral-measured-comparison.png` contains a fitted jacket torso driven by separate front/back contour rows, sleeves joined across shoulder overlap, and five curved rear hair clumps replacing six flat spikes. The trouser/boot foundation is unchanged. Baked explicit source totals 7,628 near /184 far triangles and `node --test tools/test-rook-p2-source.mjs` passes 4/4. The new jacket shoulder is less square in front/profile. The hair still reads as a rounded cap with long vertical clumps rather than the reference's irregular shag, and the vest remains two flat broad panels. This is an unscored whole-figure review checkpoint, not a claimed silhouette pass. No rig, UV, paint or runtime GLB has been started.
# Changed-method neutral pass, before geometry edits

Director and independent Crew review rejected the prior triplet: helmet-like rear hair, flat rectangular vest fronts, and rigid sleeve/trouser columns. The next pass keeps the accepted connected trouser envelope and boots. It builds the vest as rib-following cloth with an angled opening and asymmetric hems, shifts vertices on the jacket/trouser surfaces into localized diagonal fold paths, and replaces thick vertical hair slabs with narrow swept scalp-attached clumps at uneven nape lengths. Pockets and the cargo pouch must touch their cloth surfaces. No separate floating fold bars. Current near budget is 7,628 triangles: jacket 1,300, trousers 1,900, vest 672, hair 648, boots/trim/laces 1,856. Reclaim uniform vest and hair faces, spend the difference on silhouette-defining folds and clumps, and keep the actual evaluated near mesh within 8,000 triangles. The next evidence is one matched whole-body front/profile/back comparison; neutral acceptance is still pending. No rig, paint, GLB, or runtime change.

The bounded rebuild produced `art-build/crew/rook-p2/neutral-measured-comparison.png` at the calibrated scale. Near is 7,752 triangles and far is 184. Independent source tests pass 4/4. I do not consider the silhouette accepted: the hair still reads as a solid cap with hanging strips, the open vest still reads as broad flat boards, and the trousers remain too smooth in the front. The fold offsets are too small to overcome the source shape. This image and negative finding are submitted for independent review before another geometry edit.

## Narrow vest and hair depth repair, written before editing

Independent Crew audit found the cause in authored geometry, not camera calibration. Vest currently has just 10 mm contact, 13 mm edge roll, and 6 mm ripple across a rectangular sheet. The repair retains 10–20 mm shoulder and side contact but gives the lapel, chest, pocket and hem chosen 40–60 mm folds measured forward from the jacket surface. The front opening angles from narrow at scarf to wider at belt; an outer side return turns back around ribs, and hem return folds under the uneven lower edge. Three height bands shape chest, pocket and hem separately. These are cloth geometry, not free floating bars. The hair repair replaces 11 parallel 4 mm ribbons on 23 mm pitch with fewer distinct scalp-rooted clumps. Each bends 40–70 mm sideways and 20–40 mm in depth, with varied crown start, width, taper and nape tip height. The roots follow the scalp tangent. The source budget stays below 8,000 by exchanging uniform vest and hair faces; joined trousers and accepted boots are left intact. Review needs matched front/profile/back and a quarter close view for fold depth.

The first rebuilt surface totaled 8,048 near triangles, so I removed one redundant vest control column and rebuilt; the final source is 7,920 near and 184 far. Independent source tests pass 4/4. Review files are `art-build/crew/rook-p2/neutral-measured-comparison.png` and `torso-three-quarter.png`. The side profile shows more vest depth and the rear hair now sweeps, but my visual check still finds large flat vest faces and visible separate hair strips. These are submitted as evidence, not a neutral likeness approval. Full stage remains blocked on independent review.

## Native vest trace before a garment-only repair

The Director rejected the depth-only vest: it hid both pocket volumes. `art-build/crew/rook-p2/vest-native-trace.png` marks observed pixels on the original front reference without resizing it before tracing. Cyan outer edges run left `(66,156) → (56,237) → (62,289)` and right `(161,155) → (174,240) → (166,290)`. Yellow center openings run left `(91,156) → (102,242) → (104,289)` and right `(130,155) → (120,241) → (117,289)`. Red straps run left `(65,157) → (72,209)` and right `(159,156) → (155,208)`. Green lower pocket boundaries are left `x57–84, y237–281` and right `x143–170, y241–280`. The belt and hand obscure part of the lower edge, so those lower endpoints are inferred. Scale remains `1.83/578` metres per native pixel, front center `x112`, floor `y641`. The existing width traces are close, but the old pockets sit just 23 mm ahead of the jacket and are swallowed by the rebuilt vest's 40–60 mm drape. The next bounded geometry pass will build pocket fronts from these traced rectangles at actual vest surface depth, while shaping the lapel and returned hem on the cloth. Hair and joined trousers remain frozen. Awaiting measurement review before geometry edits.

The measured placement was approved before geometry. I updated the committed front contours and pocket/strap anchors, sampled the new vest surface for attached pocket backs and raised the soft pocket fronts and flaps along the cloth normal. The repaired near mesh is exactly 8,000 triangles; far remains 184. Independent neutral source tests pass 4/4, with all trouser band/connectivity checks unchanged. Latest review images are the matched `neutral-measured-comparison.png` and `torso-three-quarter.png` in ignored `art-build/crew/rook-p2`. Both pockets and shoulder straps are visible from front and quarter view. My review still sees simplified broad garment faces and the prior hair strips; no neutral silhouette approval is claimed.

## Face and hair measurement and next topology allocation

The Director accepted the traced vest and pockets as a neutral garment foundation; that garment is frozen. Whole neutral still fails at head/hair and column-like pants/sleeves. The native-pixel `art-build/crew/rook-p2/head-native-trace.png` draws front and profile guide points on untouched reference crops. Cyan tracks the outer hair from front crown `(111,63)` through left `(79,95)` and right `(149,101)` to uneven nape, and the profile from `(294,66)` through rear `(241,101)` to `(258,135)`. Yellow follows the bare face from front forehead `x99..126,y83` to chin `(110,140)`, and profile forehead `(309,89)` to nose tip `(317,120)` and chin `(299,144)`. Red tracks front brow/nose/chin center and profile ear-to-jaw transition. These are silhouette guides; eye and beard paint follows later.

Actual near triangles by part at this checkpoint: body core 348; detached face bars 48; hair 716; jacket 1,300; joined trousers 1,900; frozen vest 896; pocket 64; scarf 304; boots 640; boot trim 736; boot laces 480; pack 268; straps 124; skin/glove 176. Total is 8,000. The next method replaces detached eyebrow/nose/beard bars with a connected face surface in the existing body-core mesh, using cheek/jaw/nose bands and the measured profile. Hair becomes a scalp surface with nonparallel overlapping clumps rooted to its normals, with uneven side and nape contour. Jacket sleeve and joined trouser cloth keep their connected foundations but receive shaped elbow/knee/cuff gathers along their own faces, never floating folds. Reclaim up to 280 triangles by lowering 12 crossed lace rows per pair to 8, and roughly 100–150 more by simplifying concealed boot trim backs while retaining outer boot silhouette. Existing face-bar triangles also return to the budget. Target reallocation: body core about 500–600, hair about 800–900, jacket and trousers at existing caps, vest/pockets unchanged, total at or below 8,000. No mesh edit for this proposal until the Director approves the measured landmarks and allocation.

The Director approved the native head trace and allocation. Before editing, the concrete mesh plan is: insert four extra head height bands into the connected 16-sided core ring at chin, mouth, nose bridge/tip and brow; deform the front center and adjacent cheek vertices in depth to form continuous nose and jaw transitions, and remove the separate brow/nose/beard panels in that same rebuild. Recess the eye plane by moving its adjacent vertices back, not by adding eyebrow bars. Replace the uniform helmet and nearly parallel rear strips with one irregular scalp shell whose low edge is shorter at forehead and longer at side/nape, then overlap varied short clumps rooted at its normals with alternating lateral sweeps. Keep the already accepted vest/pockets, joined trousers, outer boots and camera fixed. Reduce each boot from six lace heights to four and reduce only the cuff's hidden side density from 16 to 12, leaving the accepted shoe/shaft/sole profile. This should recover 160 lace triangles, about 48 cuff triangles and 48 detached face triangles for about 128 extra connected face triangles and about 100 more hair triangles; source must verify the final actual count. Render one matched whole-body triplet and head-quarter; do not rig or paint.

The connected face/scalp pass rebuilt successfully at 7,914 near and 184 far triangles. Independent source tests pass 4/4. The matched `neutral-measured-comparison.png` and quarter `torso-three-quarter.png` are updated in ignored evidence. Detached face bars and beard shelf are gone, and the back hair now forms a shorter irregular scalp volume. My inspection does not claim neutral acceptance: the front face is too featureless at this stage, the nose profile is sharp, and the front clumps read like a fringe. Source is frozen for a text checkpoint and Director review; no rig, paint, GLB or runtime edit.

## Measured head-only correction proposal after checkpoint 7545e00

Director review rejected the blank front face, beak-like profile and fringe over the eyes. Current connected core uses 16 vertices per head ring. At nose height `z1.68`, the central front vertex is `x0`; its immediate neighbors are about `x±0.035 m`. The broad Gaussian nose displacement still moves those neighbors about 14 mm forward, making a wide wedge rather than a narrow bridge. The proposed head-only topology inserts two extra angular vertices on each side of the front center through the head rings, producing about 20 vertices per head ring with `x≈±0.016 m` and `x≈±0.035 m` near the nose. Join that head loop to the 16-vertex neck loop with explicit bridge faces, preserving a single edge-connected body core. At native front reference `y100` brow (`z≈1.713`), `y110` recessed eyes (`z≈1.681`), `y119` nose tip (`z≈1.653`) and `y140` chin (`z≈1.586`), shape the center and ±16 mm strip forward for the bridge/tip, taper projection to near zero by ±35 mm, and push paired eye planes back 8–12 mm. Keep cheek and jaw planes continuous with the core. The head ring insertion should cost roughly 80–100 triangles; removing six front-sector hair ribbons saves 96 triangles from the current 7,914 total, with final count verified rather than assumed.

Hair direction changes only at the front sector: current tips extend outward toward the face and can descend to about `z1.721`, covering the eye line. On the observed native front reference, crown is `(111,63)`, exposed central forehead starts around `y83`, eyes are around `y100–110`, and curls can hang lower at the temples. Front clump tips should sweep backward/outward along scalp tangents, stay at `z≥1.77` in the central forehead sector, and remain near the scalp radius. Temple clumps may drop toward `z1.68`; back/nape remains staggered. This requires fewer nonparallel clumps, not a new cap or a forehead curtain. Vest, pants, boots, camera and all other silhouette shapes remain frozen. No mesh edit until the Director reviews this localized proposal.

The Director approved the localized head correction. The new body core has a 16-vertex torso loop, 20-vertex head loops with four extra front angular cuts and a consistently wound zipper bridge. A direct edge-incidence check found zero nonmanifold core edges; the single core has 604 triangles. The near total remains 7,914; far is 184. Independent neutral source tests pass 4/4 without assertion changes. Actual near mesh maximum `z=1.82999 m`, aligned to the reference crown at 1.83 m instead of the prior roughly 3 cm overshoot. Hair drops six central front ribbons and sweeps the remaining front tips outward/back, exposing the forehead. The matched `neutral-measured-comparison.png` and full forehead-to-chin `head-front.png`, `head-side.png`, `head-three-quarter.png` under ignored `art-build/crew/rook-p2` are the review set. My inspection still finds the face almost blank in clay and the profile nose angular; these views are for independent neutral judgment, not an acceptance claim. All garments and runtime assets remained unchanged.

The Director found the first head close views cropped horizontally. This was an evidence camera bug: the new head isolate inherited a `300×761` output while using a square `0.52 m` camera scale. I corrected the isolate to `500×500` without changing any mesh. `head-calibrated-comparison.png` now places untouched front and side reference crops beside the corresponding clay at the same metres per pixel. The forehead, nose, chin and full crown are visible. The actual core coordinates at `z1.65 m` are central nose `y−0.149 m`, inner ±13 mm `y−0.133 m`, ±23 mm `y−0.109 m`, and ±31 mm `y−0.090 m`; the projection is narrow, but the side from bridge to tip to upper lip is still a wedge with no under-nose break. At `z1.71 m`, the center is `y−0.120 m`, while eye-side ±41 mm lies `y−0.084 m`; shading leaves the front face almost featureless in unpainted clay. The calibrated comparison also makes the hair mass appear wider/longer than the reference, particularly at the side nape. Geometry is frozen for independent review; no mesh change or neutral acceptance is claimed.

## Explicit reference-space face profile, before mesh edit

The Director and independent Crew rejected another broad Gaussian face adjustment. `art-build/crew/rook-p2/face-profile-grid.png` locates native side pixels; `face-profile-proposal.png` overlays the observed facial outline in green and the current connected centerline in yellow. The measured frontmost points, converted with `1.83/578 m` per native pixel and side origin `x270`, are:

| Part | Native `(x,y)` | Target depth `m` | Current projected `x` | Error, current minus target |
| --- | --- | ---: | ---: | ---: |
| Brow | `(311,100)` | `−0.130` | `307.6` | `−3.4 px` |
| Nose root | `(310,105)` | `−0.127` | `309.8` | `−0.2 px` |
| Bridge | `(313,110)` | `−0.136` | `312.2` | `−0.8 px` |
| Nose wing | `(317,115)` | `−0.149` | `314.7` | `−2.3 px` |
| Nose tip | `(319,120)` | `−0.155` | `316.8` | `−2.2 px` |
| Under nose | `(312,124)` | `−0.133` | `311.0` | `−1.0 px` |
| Upper lip | `(313,130)` | `−0.136` | `303.1` | `−9.9 px` |
| Lower lip | `(310,135)` | `−0.127` | `302.9` | `−7.1 px` |
| Chin | `(309,141)` | `−0.124` | `301.5` | `−7.5 px` |
| Under chin | `(307,145)` | `−0.117` | `299.4` | `−7.6 px` |

The evidence changes the diagnosis: the tip itself is about 2 px *behind* the reference, while lips and chin are 7–10 px too far back. The 50–60 mm tip-to-cheek difference comes chiefly from cheek planes that sit too far behind the reference. The proposed method encodes the above centerline as explicit authored profile bands, not a Gaussian. Each band also carries paired depths at `|x|≈16,35,60 mm`: at brow `−.126,−.117,−.098 m`; eye root `−.123,−.109,−.094 m`; bridge `−.127,−.106,−.095 m`; wing `−.133,−.113,−.100 m`; tip `−.132,−.111,−.098 m`; under nose `−.128,−.114,−.100 m`; upper lip `−.130,−.111,−.096 m`; lower lip `−.121,−.108,−.090 m`; chin `−.119,−.104,−.084 m`. The eye socket at y105–110 is thus about 12 mm behind the y100 brow ridge and transitions forward again into the cheek. Add enough existing-loop height bands for an under-nose break and upper/lower lip/chin, with consistent connected 20-sided head topology and no detached bars. Maintain `<8,000` actual near triangles by shortening/reallocating redundant rear hair ribbons before adding face bands. Hair roots currently share nearly one Z, creating a flat horizontal crown; vary root heights so the center peaks at the calibrated `1.83 m` and sides fall gradually, and shorten only excess nape strips to the traced reference. Garment and boot shapes remain frozen. This is a measured proposal awaiting Director review before mesh edits.

The Director approved the observed side-outline pixels and the explicit centerline method. The paired `|x|≈16/35/60 mm` depths above are **inferred design controls**, not directly observable from the side silhouette; they will be checked against the eventual front/quarter clay shape. Geometry remains frozen until independent Crew's red profile regression is recorded.

Crew wrote the independent regression before source edits. Its measured centerline check failed at brow `307.6` versus `311`, nose tip `316.8` versus `319`, upper lip `303.1` versus `313`, lower lip `302.9` versus `310`, and chin `301.5` versus `309` native side pixels, at a 2 px bound. After the Director approved the measured overlay, I replaced Gaussian face deformation with explicit committed `side.faceProfile` rows and paired depth controls. These rows remain part of one connected core through the 16-to-20 neck zipper. Three redundant rear hair clumps were removed to fund three extra face bands; roots now vary in height to round the crown, and excess nape tips were shortened. The rebuilt core has 724 triangles and zero nonmanifold edges, the total is 7,986 near/184 far, and all five focused neutral source tests pass unchanged. The native profile check now passes its six sampled landmarks. Updated evidence is `head-calibrated-comparison.png`, `head-three-quarter.png`, `neutral-measured-comparison.png`, and `face-profile-proposal.png` in ignored `art-build/crew/rook-p2`.

My visual review remains cautious. The side nose, under-nose, lips and chin now read as separate contour steps, and the forehead is more exposed. The front face looks narrow and plain in clay, with broad flat planes around the eyes; hair and the overall body still differ from the reference. This is an honest candidate for independent neutral review, not a claimed likeness pass. No rig, paint, GLB or runtime asset changed.

## Hairline contact diagnosis before the bounded correction

Director and Crew review accepted the connected face as a neutral structural foundation and isolated scalp contact as the remaining head blocker. Actual committed mesh samples at the front center show core skin `(z,depth)` of `(1.741,−.127)`, `(1.790,−.105)`, `(1.812,−.080)`, `(1.825,−.030)`. The near hair scalp samples are `(1.768,−.095)`, `(1.780,−.091)`, `(1.792,−.080)`, `(1.804,−.060)`, `(1.816,−.033)`, `(1.825,−.001)`. At the observed hairline `y≈83`, `z≈1.767`, skin interpolates near `depth−.115 m`, so the hair shell is **about 20 mm behind the skin** and cannot cover the forehead. The top is still about 29 mm behind the core. This explains the vast exposed white forehead despite a nominal hair lower edge at the right pixel height. Hair ribbons also start at nearly their full radial width high at the crown, while the actual scalp shell narrows there; this creates a flat row of roots and hovering temple strips.

The bounded correction moves only the front shell about 25–35 mm outward according to front-facing angle and height, bringing it 5–15 mm in front of skin at hairline and crown. Ribbon roots will begin near the shell's real narrow crown radius, then sweep outward along the shell toward scalp/temple/nape tips. Temple bulge and tip radius will shrink so strips touch the hair mass; three front ribbons replace three redundant back ribbons and end above the eye line. The center still peaks at the calibrated 1.83 m crown; side root heights step down for a rounded crown. Core face, vest, pants, boots, camera and all other shapes are frozen. After this pass, the explicit source JSON will be serialized with one vertex or face per line and clear mesh sections, preserving values/order, to meet the project readability rule. No rig or paint yet.

The first contact rebuild passed focused 5/5 at 7,986 near triangles but appeared like a rigid cap with a straight brim, so I did not submit it as neutral acceptance. Within the same hair-only correction I varied the front lower edge and lifted side/nape ribbon tips away from the shell; three more redundant back ribbons funded that shape without changing triangle count. The final scalp center samples are `(z1.771, depth−.122 m)` against interpolated skin near `−.114 m`, placing hair about 8 mm in front of the forehead, and `(z1.825, depth−.035 m)` against skin `−.030 m`, placing crown hair about 5 mm in front. Actual maximum Z remains `1.82999 m`. The final source is 7,986 near/184 far; focused source tests pass 5/5 unchanged, including the independent face profile regression and connected trouser acceptance. `rook-p2-source.json` now has one vertex or face per line and clear mesh blocks (11,512 LF lines), with parsed values and order unchanged by formatting. Review evidence is the updated calibrated head comparison, head three-quarter and matched whole-body triplet in ignored `art-build/crew/rook-p2`. My visual review still sees a cap-like mass and stylized clay face; this is offered for independent neutral-foundation judgment, not as final likeness or a painted candidate.

## One rig and paint candidate: contract before tests or code

Director and independent Crew accepted the explicit neutral source as a **foundation for one candidate trial**, not as an approved likeness. Hair still has a cap/ribbon risk. Checkpoint `5f663be` holds the neutral text source and five passing independent source tests. The candidate must stay under ignored `art-build/crew/rook-p2/`; the existing production Rook GLB, seven other crew GLBs, and old crew generator remain byte-for-byte unchanged until a scored game review passes. No new runtime asset loader or dependency is needed.

### Rebuild and rig

Extend `rook-p2.py` with `--stage candidate`; keep neutral as the default. `--paths-only` validates the resolved output before importing `bpy` or writing, and reports JSON keys `blend`, `glb`, `textures`, `evidence`. The candidate plan names `rook-p2-candidate.blend`, `rook-p2-candidate.glb`, `rook-p2-basecolor.png`, `rook-p2-surface.png`, optional `rook-p2-normal.png`, and a manifest, all below the dedicated ignored output. It rejects an output outside this lane's `art-build`/`.evidence`, including traversal, symlinks and `public/`. Candidate export is a separate file; it cannot silently overwrite the baseline. Its manifest records source, landmark, reference, texture and GLB SHA-256 values, actual evaluated near/far triangles, material/draw counts, UV checks and the twelve clip names.

The explicit near mesh is the accepted 7,986-triangle source. Build a *reviewed* far silhouette by controlled decimation of its main garment/head/hair volumes and keep actual exported far triangles at most 2,000; the present 184-triangle placeholder is not a candidate. Near and far retain the same skeleton, UV atlas and clip actions. Reuse the established Rook bone hierarchy and twelve authored animations from `crew-fighters.py` **without editing that generator**: import the existing local Rook GLB into the candidate build as an animation donor, copy its bone hierarchy and baked clip actions, and bind the new near/far meshes with explicit per-vertex region weights. Import is local and read-only. If import does not expose the exact twelve named clips or the skeleton differs, fail the candidate build; do not silently emit a static model. Every weight set must be nonnegative, normalized to one and limited to four joints; verify floor, hands and feet in all twelve clips, especially prone/get-up, before export. Export Blender Z-up to glTF Y-up once, with near/far names and `lod` extras expected by the existing loader. Actual GLB checks, not source counts, decide the budgets.

### One 1024-square atlas set

Use one shared material family and one UV channel named `UV0` on **every** source mesh, active and render-selected before any join/export. No material may depend on generated coordinates. Layout separate, nonoverlapping role charts into one 1024×1024 atlas: unique head/face with a seam behind ears and at hairline; jacket front/back/sleeves; separate vest fronts and pocket fronts/flaps; scarf; trousers; boots; hair; small straps/bag. The face chart reserves enough pixels for eyes, brows, beard and nose at the authored facial landmarks. Mirroring is allowed for hidden or symmetric cloth only, never face, vest fronts, visible pockets or asymmetric wear. At mip level zero require **at least 16 texels between chart interiors and 8 texels between a chart interior and the atlas edge**, allowing eight texels of matching-color bleed on every side without overlap. Record chart bounds, area, overlap and seam positions in the manifest; inspect an exported GLB UV render to catch missing `TEXCOORD_0` or stretching. Pack visible head/vest/jacket/boots first; reduce hidden chart area rather than shrinking facial detail. No second material for near/far.

The set is `basecolor` (RGBA, sRGB, alpha opaque), `surface` (RGBA, linear: R=occlusion, G=roughness, B=metallic, A=255), and optionally `normal` (RGB, tangent-space linear, neutral `[128,128,255]`). Use the same atlas dimensions and UV layout for all maps. Roughness and metalness must be material properties, not painted directional highlights. A normal map may carry fine cloth seams/folds once its tangent orientation and neutral fallback pass inspection; it is part of the one texture set, not another material. Painted color places skin, eyes, beard, scarf, leather, cloth, dust and wear on the appropriate unique islands. Do not bake scene lighting, cast shadows or a camera projection into basecolor. Texture generation can use the built-in image tool for painted detail, but the committed recipe must specify prompt, approved reference hash, island template/hash, map channel meanings and any manual edits. Trial PNGs stay ignored. If the painted candidate is promoted, its maps remain **embedded in the single runtime GLB**; document their exact extraction from that GLB and the deterministic UV/paint application recipe in text. Do not commit duplicate PNGs under `public/` or another binary source. A clean checkout can extract the approved maps from the retained GLB and rebuild against the committed explicit mesh/UV recipe. The script also offers a deterministic neutral atlas from committed palette/UV inputs for clean-build structural tests; image generation is never an implicit network/build dependency.

Candidate CLI and manifest schema for independent tests: `blender -b --python tools/blender/rook-p2.py -- --root <lane> --stage candidate [--output-dir art-build/crew/rook-p2]` builds, while `python tools/blender/rook-p2.py --root <lane> --stage candidate --paths-only` prints only the plan JSON and requires no Blender. `candidate-manifest.json` has `stage:'candidate'`, `sourceSha256`, `landmarksSha256`, `referenceSha256`, `animationDonor:{path,sha256,clipNames}`, `outputs:{blend,glb,textures:{basecolor,surface,normal?}}`, `asset:{sha256,nearTriangles,farTriangles,materials,draws,textureSize:[1024,1024],clipNames,uvChannel:'UV0',charts:[{role,boundsPx,areaPx,seams}]}`, and `qa:{baselineRookSha256,candidateSha256,substitutionRequests}` in the separate browser capture manifest. All output paths are relative to the lane root. The Blender manifest is written only after an export is reopened and checked; a failed validation leaves no passing manifest. The private browser manifest records candidate provenance and actual request count rather than modifying the Blender manifest.

## Candidate structural checkpoint and UV stop

Crew's independent candidate tests were red 0/2 before implementation. The first isolated candidate export passes 2/2: Blender-free path planning rejects production destinations, and the actual GLB has 7,986 near/1,849 far triangles, one shared material, three embedded 1024 PNG maps, bound weights and twelve deforming clips. The production eight GLBs and old generator hashes remain unchanged. The existing local production Rook GLB was imported read-only as an animation donor; its exact twelve NLA tracks and bone names are present. The ignored `blender-rook.json` and three 5 m/28-degree captures were rendered after reopening the **exported candidate GLB**, so they do not masquerade as neutral source renders.

This is **not yet a paintable UV pass**. The first atlas projection chooses one dominant axis per polygon but maps all faces of a role into the same rectangular chart. It also gives body core and forearm skin the same face chart. Eyes or wear painted there would repeat on other surfaces. The manifest currently reports allotted rectangle area, not measured mapped interior area. Director review isolated this gap. UV code is frozen until Crew's independent actual-export overlap regression is red; the next edit will create unique face versus skin/neck, front/back/sleeve, left/right vest and pocket charts with authored seam choices and measured UV area, retaining the accepted geometry, rig and budgets.

Separately, the Director's QA-only loader helper was red 0/2 then green 2/2. An actual private memory-only browser control on port 49332 captured 44 views with no warnings/errors and exactly one Rook substitution per quality; production Rook SHA `9dfa…` was unchanged. That input was a byte copy of the **baseline** at ignored `art-build/crew/rook-p2/qa-loader-control.glb`, so it proves exact-Rook request isolation and loader wiring only, not new art or this candidate. Raw control evidence remains under ignored `.evidence/2026-09-25/rook-candidate-loader-control` until the review verdict. Root owns those QA hooks.

Crew added an exported-GLB texel-overlap regression. Its first red output reported face 90.4%, vest-left 35.7%, vest-right 32.1% and pockets 86.3% reused interiors, but a later audit found the test **double-inverted glTF V**. Those percentages were a test coordinate error, not a true overlap finding. After Crew corrected glTF UV-to-image-top mapping, the current export passes the real overlap check. The candidate UV rewrite nevertheless moved each authored role family into a separate chart and the manifest now sums actual UV polygon area rather than assuming the rectangle is filled. The atlas still uses deterministic muted trial colors, so the reopened-GLB front/side/back renders are technical evidence only. The face lacks painted eyes/beard and the garment wear is faint; no visual likeness score or runtime promotion is claimed.

## Authored face seam and island clearance, before second UV edit

The actual UV wireframe in ignored `uv-layout-annotated.png` exposed a paintability fault despite the green corrected nonoverlap test. Crew's independent exported-UV check found **21 disconnected face islands**, with internal gaps around 2.4–3.4 px; vest gaps around 1.3 px and pockets around 2.2 px. Eight-pixel bleed on both sides needs **16 pixels between island interiors**. SmartProject split the head into many islands, and the connected body's hidden torso occupies the face chart. The Director accepted this diagnosis; the current candidate SHA `82b82d97…` was frozen for a real High/Performance loader and pose capture before rebuilding.

The next UV-only pass splits the *Blender candidate copy* of the committed connected body-core by face polygon at the neck/head transition (`z≈1.52 m`), duplicating boundary vertices without changing faces, world positions or the committed core. Hidden torso/neck polygons join the `skin` chart with forearms; head polygons alone enter `face`. The head receives one cylindrical unwrap with `u=.5+atan2(x,-y)/(2π)` and a deliberate longitudinal seam on the back of the head, behind the ears. `v` follows the observed chin-to-crown height. The connected front band from eye planes through nose, lips and chin stays one continuous island, with no seam across that painted region. Hairline and neck are the natural upper/lower bounds; back seam polygons may duplicate only their UV loop values. This reserves the full face chart for readable eyes/beard paint rather than hidden torso. The geometry and weights stay identical after near join; far is derived from the same near.

The first continuous face unwrap passed topology but failed the Director's paint allocation review: actual eyes/nose/lips occupy a thin central sliver of the 228-pixel face rectangle. Before any paint, remap the **same rear-seamed island** with a monotonic piecewise angular scale. Physical front is `−Y`. For `θ=atan2(x,−y)`, allocate U `[.13,.87]` to the visible front sector `θ∈[−π/4,+π/4]`; map the two side/back sectors monotonically into `[0,.13]` and `[.87,1]`. The face chart stays `[16,16,244,244]`, with the same 8-pixel interior margin. The expected eye centres at approximately `θ≈±.35 rad` should separate by at least **70 actual atlas pixels** rather than roughly 25–35. This is a texel allocation change only: no face geometry, chart boundary, UV island count or rear seam changes. Crew will first add an actual exported-GLB landmark spacing red regression, then the candidate script can change its U mapping. The annotated pre-correction image is ignored `uv-face-annotated.png`.

Vest-left, vest-right, pocket and other visible garment charts keep separate authored role allocations. SmartProject is allowed for their closed, less semantically sensitive cloth shapes, but its pack margin rises until **measured exported island interiors** are at least 16 atlas pixels apart. If a dense chart cannot meet that without collapsing visible texels, relocate that role into one of the three unused bottom-row slots or simplify a hidden island; do not weaken the 16-pixel requirement or overlap test. The same 8-pixel outer atlas margin stays. The manifest will report measured occupied UV area and minimum island gap, while Crew independently tests actual exported face continuity and gap before this rebuild. No neutral shape, rig, clip, runtime asset or texture source change is part of this pass.

## Round-1 game pose finding: role-aware binding before code

The Director captured the immutable first candidate sheet (`docs/board/looks/crew-p2/round-1.jpg`, 84,126 bytes) from 44 actual High/Performance private shots on port 33439 with zero loader issues, using candidate SHA `82b82d97…`. This is a review of the muted technical trial, not a likeness pass. The aim pose exposes severe long outer garment panels. The first visual diagnosis called them vest, but Crew's exported weight check proved the vest/pockets/pack/straps already carry torso weights. Direct Blender deformation inspection traces the stretched surface to the **single connected jacket**, whose lateral lower vertices around `x≈±.20–.28,z≈1.08 m` move roughly 0.4 m under forearm weights in aim. The jacket joins torso side and sleeves; `|x|>.20` alone cannot identify an arm. The vest claim is retracted.

The bounded binding correction keeps the same neutral geometry and bone/clip donor. The two vest cloth panels, their attached pockets/flaps, front and back straps and lower-back bag stay chest/pelvis bound, as already verified. Scarf wraps and hair follow `neck`/`head` as their attachment points require. The next source annotation distinguishes jacket torso-side faces from true sleeve faces using **authored part metadata** tied to the explicit mesh, not another broad coordinate threshold. Jacket torso follows chest/pelvis; only sleeve faces, gloves and exposed forearms follow the corresponding arm chain, with a controlled shoulder transition. Trousers/boots retain the leg chain. The independent acceptance evaluates actual exported aim deformation on the annotated jacket torso and separately checks a sleeve/hand still articulates. Crew's first broad jacket red test needs this metadata because some genuine sleeve vertices must move. No assertion is relaxed without Director review. The UV correction is separate.

Crew's narrowed actual-export red regression now identifies the physical tear rather than guessing a role: jacket source edge `559–561` grows from `0.006 m` bind length to `0.368 m` at the **start** of aim, about `59.9×`; its limit is `3×`. The same jacket surface also has true sleeve vertices that need arm motion, so uniformly chest-binding the whole jacket would hide the tear while breaking animation. For the first correction, use a continuous sleeve-to-torso weight field along the authored jacket: 0 arm influence inside the central torso (`|x|≤.14 m`), rising linearly across 0.20 m toward the outer sleeve, with the remainder on chest/pelvis. This keeps adjacent seam vertices within roughly 3 percentage points of arm influence over a 6 mm source edge, instead of the old 0-to-78% jump. A jacket-only field is permitted because it is anchored to the measured tear and visible sleeve span; other role weights stay as designed. Measure the exported edge ratio and sleeve motion after build. If this field cannot satisfy both, add explicit per-vertex part tags to the committed jacket source rather than broadening the threshold or changing the test.

The first continuous jacket weighting passes Crew's unchanged actual-export seam-edge and positive sleeve-motion tests; vest/accessory torso guards also pass. UV face expansion and explicit island packing brought the full candidate suite to 7/7 green. The Director requested the actual annotated UV layout before paint. That inspection **rejects the jacket UV despite passing clearance**: 14 unwrapped islands form a dense central knot with long crossing spokes inside the 480×228 jacket chart; a painter cannot place readable front/back garment wear there. The face and vest charts are broad enough to review separately. Whole-atlas painted output waits.

The next jacket-only UV method uses four **authored surface panels** inside its existing `[520,772,1000,1000]` rectangle: torso front, torso back, left sleeve and right sleeve. Each receives roughly 104×212 interior pixels, with 16 pixels between adjacent panels and 8 pixels at the jacket chart edge. Classify source polygons by observed jacket side/sleeve contour, then project front/back from physical x/z and sleeves from their longitudinal arm-axis angle/z. Shared vertices may have distinct loop UVs at sewn seams; geometry, triangle count, weight field and face chart stay unchanged. A reviewable annotated sheet must show four legible panels and clear room for cloth paint, not only green bounds tests. Any overlapping self-projection or distorted panel is a failed visual UV review and must be corrected before painted jacket art.

The four-panel jacket projection now preserves broad cloth strips instead of the rejected central knot. A first build left four disconnected edge-return islands on top of their parent panels; blindly packing those produced four tiny top bars because the packer treated already-atlas UV units as full-chart normalized units. Correcting that unit scale leaves four tall, distinct panels in ignored `uv-layout-annotated.png`. Crew's unchanged candidate suite passes 7/7, including actual-GLB near/far, twelve clips, eye spacing, zero face/garment interior reuse, 16-pixel island gap, torso accessories, positive sleeve motion and less than `3×` armhole-edge stretch. The painted source is still absent and no second art round is claimed from this technical correction.

## Calibrated face-paint trial: input contract before implementation

The Director generated one ignored `1254×1254` RGB face diffuse source, `art-build/crew/rook-p2/face-paint-source-1.png`, SHA-256 `cc48bbd07c6b436771537fcf416cf0c0d503e2afa5fab4c2b31b5582d46de358`. Its full prompt is beside it. The output has useful skin, eye and beard detail, but it did **not** obey requested UV positions, so pasting it directly into the atlas would put features on the wrong mesh rows. Preserve that original unchanged. The first painted candidate will use a **distinct ignored output** `art-build/crew/rook-p2/paint-1/`, so default structural tests cannot overwrite its GLB or evidence.

Add a committed small input `tools/blender/rook-p2-paint-calibration.json` (Director board hook requested). Schema: `version:1`, `method:'piecewise-linear-landmark-bake'`, `source:{size:[1254,1254],sha256,landmarks:{hairline:[630,155],leftEye:[490,470],rightEye:[768,470],nose:[630,654],mouth:[630,770],chin:[630,965]}}`, `target:{chart:'face',boundsPx:[16,16,244,244],landmarks:{hairline:[130,67],leftEye:[85,126],rightEye:[175,126],nose:[130,147],mouth:[130,169],chin:[130,193]}}`. Source landmarks are measured from this generated portrait; target y coordinates follow the approved reference-to-UV guide, not the generator's attempted prompt coordinates. Actual exported source vertex IDs 214/222 lie at `(84.81,136.59)` and `(175.19,136.59)` atlas pixels; they validate **horizontal** eye spacing, but the paint eye line follows the reference-derived y≈126 for visual review. The vertical mapping interpolates through the listed target/source y pairs and extends to chart margins; horizontal mapping is anchored by the two eyes and centerline (`sourceX≈630+(targetX−130)×(768−490)/(175−85)`). Preserve eight-pixel bleed at the chart edge and do not write outside the face rectangle. The selected portrait is an albedo source only, not a normal/roughness map or a rendered in-game portrait.

Candidate CLI adds optional `--face-paint <ignored source.png> --face-paint-sha256 <64hex>`, requiring both together, verified against the calibration SHA and actual bytes. It rejects sources outside this lane's ignored `art-build`/`.evidence`, traversal, malformed calibration, and output to `public/`. Omission preserves the deterministic structural atlas and its path plan. Paint mode requires `--output-dir art-build/crew/rook-p2/paint-1` (or another dedicated ignored descendant), writes its separate candidate GLB and records `paint.face:{sourcePath,sourceSha256,calibrationPath,calibrationSha256,targetChartBoundsPx,basecolorSha256,method}` in the candidate manifest. The selected atlas image is embedded in that GLB; raw generated portrait and trial atlas stay ignored. Before adding this path to the exporter, Crew independently tests path/hash/default behavior and verifies a selected input changes only face-chart pixels in the exported basecolor. A future accepted runtime GLB retains the painted map internally with this extraction/reapply recipe in text; no duplicate public PNG is added.

The exact built-in image-tool prompt that produced the ignored face source (the two inputs were the earlier enlarged Rook UV guide and approved crew reference) is retained here for provenance:

```text
Use case: precise-object-edit. Asset type: opaque sRGB diffuse face texture for an original 3D game character.
Input 1 is the edit target and exact UV layout guide: a square enlarged crop of the Rook head texture island. Input 2 is the approved character reference; use ONLY the brown-haired bearded man in the leftmost front/side/back triplet, Rook.
Paint a professional hand-painted realistic game face texture matching that original man, directly over the layout in input 1. Output one 1024 by 1024 square raster texture. This is a flattened UV texture, not a rendered head, portrait, poster or turnaround. Preserve the layout and facial landmark placement; remove ALL white wireframe lines, yellow guides, black labels, text and borders from the final texture.
Coordinates in the 1024-square output: vertical facial centerline x512. Hairline y248, brows around y403, eyes centered around (330,495) and (694,495), nose tip (512,583), closed mouth centered (512,676), chin y776. Fit the visible face continuously through those landmarks. Side and rear head skin wraps toward the left/right edges; the narrow lower area is neck. The upper area is scalp beneath separate modeled hair. Keep this slightly expanded flat UV geometry rather than making an ordinary oval portrait.
Identity and materials: adult man from the left reference, weathered warm light skin, deep-set hazel-brown eyes, strong natural brows, straight sturdy nose, closed neutral mouth, short dark brown beard and moustache, brown sideburns/scalp roots. Match his reference likeness and restrained earthy colors. Fine skin variation and convincing beard strands; no invented scars, accessories, brands or symbols.
Diffuse/albedo only: even neutral illumination, no directional cast shadows, no dramatic ambient occlusion, no glossy highlights, no background scene, no perspective, no three-dimensional silhouette or detached head. Let the real game lighting create the shading. Fully opaque edge-to-edge skin/scalp color with extended bleed around the island; no transparency. No text or watermark.
```

### Private candidate QA and review

The face-paint acceptance was red before implementation (CLI/path/hash, calibration, and actual isolated export). It is now green 3/3 unchanged. The generator verifies the ignored source bytes and ordered calibration before importing Blender or writing output, then bilinearly samples the original portrait into the face chart only. The trial basecolor and the GLB's embedded basecolor have identical decoded pixels, and control-versus-painted exports differ only inside `[16,16,244,244]`. The candidate manifest records the input and calibration hashes. All eight runtime crew GLBs and the baseline generator remain byte-identical. The source and mapping recipe are kept as small text inputs; the original portrait and painted trial stay ignored.

The same trial updates the **structural control palette** in both painted and unpainted builds to reference-guided faded blue-green jacket/shirt, dusty tan vest and scarf, brown-khaki trousers, and dark boots and gloves. This is a reviewed flat color block, not finished garment painting. The face-only regression compares against the matching control build, so it still proves no other atlas pixels change when the face input is selected. The painted front render shows a coherent face at game-review distance. The side view exposes a tan scalp gap above the hair clumps. Hair coverage, cloth weave/seams, and full scene likeness remain open for actual High/Performance review; this technical export is not a promotion or likeness pass.

After the palette change, `node --test tools/test-rook-p2-source.mjs tools/test-rook-p2-candidate.mjs` passes **15/15** in 39.2 seconds. `git diff --check` is clean. The ignored paint-1 GLB SHA-256 is `9d7a2cd624aca63771b14b4ef27bf7838b28752039f57d48b31c7edc2067d3f6`; the default structural candidate remains separate. These test results were obtained with the ignored real portrait present. Crew is replacing the independent test's unconditional image dependency with deterministic temporary fixtures so the gate will work from a clean checkout; that fixture update has not yet been rerun here.

Add optional `GFX_ROOK_CANDIDATE` only to `tools/scenarios/crew-fighters.mjs`, allowed only with `GFX_CREW_ONLY=rook`. Resolve it beneath this lane's ignored `art-build/crew/rook-p2`, require `.glb`, hash and read bytes in Node, and intercept browser `window.fetch` after the memory-only guard but **before** the on-foot spawn. Substitute a `Response` with those exact bytes only for `/assets/models/wasteland/crew/rook.glb`; pass every other request to the original fetch. Count and assert actual Rook substitutions, record candidate path/SHA and baseline production Rook SHA separately, and never label candidate captures as production-asset captures. Default full-eight mode and all existing capture checks remain unchanged. Capture the real candidate in High and Performance, all three views plus clips, with matched reference/Blender/game sheets using additive Rook-specific family support in `tools/fidelity-sheet.mjs` and `tools/blender/fidelity-sheet.py`; preserve other families' sheet behavior. The independent gate checks exact twelve clips, weights/deformation, GLB near/far/UV/map budgets, local embedded resources, unchanged baseline hashes, and private QA request isolation before visual scoring. The score follows SPEC 0.3's five items; each must reach at least four, with twelve-fighter frame cost inside the 24-draw-call and combat budget. Three rounds alone do not establish a pass. A failed trial remains ignored and cannot change runtime Rook.

### Round-2 measured trouser rig repair, before source edit

Crew traced the long tan geometry in the immutable paint-1 GLB across actual UV roles and browser pose times. It is the **trousers**, not the vest: connected source edge 719–689 rests at 7.47 mm, stretches to 829.5 mm in aim (111.1×) and 734 mm at get-up 0.6 s (98.3×). Vertex 719 at x=.20638, z=.79188 took hand R .8/forearm R .2 from the generic `abs(x)>.20` arm rule, while its adjacent vertex at x=.20257, z=.78939 took thigh R .85/pelvis .15. The next code change adds a `role == 'trousers'` branch **before** that generic arm rule. It binds upper trousers to pelvis/thigh, knee transition to thigh/shin, and lower cuffs to shin/thigh with smooth height blending; no trouser vertex may receive an arm, hand or chest bone. Existing source geometry, UVs, face paint and all other roles stay fixed. The independent red test checks the actual exported short seam in aim and get-up, and positive leg motion, before round-3 game capture. A corrected painted build goes to ignored `paint-2`; paint-1 stays byte-identical for round-2 evidence.

The role branch is now implemented. The independent test stayed unchanged and passes: no exported trouser chart vertex has arm/hand influence, the short source seam no longer tears at aim 0 or get-up 0.6 s, and get-up leg motion exceeds 5 cm. `node --test tools/test-rook-p2-source.mjs tools/test-rook-p2-candidate.mjs` passes **16/16** in 42.8 seconds. Painted output `art-build/crew/rook-p2/paint-2/rook-p2-candidate.glb` has SHA-256 `07bc7185ec29862861a6b1291f2b038246708fa164234dd630cb467c78c269c7`, 7,986 near and 1,849 far triangles, and the same calibrated face source. The paint-1 artifact is unchanged. This is an exported-rig pass only; actual round-3 High/Performance views are needed before judging the visible defect. Blender still logs its prior face mesh validity warning, although actual GLB reimport, clip/UV/weight checks and renders pass; that warning remains an export risk to audit before any promotion.

Actual private round-3 High/Performance game views on that paint-2 hash confirm the long trouser panels are gone in aim and get-up; the legs still bend. The Director's overall resemblance verdict remains **2/5**, so this is a rig repair, not art acceptance or a runtime swap.

The round-3 get-up side pose exposes a separate waist opening. Source audit before a fix finds trousers top z=1.019, belt z=1.027–1.047, jacket bottom z=1.038 and body-core bottom z=1.040. The authored rest gap is therefore at least 8 mm between trousers and belt, and about 19 mm between trousers and jacket/core. At the nearest rows, trousers are roughly pelvis .81/thigh .19; jacket hem is roughly pelvis .86/chest .14. Relative bone motion can widen the authored gap. Crew is measuring exact exported waist bounds at get-up 0.6 s and aim start and writing an independent continuity regression. The correction will follow that measured geometry/weight cause; it must preserve the trouser no-arm fix and real leg motion. Hair work is queued until the waist remains covered in actual game action captures.

Crew's exact exported ring scan identifies 61 jacket-hem/trouser-top pairs within 55 mm in authored XY. Idle median separation is 32 mm and p90 is 52 mm; aim p90 grows to 128 mm, with right pair jacket 386/trouser 730 growing from 34 mm bind to 154 mm; get-up 0.6 s p90 is 118 mm, right pair 131 mm. The independent exported-ring test is red with a reviewed 80 mm p90/pair ceiling in both action poses. First correction will **align the upper trouser waist and lowest jacket hem to pelvis** while blending farther down the leg/up the torso, preserving knee and sleeve motion. It will not add an arbitrary skirt or move the accepted trouser/jacket silhouette. If this reduces pose separation but the authored neutral gap remains visible, a second measured overlap adjustment needs separate review. The same source test must keep all trouser arm weights zero and real leg action positive.

The first binding trial did **not** improve the measured gap: aim p90 remained 127 mm and the reviewed right pair remained 152 mm (versus 128/154 mm before). Jacket armhole and trouser no-arm tests stayed green. I reverted only that trial's jacket/upper-trouser blend constants, preserving the approved trouser repair and paint-2. Crew is checking the exact exported pivot vertices, weights and posed coordinates before choosing a geometry or rig correction. The independent waist test remains red; no softened threshold or visual concealment was applied.

The exact paint-2 GLB resolves that failed trial: jacket source vertex 386 exports as IDs 6146/6147 with **pelvis .5620, forearm R .3026, chest .1354**. Trouser vertex 730 exports as IDs 5343/5344 with pelvis .8091/thigh R .1909. The jacket vertex is an actual hem/torso-side point `(x .20053, y .00618, z 1.04883)`; all six incident source faces have mean `|x|≤.203` and belong to the torso panels, not the sleeve. At aim it moves to `[.1757,1.1267,.0980]` while trouser 730 is `[.2106,1.0102,.0039]`, opening 154 mm; get-up opens 131 mm. Merely altering chest/pelvis fractions did not remove its erroneous **forearm** influence. The next bounded fix derives per-vertex jacket torso/sleeve participation from the already authored four-panel face topology: torso-only vertices get no arm weight, sleeve-only vertices retain arm motion, shared armhole vertices blend by incident sleeve-face share. The reviewed right waist vertex is torso-only. Do not change mesh geometry or the trouser fix. The independent waist, short jacket seam and positive sleeve-motion tests decide whether this closes the gap.

The first all-height topology vote closed the waist regression but tore one lower armhole seam 5.2×; a 30 mm transition still tore it 4.3×. The accepted narrow method removes arm influence at torso-only hem vertices below z=1.06, then returns to the established armhole field by z=1.079. This passes the independent exported waist, original short-armhole seam, trouser no-arm and positive sleeve movement checks without changing assertions. Crew added an extra **lower cuff** positive-motion guard at z≈1.04–1.12; it also passes, so the hem guard does not freeze cuffs. The default structural focused suite is **17/18**: the sole expected red is the independently queued hair-coverage contract, currently 3/5 frontal probes. A separate painted candidate `art-build/crew/rook-p2/paint-3/rook-p2-candidate.glb` has SHA-256 `6e1b78541a2ea65a6f4028470b28c77f304606a27ffa38409839b9e3565c2a28`, 7,986 near and 1,849 far triangles. Paint-1 and paint-2 remain unchanged. Round-4 actual game views must confirm the waist is visually closed before the hair correction proceeds.

Round-4 High/Performance actual views on paint-3 confirm a better aim hem, but the get-up side pose still exposes a grey opening at the waist. The 80 mm nearby-vertex distance gate does not prove surface coverage. The source explains the uncovered strip: core bottom is a 16-vertex narrow ring at z=1.040 (x±.125, y±.092); trouser lip reaches only z≈1.019, with side width to roughly x±.22; jacket hem starts at z≈1.038 and belt covers z=1.027–1.047. Before another source edit, Crew will add an independent **actual exported coverage** test sampling waist silhouette/rays at get-up 0.6 and aim start, rather than only point pairs. If it proves the hole, extend the existing core bottom ring below the trouser lip and adjust existing jacket/belt overlap to meet measured side/front coverage, without adding triangles or an artificial skirt. The waist remains pelvis-bound; accepted face, hair, trousers and sleeve motion stay intact. Paint-3 remains immutable for the round-4 review. Hair work stays queued until this visible joint closes.

Crew's independent source ray test was red before geometry: at anatomical z=1.000, the front ray met trousers but no body-core surface. It samples front/front-right/right/back/left at z=1.000 and 1.010, requiring core beneath the trouser shell. Moving only the existing 16 core bottom-ring vertices from z=1.040 to z=.985 made the unchanged source test pass **1/1**, without extra vertices or triangles. The new actual exported ray assertion is correctly red on the stale pre-edit GLB. Rebuild and actual get-up side game views remain pending. No belt or jacket vertex was moved in this first bounded pass.

After rebuilding the structural candidate, the independent source **and actual exported GLB** waist rays pass unchanged, and the existing jacket hem/cuff and trouser motion checks remain green. The isolated painted `paint-4/rook-p2-candidate.glb` SHA-256 is `41d54088ea846ce435fe1dc3c17e2004dbc715da137a4eb02daf3f4984d8d2ac`; it has 7,986 near and 1,850 far triangles. Earlier reviewed paint-1/2/3 binaries remain untouched. Private actual High/Performance aim and get-up side views are the remaining visible-hole gate for this repair.

Round-5 private High/Performance game views on paint-4 close the absent waist opening in get-up side in both qualities (44 captures, zero issues, port 15256). The Director scores resemblance 2, readability 3, grounding 3, scene consistency 3; frame cost remains unmeasured. Aim still shows a narrow dark core between garments as fit polish, but no background hole. The immutable round-5 sheet is 85,726 bytes. Hair coverage is now the next bounded defect.

Before changing hair, sampled source rays at z=1.766 showed real misses at x=−.03 and +.06: the cap's lower front ring rises to about z=1.771 and z=1.770 at those lateral positions, above the traced hairline. At z=1.82, the original fixed ±.06 probes had no scalp surface at all, so four-of-five coverage there was impossible. Crew calibrated only that high band to five real scalp rays x=[−.03,−.015,0,.015,.03], retained the 4/5 coverage and 3 mm outward contact requirements, and kept the original five lower hairline probes/red result unchanged. An initial automatic review rejected the test edit as possible weakening; Crew supplied the measured skull cross-section and Director approval before the corrected test. The bounded geometry edit will lower existing cap front-edge vertices around x−.03/+ .06 toward z≈1.756–1.760, keep their outer depth at least 3 mm in front of the actual skull, and settle existing short clump roots on that cap. It must keep the reference crown ≤1.833 m, near ≤8,000 triangles, face/UV/paint/waist fixed. Structural source and exported hair rays must pass before paint-5 actual game views.

The bounded cap edit lowered two front-edge vertices to z=1.757 and widened/raised five existing upper-front cap vertices to z=1.826–1.828, x±.034. No faces or new triangles were added. After rebuilding, Crew's unchanged source/exported hairline and crown rays pass; the complete focused source/candidate suite passes **19/19** in 46.2 seconds. This proves contact at sampled cross sections, not full hair likeness. The Director deferred a separate 44-capture hair-only likeness round so the next scored round can combine meaningful garment and silhouette work.

## Grouped garment and painted-cloth candidate contract, before code

The current atlas has four legible jacket panels and separate vest fronts, but the trousers still use many narrow SmartProject strips. The next changed method has three bounded parts. First, trace the reference's asymmetric knee, calf and bunched cuff boundaries onto the existing front/back `trouserBands` in the landmark file. Move **existing** trouser ring vertices within those observed contour envelopes to break the straight culotte columns into shaped knees and cuff gathers, preserving the joined pelvis, waist coverage, boot contact, near ≤8,000 and far ≤2,000. The reference pixel bands, not an arbitrary tolerance, decide the contour. Second, replace only the trousers' UV projection with two sewn front/back panels inside its current chart `[772,268,1000,496]`: target painted interiors `[780,276,878,488]` and `[894,276,992,488]`, giving 16 pixels between panels and eight pixels around the chart. Each panel carries both leg columns and the connected crotch; seams lie on inner/outer trouser sides, not across knees. Test actual exported UV triangle reuse/coverage and inspect an annotated GLB UV guide before any painting. The face, jacket, vest and hair UV charts remain fixed.

Third, use **one** local 1024×1024 flat material reference sheet from the built-in image tool, with four visually separate, unlit albedo swatches: upper-left faded blue-green worn shirt cloth; upper-right dusty tan canvas for vest/scarf/pouches; lower-left brown-khaki cargo trouser cloth with directional folds; lower-right dark weathered leather for boots/gloves/straps. No 3D garment render, directional light, symbols or text. The original ignored PNG is never a runtime file. After visual inspection, commit only a small `tools/blender/rook-p2-garment-calibration.json` with `version:1`, `method:'quadrant-panel-bake'`, `source:{path,sha256,size:[1024,1024],regions:{shirt:[x0,y0,x1,y1],canvas:...,trousers:...,leather:...}}`, `targets:{jacket:{source:'shirt',panels:[four rectangles above]},'vest-left':{source:'canvas'},'vest-right':{source:'canvas'},pockets:{source:'canvas'},scarf:{source:'canvas'},pack:{source:'canvas'},trousers:{source:'trousers',panels:[[780,276,878,488],[894,276,992,488]]},boots:{source:'leather'},gloves:{source:'leather'},straps:{source:'leather'}}`. The initial source crop candidates are `[8,8,504,504]`, `[520,8,1016,504]`, `[8,520,504,1016]`, `[520,520,1016,1016]`; actual crops are measured on the selected image before committing calibration, since image generation may not obey exact quadrant boundaries. A target without explicit `panels` uses its existing chart interior with eight-pixel edge bleed. For jacket the four interiors are `[528,780,632,992]`, `[648,780,752,992]`, `[768,780,872,992]`, `[888,780,992,992]`. These swatches supply real painted material texture and fold direction; subtle seam/stitch paths can be authored from measured reference anchors in the same small JSON, not invented as random stripes. Raster transfer is deterministic bilinear source-crop→target-panel; source alpha must be opaque. Basecolor alone changes; surface/normal maps and face/hair pixels stay byte-identical to the matching face-painted control.

The optional CLI is `--garment-paint <ignored PNG> --garment-paint-sha256 <64hex> [--garment-calibration tools/blender/rook-p2-garment-calibration.json]`, usable alongside the existing face-paint arguments with a dedicated ignored output directory. `--paths-only` validates source path/hash, calibration order/rectangles and safe output **before** Blender or any write; omission leaves the existing structural candidate unchanged. The candidate manifest adds `paint.garments:{sourcePath,sourceSha256,calibrationPath,calibrationSha256,basecolorSha256,method,roles}` beside `paint.face`; `roles` is an object mapping each target role to its source swatch name, for example `{jacket:'shirt','vest-left':'canvas','vest-right':'canvas',pockets:'canvas',scarf:'canvas',pack:'canvas',trousers:'trousers',boots:'leather',gloves:'leather',straps:'leather'}`. All painted maps stay embedded in the ignored trial GLB; the approved extraction/reapply recipe is text, and no duplicate public PNG is committed. Independent red tests will use deterministic temporary source/calibration fixtures, verify actual two-panel trouser UV area/no overlap/16px gap, hash/path rejection, face-only pixels unchanged, selected garment chart changes, embedded GLB pixels equal the authored PNG, and baseline eight crew assets plus old generator byte-identical. Only after green structural/art-paint controls will one grouped High/Performance game round be scored for likeness.

The independent garment contract was red before implementation: 213/1,900 actual exported trouser triangles sat outside the planned broad panels, and the optional garment CLI was unrecognized. The new candidate path validates the selected ignored PNG and calibration before Blender, paints only the named role chart/panels into the basecolor, preserves face/hair/surface/normal pixels against the matching face-painted control, and records role→swatch provenance. Actual GLB pixels equal the authored PNG. The two-panel trouser UV rebuild places all 1,900 triangles in the reviewed interiors; each broad panel contains connected leg/crotch area, and the inspected ignored `uv-trousers-2panel.png` shows front/back surfaces with the 16px gap. The unchanged focused garment subset passes **4/4** in 56.2 seconds. The generated four-swatch art source and contour-led trouser shape edit have not yet been made; structural green does not establish garment likeness.

The Director accepted that actual exported UV overlay for a paint trial. The one generated ignored source is `art-build/crew/rook-p2/garment-paint-source-1.png`, SHA-256 `793ee85733cd1ed0446b54d0cf5ef3746d5dba311aaf8b79d4fda6828e93e928`. It is **1254×1254**, not the requested 1024×1024, and the four swatches meet near x/y=627. The committed calibration records visually checked inset source crops `[12,12,615,615]`, `[639,12,1242,615]`, `[12,639,615,1242]`, `[639,639,1242,1242]`. Its measured source alpha ranges 216–250 despite the prompt's opaque request. The original remains unchanged; the bake takes its RGB as color data and writes into an already opaque basecolor atlas, so final output alpha stays 255. Crew is reviewing that explicit source-contract correction and final-alpha regression. The selected source is material texture input, not a second runtime asset.

The exact built-in image-tool prompt retained for a clean re-creation attempt (the selected generated PNG is still an ignored, hash-verified trial input) is:

```text
Use case: stylized-concept
Asset type: one square opaque base-color material source atlas for a weathered wasteland survivor 3D character. This is a texture-making input, not a picture of clothing or a character.
Create a square 1024 by 1024 image with exactly four equal square quadrants meeting at the center, no border, gap, labels or text. Each quadrant is filled edge-to-edge with a flat, closely viewed material surface in the same realistic hand-painted game-art style.
Upper left: faded desaturated blue-green cotton work-shirt cloth, fine woven grain, irregular frayed seam and subtle vertical worn creases, muted dusty grey-green highlights, restrained earthy grime. The fabric is mostly blue-green, never bright teal.
Upper right: dusty light tan canvas for a sleeveless utility vest, scarf and pouches. Strong readable irregular stitched seams, woven threads, worn stitched edging, grime collected along folds, pale rubbed canvas. Keep the dominant midtone tan with earthy brown wear, no green or skin tone.
Lower left: brown-khaki cargo-trouser cloth, tough twill weave with vertical/diagonal gathered knee and cuff creases, worn knees suggested by uneven fading, a few frayed repaired stitch lines. Irregular organic fold rhythms; no repeated wallpaper ornament.
Lower right: dark brown worn boot-and-glove leather with small scuffs, rubbed warm-brown grain, crease breaks and a few close double-stitched leather seams. Keep useful readable brown midtones, not black voids.
All four are flat orthographic albedo swatches. Fine detail plus readable medium-scale seams and crease color variation that survives downsampling to 100 pixels wide. Broad neutral diffuse appearance, no directional lighting, cast shadows, specular highlights, reflections or photographic studio scene. No 3D garments, mannequin, body, face, hands, boots, pockets as separate objects, accessories, buckles, symbols, text, numbers, borders or watermark. The full square should consist only of these four material texture surfaces.
```

### Grouped paint build and contour audit

The independent garment focused tests pass **4/4 in 30.9 seconds** after Crew changed its deterministic temporary source to cover alpha 216–250. The test checks that the original source hash is unchanged, RGB is transferred into an opaque atlas, every final embedded atlas alpha is 255, and face/hair pixels remain unchanged. This replaces the earlier provisional 56.2-second subset result; no test limit was relaxed.

The real four-quadrant source and the established face source built into the distinct ignored `art-build/crew/rook-p2/paint-5/` candidate. Actual GLB SHA-256 is `b226110a496145feb1ff56ffbdc9f1b2c6ba208358d8900afb1668078199cdaa`; evaluated near/far counts are 7,986/1,849 triangles. The Blender front render shows the intended blue-green shirt, tan canvas vest/scarf, brown-khaki trouser cloth, painted face, and revised hair contact. Blender export still warns that the body-core face mesh "is not valid"; reimport and independent exported-GLB tests pass, but this warning must be resolved or reviewed before promotion. No runtime asset changed. The candidate SHA is frozen for the Director's actual High/Performance round 6; Blender appearance is not a likeness verdict.

The planned trouser contour adjustment was held after a native-pixel audit. At thigh, knee, calf and cuff heights, the existing front garment boundaries match the traced visible reference by roughly 1–2 pixels: left thigh `48.2..103.5` vs `49..104`, knee `43.2..102.5` vs `43..103`, calf `47.3..98.5` vs `48..99`, cuff `52.6..94.7` vs `54..95`; right thigh `116.3..181.3` vs `116..179`, knee `114.3..178.3` vs `114..177`, calf `117.5..175.2` vs `117..173`, cuff `121.3..169.1` vs `121..168`. The right outer thigh contour is partly hidden by the holster. Crew is independently measuring intermediate cloth inflections; broad narrowing now would move accepted bands away from evidence. The grouped paint/UV/hair candidate is the next game-visible test of whether directional cloth texture changes the straight-leg impression.

### Hairline/contact plan before source edit

The painted side view exposes tan scalp under the angular hair cap. Existing front clumps 18–27 bottom out near z≈1.778, above the observed reference hairline near z≈1.766. The cap itself nearly touches the skull front at z≈1.75, but at z≈1.82 its front y≈−.068 is about 12 mm **behind** the skull's y≈−.080. Thus a uniform outward offset would be unreliable. Sample the actual skull front/temple surface at several hairline heights and reshape the existing cap-front rows to remain slightly outside it, with a curved front edge descending to the measured hairline. Reposition current short front clump tips to overlap that edge and sink side roots onto the cap. Preserve cap crown height, face geometry/UV/paint, garments, rig and all accepted source envelopes; reallocate existing hair vertices only, leaving actual near triangles ≤8,000. An independent coverage/contact red test must precede this source edit. Rebuild to a distinct ignored paint-3 output, then check matched front/side/back and actual game views; do not infer likeness from geometry counts alone.

## Round-6 actual verdict and next design boundary

The Director captured the frozen paint-5 SHA `b226110a496145feb1ff56ffbdc9f1b2c6ba208358d8900afb1668078199cdaa` in the actual game on private port 5808: 44 High/Performance captures, zero browser issues, immutable `round-6.jpg` at 90,574 bytes. Its scored review is resemblance **3**, readability **3**, grounding **3**, scene consistency **3**; frame cost is unmeasured. The material method improves Rook and the waist stays covered, but the score does not pass the required 4s. Side/back still show a large tan crown patch. The front face is recognizable; hair cap, scarf and cuff/hand forms remain simplified. This is a reviewed trial only: no runtime GLB is replaced and the other seven crew assets remain unchanged.

Before another mesh or paint edit, investigate whether the tan crown is exposed geometry, the face chart's portrait-background color above the measured hairline, or both. The next grouped method will author a dark scalp/hair base in the existing face atlas above the observed hairline while preserving painted eyes and beard, reshape existing hair clumps around actual scalp rather than adding sparse probe guards, and make the scarf read as layered neck cloth with rolled sleeve/cuff transitions. These must fit the existing near/far limits and keep accepted vest/boot/trouser envelopes, face features, waist coverage, rig clips and single material. Crew is independently diagnosing actual side/back pixels and geometry. Record measured source/UV bounds and independent failing acceptance before implementation; use a distinct ignored candidate output and actual High/Performance review for the next score.
