# JANITOR-AUDIO-NOTES

status: ready to merge

## Design before edits

Fold the durable audio rules and current evidence from AUD-10, AUD-11, AUD-GATEKEEPER, and AUD-14 into `docs/AUDIO_ITERATION.md`. Keep the useful engine, ambience, tire, mix, and tunnel explanation already there. Replace obsolete claims that source WAV/MP3 files ship in `public/` and that the current build needs no FFmpeg. Distinguish committed compressed runtime sounds, the two irreplaceable retained sources, external licensed-source cache, and exact catalog recipes. Document strict flag-off baseline, enabled routing, booth and measurement limits, gatekeeper voice, recorded combat cues, and intentional finite projectile flight voice. Keep human listening pending where it is pending. Delete only the four consumed notes once the durable text is complete; Git retains their historical detail. No audio asset, license, code, save, external folder, or user decision changes.

## Checks

Only the board card refers to the four note paths, as ownership metadata. No live documentation or test consumer links to them. Independent Director review found and corrected the legacy-versus-new source cache path, file-versus-loop count, and rebuild-command scope; native-rate capture is now explicit. Lane tier passed: 6 suites, 0 failed, including 518 core checks. Build passed with Vite's existing chunk-size advisory. The first build attempt hit sandbox EPERM on Vite's temporary config; the approved retry passed. No synthetic wording tests were needed for this documentation fold.

## Removed

Removed four consumed task notes after folding their current facts into the durable guide. Git keeps their historical detail. No audio source, runtime file, license, voice take, AUD-12, or pending AUD-17 note was removed.
