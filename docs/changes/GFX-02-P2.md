# GFX-02-P2: broad Rook sleeve construction proof

status: round-1 visual 3/4/3/3; round-2 material proof built, matched review pending; runtime assets unchanged

## Decision and baseline

P1 finished as a non-promoted Rook hand proof. Its connected hands, wrap, skin pixels, rig, eight clips and explicit selected paint are useful, but both actual-game reviews scored likeness/readability/grounding/consistency 3/4/3/3. The current production `hands/rook.glb` SHA-256 is `34de29dd6a2fb446cd9b8ac2f48ba5ffa3a0171ce40a331eb427763cd702c02c`. The reviewed P1 candidate SHA-256 is `8b985010d7a511503fa2fdeb37d7665bfad420f09b6751f87110d3ce55e30579`; it is a construction baseline, not the selected runtime asset. The P1 recipe and `tools/blender/first-person-p1-source.json` remain rebuildable under `--p1-rook` with the explicit original paint path/hash. P2 will use a separate opt-in mode and source JSON so no P1 output or behavior silently changes.

The open question for this slice is whether broad cut-and-sewn cloth volume can correct the long narrow sleeve silhouette at game scale. The first P2 proof changes only Rook sleeve construction. Palm, thumb, digits, skin, wrap, tool meshes, sockets, bone names/weights, clips and action timing remain P1-equivalent. Contact calibration is a later written and independently tested subtask; the P1 14-pose contact result is unsupported, not passing. No runtime GLB is replaced unless all art, contact and frame gates later pass.

## Source, CLI and isolation contract

Add `tools/blender/first-person-p2-source.json` with `schemaVersion:1`, `units:"metres"`, `frame:"camera-local-x-right-y-up-negative-z-forward"`, `parentRecipe:{path:"tools/blender/first-person-p1-source.json",sha256}`, `selectedPaintSha256`, `sleeves:{R,L}`, and provenance labels marking the volume controls as inferred construction, not traced pixels. Resolve and hash the committed P1 JSON before output; a mismatch stops before writes. P2 source records exact panel angular domains, axial controls and seam paths. P2 may read the P1 authored hand landmarks, clothing and material crop fields; it does not duplicate or silently override them.

Use `--p2-rook --output-dir <ignored art-build/first-person-p2 subtree> --p2-paint <PNG> --p2-paint-sha256 <hash>`. `--paths-only` validates a dedicated ignored destination and source path/hash before Blender import or writes. Reject simultaneous `--p1-rook`/`--p2-rook`, paint flags from the other mode, a missing paint path/hash pair, and hash mismatch before any write; never silently choose or fall back to P1. The standard candidate output is `art-build/first-person-p2/candidate/hands/rook.glb`, sibling `rook.blend` and three authored maps, root `manifest.json`, and `evidence/blender-manifest.json` plus eight pose PNGs. A test-owned 1254² triptych with its own correct explicit SHA may build a fresh isolated fixture; the manifest marks `selectedArtwork` only when the input hash equals the reviewed original `785c80bb03380c6454607e5fba687b633e47642cac0a1b58d7176afdf9a64b3b`. Default `--p1-rook` and no-flag export paths stay unchanged. No generated source PNG enters `public/`; no added runtime dependency or material draw.

The selected input remains the original generated file `C:/Users/kyleb/.codex/generated_images/01a0d53e-3baa-7c12-883d-38d2395c6099/exec-643b8830-8970-4809-a7a2-19004a01fa10.png`, SHA-256 above. P2 reuses the verified P1 three square crops and *actual direct-glTF-V consumer mapping*: cloth/leather/wrap occupy saved PNG top-origin y776..1016, protected actual skin stays byte-identical. No new image or painting before the silhouette proof. The P2 manifest records the parent P1 source SHA, P2 source SHA, explicit paint path/SHA, exact authored/embedded map hashes, actual GLB hash, triangles/draws, and pose/camera hashes. A clean checkout requires this original image via explicit CLI; a no-input procedural fixture is not the selected result. No atlas extraction/reapplication command is claimed.

## Sewn sleeve geometry

First export adjustment before scored capture: the initial R elbow measured 11.963 mm of added breadth at the second outer angle (145°), just below the independent 12 mm floor. The source also contained an extra hard-coded 2 mm Gaussian crest that was absent from this design. Remove that extra crest and set the authored t=.40 outer offset to R 20 mm and L 23 mm, leaving the other axial nodes, seams, falloff and frozen regions fixed. This funds visible breadth through the reviewed panel controls rather than an undocumented bump. Rebuild and rerun the unchanged actual-export test before accepting the change.

Test-oracle correction: the first frozen-cloth comparison classified both arms by the requested arm's Z-derived axial coordinate. An L interior vertex at z=-0.6136 m appeared as R t=1.64 and was falsely treated as frozen R cloth. The independent test now assigns each vertex to its nearer authored arm before comparing the same exact positions, UVs and skin weights on that arm's frozen rings. The 12 mm breadth and other shape thresholds remain unchanged. The amended t=.40 values in this paragraph supersede the earlier preliminary and node arrays below.

Source correction from the next export: the P1 authored fold is nonzero on the L transition ring at t=.28. Returning zero from P2 outside the altered interior rings changed that supposedly frozen surface. P2 now calls the original P1 fold displacement on every ring outside t=.40..77. The unchanged independent frozen position/UV/weight comparison and sewn-volume test then pass. This measurable contour difference is structural evidence, not a visual likeness verdict.

Final design scope for this first sleeve proof: remove the prospective cross-panel crest-drift field because the unapproved Gaussian crest was removed and no geometry consumes that field. A separate diagonal crest shift is deferred. The actual implemented diagonal variation comes from the documented seam-angle paths and axial bulge/gather/ridge stations. This paragraph supersedes the earlier crest-drift proposal below; no likeness improvement is claimed from an unused setting.

Use the existing camera-local authoring frame and the same elbow-to-wrist centreline, bones, grip and mounted RPG/wrench poses. Axial `t` is the current elbow-to-wrist interpolation used by the P1 sleeve stations. `t>=0.82` and the hand/grip envelope are frozen. The first ring at the offscreen start and the wrist/glove contacts also remain fixed. These physical dimensions are inferred garment construction limits, not pixel measurements from the differently framed RPG photo.

Each side gets one connected cloth shell with a broad outer/front panel and a taut inner/underside panel. They share actual boundary vertices/edges at longitudinal seams; UV seams may split exported vertices but must weld to one connected surface within 0.1 mm with no edge having more than two incident faces. There is no second overlapping sleeve tube or hidden closed cap. Panel boundaries are measured in authoring XY azimuth from +X toward +Y: R front/outer panel `[40°,200°]`, L `[-50°,125°]`, with the complementary arc as the underside. The two longitudinal seam paths may drift at most 12° across the forearm and sit 1–2 mm proud of the local surface; they must not become bright painted stripes. The visible front panel uses the existing cloth chart with continuous longitudinal UV, and the underside samples the same padded cloth material with altered grain direction only through UV orientation. Keep existing exact skin and tool charts untouched.

Freeze these first shape controls in `sleeves.R/L`, with each path defined by ordered `(t, radialOffsetMetres)` stations and a broad angular falloff that reaches zero at the sewn boundary. R outer panel: elbow bulge `+0.020 m` over `t=.34..52` with peak near `.43`; gathered pinch `−0.009 m` over `.60..68`; diagonal compression ridges `+0.012 m` near `.69` and `+0.010 m` near `.77`. L outer panel: bulge `+0.023 m` over `.34..52`, pinch `−0.010 m` over `.60..68`, ridges `+0.013 m` near `.68` and `+0.009 m` near `.78`. A front/outer radial falloff spans roughly 105–140°; the underside stays close to the P1 radius and can narrow by at most 4 mm to show cloth tension. The absolute bulge envelope is 15–25 mm, pinch 8–12 mm, and ridge height 8–15 mm. Smooth the transition across neighbouring axial rings without periodic whole-circumference bands. Reuse or replace the P1 small fold paths deliberately: broad panel volume should dominate the silhouette, while fine folds may remain only if they do not restore a regular tube/ring rhythm.

The source JSON must store these values, the angular domains, seam clearance and the frozen `t>=.82` boundary. The builder may choose triangulation but cannot invent different controls without updating the written design before code. Start within the P1 4,260-hand triangle allocation; RPG adds 3,276, so at most 464 extra hand triangles remain under the 8,000 loaded limit. Reclaim uniform hidden ring/subdivision detail before spending them. One hand material primitive plus the unchanged tool must stay within three draws. The 1024 color/surface/normal set, padded actual-face UVs and original selected image identity remain fixed for this first proof.

## Independent tests and matched review

The exact P2 source nodes supersede the preliminary ranges above. At axial `t=[.28,.40,.52,.60,.69,.77,.82]`, R outer-panel radial offsets in metres are `[0,+.020,+.006,-.009,+.012,+.010,0]` and L are `[0,+.023,+.007,-.010,+.013,+.009,0]`. Both inner/underside panels use `[0,-.003,-.004,-.004,-.002,-.001,0]`. Interpolate without overshoot; P2 retains the exact P1 authored displacement outside the altered interior rings, including at the .28 transition and every t>=.82 ring. At these same nodes, R lower seam angles are `[40,44,48,46,42,40,40]°`, R upper `[200,196,192,194,198,200,200]°`, L lower `[-50,-47,-43,-45,-48,-50,-50]°`, and L upper `[125,122,118,120,123,125,125]°`, all in authoring XY azimuth from +X toward +Y. Interpolate linearly between angular nodes. An outer-panel displacement ramps from zero at either seam to full strength 20° inside that seam, then stays full through the middle; the inner panel uses its own offset. A 1.5 mm raised seam may extend at most 5° on either side and tapers to zero at `t=.28/.82`.

Keep all P1 axial rings and **20 angular vertices per ring**. On deformed cloth rings `.40,.52,.60,.69,.77`, redistribute those 20 angles monotonically around the circumference: reserve a vertex at each exact interpolated panel seam and at 20° inside/outside each seam, then distribute the remaining 14 angles across the two arcs while preserving cyclic order. The reserved vertices resolve each boundary/falloff and the `.69/.77` ridges without adding triangles. Keep the original uniform P1 20-angle positions, vertices, UVs and weights on the first/offscreen ring, `.28` transition ring, `.82` and every later skin/cuff/hand ring. Displacement is zero at `.28/.82`; connect adjacent 20-vertex rings one-to-one with the original manifold quads, without a 24-to-20 seam or second shell. No other P1 non-sleeve triangle, UV or skinning value may change. This corrects the earlier prospective 24-angle allocation before code; no independent assertion was changed. The design values are inferred cloth controls, not reference-pixel traces. Actual exported triangles decide the 8,000/three-draw limit. Crew's test compares outer/inner positions at the same P1 centerline and angles and verifies first ring, `.28`, `.82` and all non-sleeve geometry/UV/weights byte-equivalent after normalization rather than relying on landmarks alone.

Crew writes red actual-export acceptance before generator changes. Test `--paths-only`/isolated output and P1 recipe/production eight-asset byte preservation. On decoded actual GLB, compare P1 and P2 at the same authored camera and tool landmarks rather than raw pixels from different cameras. Require one connected sewn cloth surface after 0.1 mm weld, no 3-face seam, no non-wrist cloth holes, a visible broad nonmonotonic contour: outer-panel radius exceeds matched P1 by at least 12 mm in an elbow band, falls at least 6 mm below its own neighbouring bulge in the gather band, and recovers at least 8 mm across a diagonal ridge at multiple axial/azimuth positions. Require controls to taper to zero at panel boundaries and `t>=.82`; reject a whole-ring sinusoidal swell or disconnected overlay that would satisfy a single point. These thresholds are below the inferred design offsets and above the 2.5 mm P1 micro-fold test. Measure actual exported positions and normals, not JSON tags alone. Keep the existing P1 connected palm/digit, rounded tip, wrap, rig, clips, socket, UV, source-to-embedded-to-consumer paint, 8,000 loaded-triangle and three-draw tests. Any changed assertion needs a recorded old/new meaning and independent review.

Career owns the P2 private memory-only candidate fetch, eight authored pose/tool rows and sheet hooks. Use the same camera `{position:[0,0,0],target:[0,0,-1],verticalFov:72,near:.15,width:1280,height:720}` for Blender and actual High/Performance game views. Separate immutable family `docs/board/looks/first-person-p2/round-N.jpg` and matching review note. The approved RPG reference supplies material and overall arm direction, not a native-pixel contour oracle for this camera. The family already has its required three measured rounds; P2 runs scored rounds until the target and other gates pass, changes method after two no-gain rounds, and observes the ten-round wave cap. Only a real game likeness/readability/grounding/consistency score of four each can select P2 for runtime. Contact needs its own valid fixed-surface 14-pose proof across reload/fire/repair. A quiet A1/B/A2 complete-render CPU/RAF comparison with both qualities and the unchanged public Rook hand GLB SHA `34de29dd6a2fb446cd9b8ac2f48ba5ffa3a0171ce40a331eb427763cd702c02c` as baseline must stay within 1.10 before promotion. P1 was never a runtime asset. No GPU-time claim from that check.

## Reversal and Removed

## Matched round 1 and next material trial

The first actual High/Performance game review scored likeness/readability/grounding/consistency 3/4/3/3; frame cost is unmeasured. The sewn volume is visible, but the large, high-contrast woven marks still dominate the cloth. The arm remains a smooth pinched loft at game scale. Beige wraps are even and pale, the right wrist shows a bright exposed gap, and the glove remains generic. Geometry acceptance therefore does not select the asset for runtime.

Before changing code, the second trial holds the exact round-1 mesh, rig, clips, tool, camera, leather, wrap and skin charts fixed. It changes only the sleeve-cloth material sampled by actual glTF cloth faces. Add an explicit optional `--p2-cloth-paint <PNG> --p2-cloth-paint-sha256 <hash>` pair, valid only with `--p2-rook` and the existing explicit three-panel paint input. Without that pair, the round-1 P2 recipe must reproduce its current output. Missing, mismatched or unsafe input fails before writes. The input is a square sRGB PNG with each side 1024–2048 pixels and no alpha-dependent content, read from the ignored P2 art source folder or the original generated-image folder. Use the **entire native square** as the crop, box-filter it to 240 square pixels without stretching, and place it only in decoded PNG cloth rectangle x8..247, y776..1015, which actual exported glTF V samples directly. Keep the eight-pixel chart border. Record top-level `clothPaintSource:{path,sha256,width,height,crop:[0,0,width,height],method:"full-square-box-filter-to-240",selectedArtwork}` in the candidate manifest. `selectedArtwork` means the explicit input hash equals the reviewed new cloth original, independent of the existing `paintSource.selectedArtwork` for the three-panel source. A deterministic test-owned 1024² cloth fixture may use its own explicit SHA; it cannot silently become selected art.

The cloth source should depict tightly woven matte dark teal workwear over a roughly 0.5 m sleeve area. Threads should average below one output texel after full-square downsampling to 240². The source must have restrained broad tonal drift (roughly 3–5% locally), subtle irregular abrasion, and no macroscopic knit loops, decorative stripes, text, logos, weapon/hand imagery or baked directional highlights. Proposed image-generation prompt: “Square seamless material swatch of worn dark teal tightly woven canvas workwear, viewed straight on, fine dense thread grain, very subtle irregular abrasion and soot, low contrast, matte, neutral flat illumination, even color across the image, no strong folds, no embroidery, no stripes, no objects, no lettering, no cast shadows.” The original output path, exact prompt, native dimensions and SHA must be retained if the trial is selected. No new source image is embedded in `public/` separately; the selected pixels live in the current GLB atlas.

The first built-in imagegen trial is a 1254×1254 opaque PNG at `C:/Users/kyleb/.codex/generated_images/01a0d549-18ea-7f60-8b6c-cd0d3c382114/exec-f6024d6e-0813-45e4-afc0-41081de6e123.png`, SHA-256 `e4a286ebc7a4b07f0645c63f0eb0a66db93fd9bfcec0f213a0671f5e56bca3ac`. An ignored working copy is `art-build/first-person-p2/paint-source/rook-cloth-r2.png`; retain the original outside lane. Exact successful prompt: “Square seamless game texture swatch: worn dark teal tightly woven matte canvas, viewed straight on under neutral uniform lighting. Very fine dense threads and restrained subtle abrasion; low contrast. No folds, shadows, border, text, objects, stripes, embroidery, or large knit loops. One opaque square image.” This is a visual trial, pending Director review and actual game scoring; image identity alone does not select it for runtime.

For the first controlled paint proof, change the actual cloth color field and its matching surface/normal field only inside that same cloth rectangle. Keep roughness matte around 0.9 with low variance. Quarter the cloth normal-map height/slope before recomputing and renormalizing its normal vectors; never quarter encoded RGB bytes. Compare decoded color/surface/normal bytes outside the cloth rectangle with round 1 and require exact equality, including both protected skin charts. On the actual exported mesh, at least 75% of cloth-face samples must match the downsampled new source through direct glTF V, while leather/wrap/skin samples must remain on their old chart pixels. Verify P2 round-1 geometry, joints, weights, clips, sockets and loaded budget are unchanged. Then use the same eight matched Blender/game poses for independent visual review. If the new weave remains coarse at game size, stop this source choice and keep the earlier candidate; the next design must address projected material scale rather than adding noise.

The independent material acceptance was red before the optional flags existed. The first new export passed unchanged geometry, outside-chart pixels, direct-V cloth consumers and quarter-normal matching, but its full-strength negative control was too weak for the test fixture's low gradients after 8-bit normal encoding. Crew strengthened only the deterministic fixture's dark/light/mid contrast (about 20/240/120 luminance) so quarter versus full normal slope is measurable. The same accepted thresholds then pass the actual-export test; no runtime source, chart boundary or visual criterion was relaxed. The selected e4a286eb cloth art remains separate from this synthetic test fixture.

## First actual-export checkpoint

Crew wrote the red acceptance before model code. The unchanged generator rejected `--p2-rook`; the frozen P1 source also measured 0 mm broad-sleeve surplus and failed the new shape oracle. The first P2 export revealed a false frozen-ring selector that assigned an L interior vertex to R. Crew corrected side classification while retaining exact frozen positions, UVs and weights and all 12 mm shape thresholds. The next P2 export measured 11.963 mm at R 145° and failed the unchanged second-angle minimum. The reviewed source amendment raised the t=.40 outer offsets to R 20 mm/L 23 mm and removed an undocumented 2 mm Gaussian. A further red exposed the P1's nonzero fold on the L t=.28 frozen ring; P2 now preserves that exact P1 displacement outside the altered interior rings. Seam angles use the contracted linear interpolation. The final isolated export passes `node --test tools/test-first-person-cloth.mjs` 8/8 in 6.47 s, independently rerun by Crew. It has 4,260 hand triangles; the unchanged RPG has 3,276, for 7,536 loaded triangles. One hand primitive and the unchanged tool stay within three draws. This is structural acceptance only; actual game likeness, contact and frame cost remain open.

The selected source input for P2 remains the original 1254 px image at `C:/Users/kyleb/.codex/generated_images/01a0d53e-3baa-7c12-883d-38d2395c6099/exec-643b8830-8970-4809-a7a2-19004a01fa10.png`, SHA-256 `785c80bb03380c6454607e5fba687b633e47642cac0a1b58d7176afdf9a64b3b`. Build the standard ignored candidate using `--p2-rook --output-dir art-build/first-person-p2/candidate --p2-paint <original path> --p2-paint-sha256 <hash> --round 1`; normal eight Blender pose renders accompany the candidate manifest. The paint stays opt-in and no public asset changes.

## Round 2 verdict and remaining construction

The final selected fine-cloth trial has candidate GLB SHA-256 `d567b345bb780f20cca0c07f8e6bd56cb3414f8060c8f1fb6f9e62b15fe7e779`, candidate manifest SHA-256 `b00d69b59e8d05b7a3d12bb4df1e14257462236ff3909abbf0a37ef6442ca2b0`, and Blender manifest SHA-256 `aafc32fd0f8efcf93343b134e6de555b67294fce833a8b47c033e9262dbf1bf2`. The explicit base triptych and new cloth input both record `selectedArtwork:true`; the build rendered eight authored Blender poses. The independent P2 suite passed 10/10 in 12.88 s, including actual face sampling, exact preservation of all atlas pixels outside cloth, exact geometry/rig/clip/socket identity and the 7,536 loaded-triangle count. The actual High/Performance game review again scored 3/4/3/3. The finer cloth is quieter and is retained as the development material baseline, but it does not meet the likeness target. At visual review, frame cost was still unmeasured and continuous tool contact remained unproven, so the asset could not replace the public Rook hand.

The later quiet private frame check on this exact GLB and clean lane HEAD `03b0d8a` passed the scoped 1.10 limit: 30 warm plus 600 native measured frames per six A1/B/A2 High/Performance legs, one complete production renderFrame per tick, zero browser issues on private port 10697. High mean CPU B/A1=1.0086 and B/A2=0.9481; Performance=1.0509/1.0616. All CPU p95 and RAF p95 ratios were at most 1.0056; worst required ratio was 1.0616. Raw evidence is `.evidence/2026-09-25/first-person-p2/frame-cost-r2-1/cost.json`. This is idle CPU/RAF evidence, not GPU or action cost. The preceding visual paragraph's “frame cost unmeasured” describes the state at visual review; this later measurement closes only the frame gate. Contact remains unsupported and likeness remains three, so the public asset stays unchanged.

Two flat visual rounds require a changed construction method. The next bounded proposal is to remove the two disconnected rigid wrap annuli per arm and extend the existing 20-vertex cloth shell from sleeve hem to glove edge, with one asymmetric gathered ring and wrap material on the same connected surface. Authored intervals are R z−.4422 to −.468 (25.8 mm) and L z−.7426 to −.808 (65.4 mm). Target at least 90% axial and circumferential garment coverage, 1–4 mm local rise, no interior open or three-face seam, and at least 2 mm clearance from unchanged tool surfaces across sampled action clips. Preserve glove cuff, palm, digits, skin padding, sockets, clip timing and three draws. Reclaim approximately 160 triangles per arm from the old annuli before adding any new cloth ring; stay below 8,000 loaded triangles. This proposal changes the previously frozen clothing boundary, so it needs a separate written choice and independent red actual-export test before source edits. It is not part of the two scored rounds or a contact pass.

Director review did **not** approve those endpoint values as the next build. The R authored glove-edge marker near t=.94 may stop short of actual exported leather near t≈1.0. The next card must measure the camera-facing cloth, wrap, exposed skin and leather surfaces on the exported mesh before setting a coverage endpoint or relaxing the protected boundary. No further P2 model edit follows this speculative proposal.

Reversal is to stop selecting the opt-in P2 candidate; current production hands and P1 recipe remain intact. The first proof replaces only the P2 candidate's monotone sleeve loft and uniform decorative fold paths with joined panels and broad asymmetric cloth mass. It does not delete P1 source code or its protected material data, and it does not remove a runtime asset. Ignored raw candidates and browser evidence are consumed after review; retain only scored comparison JPG/review and exact recipe/source hashes. No history rewrite or release is authorized by this card.
