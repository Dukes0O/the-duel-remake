# GFX-02-P1: first-person Rook hand proof

status: round-two game reviewed below likeness target; round-three changed method designed before code

## Baseline and decision

The merged round-three first-person set remains the runtime baseline. At lane base `86369b8`, `hands/rook.glb` has SHA-256 `34de29dd6a2fb446cd9b8ac2f48ba5ffa3a0171ce40a331eb427763cd702c02c`; `tools/blender/first-person-gear.py` has SHA-256 `ce44f754c56962bb41dc75e9c79922f459bffe73a382bb41b408b495c57f7a3c`. The other seven hand assets, shared tools, selector and renderer remain unchanged in this proof. Round three scored 3/3/3/3; the scoped frame result was 18.1 ms hidden and 18.2 ms visible at p95 with 120 samples, 6,932 triangles and three draws at the largest combination. It did not measure texture memory or full presentation CPU.

The final Rook body P2 visual verdict was likeness three without runtime promotion; its passing gate merged at `3069f34`. Its connected surfaces, measured contours, padded explicit UVs and source verification are useful construction techniques, but they do not certify a winning whole-body method. This hand proof is independent of body promotion and precedes any other crew conversion. Reversal is to stop selecting the opt-in candidate and retain the above committed runtime GLB; no saved data, gameplay, tool asset or old review sheet changes.

## Construction and authored source

Add the small committed `tools/blender/first-person-p1-source.json` for measured Rook hand contours, digit lengths/widths, knuckle and grip landmarks, cuff hem, and painted material chart allocation. It is an authored input with source-reference crop and hash, not a generated mesh dump. Keep the geometry/rig/export algorithm in `first-person-gear.py`. This separates anatomy decisions from reusable meshing code and permits red tests against actual declared landmarks. A new dependency or implicit discovery of ignored files is unnecessary.

Use the generator's existing camera-local authoring metre frame: X right, Y up, and forward along negative Z. The generator's `bv` converts authoring (x,y,z) to Blender (x,-z,y) once. The fixed right RPG grip is (0.14, -0.24, -0.55) m; the left support grip is the same point shifted -0.34 m in Z; the wrench grip is (0.21, -0.24, -0.58) m. These are existing tool/socket coordinates, not inferred reference anatomy. The Rook body/arms crop [0,40,510,675] in `public/assets/reference/wasteland-crew-1.png` establishes identity. The bottom-right first-person crop [790,338,1536,1024] from native 1536×1024 `public/assets/reference/wasteland-rpg.png` (SHA-256 `5b03e9598c972b44e556e6bea6fcb3e1971a89bd2b9380c25b39b6df17c09bea`) gives the more useful arm treatment: worn teal cloth, layered beige wrist wrap and dark leather glove around the unchanged launcher. Use that view for sleeve/glove construction and paint; it does not reveal hidden palm topology. Hidden palm depth, web thickness and fingertip underside are necessarily inferred for grip and motion and must be labelled as such in the JSON and proof manifest. The unchanged actual RPG and wrench meshes are the contact oracles.

The initial anatomy envelope, to refine only through an independently reviewed reference trace, is palm breadth 0.065–0.115 m, wrist-to-knuckle length 0.065–0.125 m, four distinct finger lengths 0.045–0.105 m, and thumb root-to-pad 0.035–0.075 m. The thumb web at its narrowest useful cross-section must be 0.012–0.040 m wide, with visible concavity rather than a separate club. These bounds keep the existing approximately 0.11 m wide palm and posed grip plausible; they are *inferred hand-space construction limits*, not measured pixel claims from the small reference. Source JSON should record the reference/inferred provenance per landmark, then the exported mesh should be checked against the chosen bounds.

The JSON contract is `{schemaVersion:1,units:"metres",frame:"camera-local-x-right-y-up-negative-z-forward",reference:{path,sha256,crop},rpgArmReference:{path,sha256,crop},hands:{R:{landmarks:{wrist,palm,knuckles:{index,middle,ring,pinky},fingertips:{index,middle,ring,pinky},thumbRoot,thumbWeb,thumbTip,distalStations:{index,middle,ring,pinky,thumb}},provenance:{...}},L:{...}},atlas:{size:1024,paddingPixels:8,charts:{skin:{rect},leather:{rect},cloth:{rect}}}}`. The original `reference` keys remain the crew sheet; `rpgArmReference` records the bottom-right first-person crop and hash above. Each landmark is a three-number authoring-frame point. Each `distalStations` entry is exactly two centerline points, penultimate and terminal, along the last curved 6–15 mm; their vector gives a local fingertip tangent for normal tests, rather than the misleading knuckle-to-tip chord. Provenance labels each landmark `reference-visible` or `inferred-grip`. Chart `rect` is `[x0,y0,x1,y1]` in integer PNG pixels; rectangles are disjoint after padding. Verify PNG top-row versus exported glTF V orientation against the actual embedded Rook atlas before testing sampled colors; do not assume `1-v`. The source must contain actual chosen numbers before export, not defaults silently invented by the builder. On export, the skinned hand node carries `extras.handRegions`: keys `R:palm`, `R:thumbWeb`, `R:thumbTip`, `R:indexTip`, `R:middleTip`, `R:ringTip`, `R:pinkyTip`, `R:cuffSeam` and their L equivalents each map to `{center:[x,y,z],radiusMetres}` in the exported camera-local metre frame. These are selector hints, not vertex indices or proof. Acceptance selects actual decoded mesh vertices/faces inside the spheres, rejects empty/wrong regions, and measures positions, connectivity, normals and UVs independently.

Build one connected deforming hand surface per side from wrist through shaped metacarpals, palm, thenar web, thumb and four separated fingers. Branches must share welded surface topology with the palm rather than merely occupy the same skinned object. Weld vertices within 0.0001 m (0.1 mm), then prove an edge-connected hand component; a pinched one-edge bridge does not count as a useful palm/web cross-section. Use staggered digit lengths, width taper, natural knuckle bulges and distinct curl arcs around the tool handle. The thumb web should have a broad concave bridge and a smooth root transition. Avoid palm slabs, separate pad shells and identical finger tubes. Retain current wrist/finger bone names, skin weights that sum to one, `rpg-mount` and `wrench-mount`, and all eight clip names/durations so the current renderer can load the proof without a code change.

Terminate exposed fingertips with rounded multi-ring pads: at least two tapering rings over the last 0.006–0.015 m, a positive-radius end, outward cap normals and no flipped hard edge at the side-to-pad junction. Their terminal faces must sample the skin chart, never the leather chart that closes the current skin loft. Place the glove-to-skin boundary on the intended finger joints with a padded UV seam; require each side to sample its declared material, with at least eight texels of safe padding at 1024 resolution and no tip UV collapsed to a point. Keep a single material and existing 1024 color/surface/normal set for Rook. Start with a deterministic procedural atlas using explicit padded skin, leather and cloth charts plus painted broad wear/seam masks; compare actual in-game pixels to the approved reference. If it stays generic, propose a separately verified reference-derived paint source with its hash and protected charts before using it. Do not imply that unspecified detail is already authored. Build an asymmetric tailored sleeve shell with an open fitted cuff, hem thickness, seam path and worn edge, separate from the continuous hand. It may overlap the wrist but must not make a fake closed skin cap. Use explicit cloth UV strips aligned to the forearm rather than repeated generic sine-fold rings. Preserve texture padding and protected existing tool charts.

Crew calibrated the current exported Rook GLB against its decoded embedded PNG before implementing the new atlas: distal skin samples were 16.3 color-distance units from the skin tile with PNG top-row `y=1−glTF V`, versus 65.1 with direct V. Candidate pixel tests therefore use `1−V` for this asset. This closes the convention check above without borrowing a mapping from another GLB. In the manifest, `asset.textureBytes` means the sum of encoded byte lengths of the three authored PNG files named by `textures`; it is not decoded GPU memory. Report approximate decoded GPU allocation separately in the later frame/resource review.

## Candidate isolation and review

Proposed CLI: `--p1-rook --output-dir <ignored path under art-build/first-person-p1>`, with `--paths-only` validating the destination before importing Blender or writing files. The standard candidate output directory is `art-build/first-person-p1/candidate`; the GLB is `art-build/first-person-p1/candidate/hands/rook.glb`. Tests may override the candidate path with `GFX_FIRST_PERSON_P1_CANDIDATE`. The default generator behavior and production output paths remain unchanged. The opt-in export writes only Rook's candidate GLB, its rebuilt blend/textures, and manifest to the ignored destination; it must not overwrite `public/assets/models/wasteland/first-person/hands/rook.glb`. The manifest records source JSON/reference hashes, camera/near-plane, material maps, bounds, topology metrics, triangle/draw counts, capture paths and output SHA. No candidate binary is committed before a reviewed promotion.

For independent path tests, `--paths-only` prints JSON with `mode:"p1-rook"`, resolved `outputDir`, `sourceJson`, `candidateGlb`, `candidateBlend`, `textures:{color,surface,normal}`, and `manifest` paths. The candidate names are `hands/rook.glb`, `hands/rook.blend`, `hands/rook-{color,surface,normal}.png`, and `manifest.json` under that output directory. No directory may be created during the path-only call. The built manifest uses exact top-level keys `schemaVersion:1`, `mode:"p1-rook"`, `source:{path,sha256,referencePath,referenceSha256,rpgArmReferencePath,rpgArmReferenceSha256}`, `asset:{path,sha256,triangles,draws,textureBytes}`, `textures:{color:{path,sha256},surface:{path,sha256},normal:{path,sha256}}`, `regions` with each named region's bounds and actual connected-component ID, `charts` with the pixel rectangles and padding, `clips` with names/durations, `sockets` with positions, and `captures` with path/hash/pose. Hashes refer to exact bytes produced/used by this candidate build, not a later rerun. The build should fail on a public or outside output path before writing, and tests should compare production GLB SHA before and after the isolated build.

Use a private memory-only browser origin for same-origin fetch substitution of only the Rook hands URL. The candidate must pass the production loader, skin, sockets and clips; all other files load from the unchanged bundle. A separate immutable `docs/board/looks/first-person-p1/` sheet family compares native reference, Blender, game High and Performance at matched idle, aim, fire/recoil, reload and repair views. Show a short ordered motion/contact sequence across reload insertion, recoil recovery and repair, including first/last action boundary. Measure grip gaps, tool penetration, near-plane clipping (0.15 m), HUD clearance and full car/menu/other-camera exclusion at the existing 72-degree on-foot FOV. Real input proves exit, successful shot, reload, repair and re-entry; labelled pose snapshots supplement it. Do not infer continuous contact from a single pose. The existing 18.1/18.2 ms result is a limited 120-frame observation. Candidate frame acceptance requires a quiet, same-scene A1/B/A2 comparison of complete production renders in both qualities with ordered CPU and RAF samples, means and p95 against both baselines; do not infer a GPU-time result.

Contact applies to named phases, not every fingertip. In idle, aim, fire and recoil recovery, the right palm/index/thumb grip region must remain within 0.015 m of the RPG handle, while the left support palm remains within 0.020 m of its support grip. During reload progress 0.18–0.76 the left hand deliberately releases that support, reaches the rocket and guides it; require its thumb/index grip region within 0.020 m of the rocket at the guide/insertion samples, then support-handle contact again by progress 0.90–1.00. No left-handle contact assertion is valid during the released interval. For wrench idle and repair, the right palm/thumb grip region stays within 0.015 m of the wrench handle while the tool strokes; the left hand is free and has no tool-contact requirement. These are candidate proof tolerances in tool-local metres, to be evaluated on skinned vertices and the corresponding unchanged tool meshes across ordered phase samples. Allow fingers not used for the pinch to curl freely, but reject visible hand/tool penetration beyond 0.005 m at contact zones.

## Independent red tests before source edits

Crew owns the acceptance tests. Exported GLB tests should inspect actual connected components and manifold branching across palm/thumb/five digits, avoiding an object-name-only pass; meaningful web cross-section and fingertip curvature; outward pad normals and skin-chart UV/pixel sampling; padded material islands and sleeve chart orientation; unchanged wrist/finger bones, skin normalization, sockets, clip names, animation duration, and last-rocket behavior. Preserve the existing 8,000 visible-triangle/three-draw cap with selected tool, 1024 maps, and all default eight-crew tests. Candidate path tests must reject public/outside targets before writes and prove production hashes unchanged. Browser acceptance uses both qualities and memory-only storage. A frame check must compare candidate to baseline in the same real scene and report texture memory as well as draw/triangle counts. A topology or test pass is not a likeness score: independent visual review needs at least four on the Rook hand proof before conversion or runtime replacement.

No `src/first-person-gear.js` change is authorized. If motion review proves the current selector/renderer causes contact loss independently of the GLB clips, record a reproducer and request a named card hook before editing that source. The body P2 merge and Director design approval precede generator implementation.

## Tests and evidence

Crew's first independent tests were red on unchanged source: candidate CLI/source/geometry were absent and the current Rook thumb was disconnected from the wrist/palm after a 0.1 mm exported weld. The initial isolated Rook candidate builds at 3,984 hand triangles and one material; the other seven hand GLBs and production Rook SHA remain unchanged. The first ordinary fresh candidate test passed 11/12, including source/path planning, own-build atlas pixels, sockets/clips/budgets, and connected hand components. Its remaining failure was an actual Rook thumb-tip pixel looking more like leather than skin. Inspecting the authored PNG showed that Blender's bottom-up pixel buffer placed source tiles 12/13/14 in the exported top-row cloth/leather/skin UV charts; the old procedural palette left top-row skin dark grey. The P1-only palette now paints those actual sampled charts explicitly. Rebuild and test remain pending; no browser likeness or frame score is claimed yet.

Crew corrected one test fixture pairing after independent review: a read-only candidate override under `candidate/` was incorrectly paired with `test-proof/manifest.json`. The selected GLB now pairs with its own manifest/maps under the same ignored root. The focused test is green 1/1. No geometry/UV threshold, hash requirement, or source was changed to make it pass. The default fresh `test-proof` build remains required.

Crew's read-only old-face diagnostic confirmed the skin-padding issue: terminal thumb face 1632 sampled PNG-top UV [522.24,167.25] to [541.87,245.76], only 2.24 px inside the declared skin rectangle's left edge and 2.24 px above its lower edge, where the test requires eight more pixels. The face was 3.84 mm from the thumb tip, outward facing. P1-only skin UV now uses inner .1–.9 tile coordinates and 12/12 focused tests pass on a fresh isolated export. This corrects actual exported pixels, not an assertion.

Career then found two further source defects before visual review: P1 still honored arbitrary `DUEL_EVIDENCE_DIR` outside its validated candidate tree, and the connected finger/thumb bridges were sewn onto already closed root caps and an interior palm ring. Those edges had three incident faces and failed manifold construction despite reachability passing. The first geometry correction used an open palm terminal ring with 20 edges and five outlet arcs of four edges each. Index, middle, ring and pinky use arcs 0–3, 4–7, 8–11, 12–15; thumb uses 16–19. Each twelve-edge digit root ring stays open while its distal pad remains capped. Digit root edge j maps to palm arc edge floor(j/3); successive root edges sharing one palm vertex make a triangle, while every third step makes a quad. That removed all >2-face edges in the actual exported GLB. Career independently classified the remaining 188 boundary edges: ten intended proximal sleeve/cuff loops, plus one genuine ten-edge distal palm hole per hand between outlet webs. A bounded leather web fan then filled each actual distal loop without touching sleeve/cuff openings or returning third-face seams. The 8,000/three-draw cap and all tip UV, grip and clip checks remain. The independent manifold test is unchanged apart from its reviewed exemption for proven clothing loops.

Crew first recorded the broad boundary assertion red on the actual old candidate; Career classified its false-positive proximal sleeve loops and the two genuine distal holes. I wrote the bounded hole-fill source before Crew's refined check arrived, but did not build it. Crew then froze a refined assertion red on the exact previous GLB SHA `f478a564b93766886bae62ac12bb723806eafc27d139c0430a1e6a8f60264319`: ten distal hole edges remained per hand; only the proven clothing loops were exempt. The fresh repaired export passed all 14 independent tests. Career independently measured zero >2-face edges, zero reversed adjacent pairs and zero distal openings on its SHA `c24a1ec3c11705050eb4c73b236bbee71dc79ee9a01676edb2bfa849d681789f`; only intended proximal sleeve/cuff loops remain. This is topology evidence, not a likeness score.

The isolated Blender round-one proof rendered eight source-module poses in 25 seconds. Its manifest at `art-build/first-person-p1/candidate/evidence/blender-manifest.json` records the camera, reference crops, unchanged tool hashes, candidate SHA and each PNG hash. The first idle inspection shows smooth teal sleeves and bright beige fingertip wedges. Production hands and shared tool GLBs remain byte-identical. The private game run captured 28 screenshots on memory-only port 43649 with zero console warnings/errors, including 16 matched poses and 12 real-input views. Each quality made exactly one positive Rook candidate request. The published immutable sheet is `docs/board/looks/first-person-p1/round-1.jpg` (253,447 bytes). Its `observationCommit:e6cb702` names the checkout used for capture, but that checkout had uncommitted source. The commit alone does not rebuild the candidate: frozen review inputs are candidate GLB SHA `c24a1ec3c11705050eb4c73b236bbee71dc79ee9a01676edb2bfa849d681789f`, generator SHA `86ff740315e4bf28b3ca817b91458a856170618458beF37d4fa41f5cf1f1b9f9`, source JSON SHA `13d1987d96b8d3634af0c30aab71999451aaa9cb97001fd9f225589ae43ff655`, scenario SHA `0797ebf3c41766eadcc86892ed46582ac92860bc99b81818b04ad81ae3c9aeb8`, and sheet helper SHA `dc601e92a687b6732346d4b07bdcf3bd9a9cdf2644ffd571cf1cbe0a9def29ed`. Root's game-sheet scores were likeness 3, readability 4, grounding 3 and consistency 3; frame cost remains unmeasured. This is a development candidate, not accepted runtime art.

## Round-two method, settled before source edits

The round-one reference/game comparison shows a long exposed skin cylinder between each glove and sleeve, a smooth tube-like forearm, equally spaced bright wear dashes, flat light leather, and a pointed peach thumb pad. The approved RPG crop shows overlapping beige wrist wraps, heavy teal cloth and dark worn leather. Keep the accepted connected palm, thumb web, digit tips, clip names, socket positions and unchanged RPG/wrench meshes. Change the visible construction and paint first; no camera, rig or weapon movement is proposed. This proposal waits for independent round-one review and red acceptance before code.

**Connected cuff and wrap.** Leave the skin hand surface as one manifold branch per side. Replace the separate narrow sleeve hem and glove-cuff rings with an open, overlapping cloth cuff and two or three staggered wrap turns that bridge most of the current visible wrist interval. They are clothing shells, not skin caps or fake hand connections. Each turn has measurable thickness, a nonuniform width, a slanted overlap seam and a tucked end; the upper sleeve remains visibly outside the inner wrap. Offset the left/right wrap breaks and folds so both arms do not repeat one cylindrical stamp. Skin remains visible as a narrow transition, not a long uninterrupted tube. Prove in exported mesh that neither hand gains a distal opening or >2-face edge; sleeve and wrap openings stay intentional, and wrap shells do not enter the launcher or wrench grip envelope in sampled poses. Record actual added triangles and remain under 8,000 visible triangles and three draws with a single 1024 map set.

The frozen R1 authoring-frame wrist axis fixes the clothing oracle: the sleeve hem center at `t=.82` is R `[.2391,-.3047,-.4422]` and L `[-.0095,-.3047,-.7426]`; the glove's proximal edge center is R `[.205,-.285,-.468]` and L `[.075,-.285,-.808]`. Their straight centerline gaps are about 47 mm and 109 mm respectively. Round-two source JSON will record these as `clothing.R|L.sleeveHem` and `gloveEdge`, with `minWrapCoverage:0.70` and `wrapThicknessMetres:[0.001,0.004]`; the test measures the exported clothing surfaces projected onto each side's actual axis, not just these numbers. At least 70% of each old exposed interval should be covered, leaving a short skin transition. `extras.wrapRegions` may locate the R/L geometry, but actual triangles and their sampled material establish coverage and thickness. Tool intrusion uses the unchanged RPG/wrench meshes across the held poses. These are inferred construction endpoints from the existing generator, not reference-traced coordinates.

**Weighted sleeve shape.** Replace the alternating ring-radius sine fold (current `fold` and `sleeve_folds` logic) with authored, low-frequency fold paths on the forearm: one compression bunch near the cuff, a diagonal tension fold running from inner wrist toward elbow, and a few unequal soft troughs. Use the existing skinned sleeve rings and cloth UV strip; add only the ring/profile control needed for visible asymmetric fold planes. The silhouette must retain forearm taper and never look like stacked torus bands. Compare idle, aim, recoil, reload and wrench poses, since a static fold that shears under skinning is not acceptable.

**Paint method.** The procedural atlas remains the safe first build, but its current high-frequency dash mask does not carry the reference material. Author broad teal fading at fold crests, darker seam/underfold shadows, beige wrap fibers and frayed edges, and dark leather knuckle/palm panels with a few stitch and abrasion paths at material scale. Use the existing padded cloth/leather/skin charts and preserve the verified exported PNG top-row sampling convention (`1 - glTF V`). Remove the evenly spaced pale dash pattern from the Rook cloth charts. Record protected chart pixel hashes and inspect actual embedded atlas pixels before game capture. A copied reference photograph or new generated bitmap is **not** selected for round two: the provided first-person crop is small and perspective-lit, so direct projection would bake tool lighting and cannot cover hidden wrist sides. If authored procedural paint still reads flat in round two, choose a separate painted source with exact prompt/path/hash and a clean-checkout recovery plan before using it; never silently depend on an ignored image.

The wrap has its own declared `atlas.charts.wrap.rect:[776,8,1016,248]`, with the same eight-pixel minimum interior padding as the other charts. This is hand-atlas tile 3; the current Rook hand source emits cloth/leather/skin/cuff tiles 0/1/2/7/9, while its separate tool generator is the only current caller found using tile 3. That tool uses a separate atlas. The exported Rook hand GLB and own-build atlas remain the final consumer check before painting this tile. The wrap must sample beige pixels inside its own chart; no skin-chart reuse or metadata-only color claim is acceptable. Existing skin/leather/cloth pixels stay protected except for the explicitly documented authored Rook cloth/leather changes.

**Thumb and glove border.** Preserve the proven outward rounded skin tip and padded skin UV. Rework only the visible glove-to-skin border and thumb side profile so the exposed pad has a rounded contour and dark leather curves around its base. Move the visible color boundary toward the joint rather than enlarging peach material over the handle. Check the actual exported face normals and sampled skin/leather pixels in the named thumb region; do not recolor the whole connected thumb as a shortcut. The release/reload pinch must still reach the rocket and the right grip must stay on its original handle within the written contact tolerances.

Independent round-two tests should first reproduce the long exposed wrist, repeated wear pattern and wedge thumb on the frozen round-one candidate. They should then inspect exported wrap coverage and glove/skin UV/pixel regions, preserve existing topology/clip/socket/budget/production-hash assertions, and use the private matched game sequence for final appearance and contact. The frozen round-one sheet and hashes above remain immutable.

Round-two implementation checkpoint: Crew's first actual-export test was red on the frozen round-one GLB because the wrap chart was absent. The first new three-turn wrap probe passed the chart and coverage check but produced 5,668 hand triangles; with the unchanged 3,276-triangle RPG it exceeded the 8,000 limit by 944. I replaced it with two thin closed ten-segment turns, removed the superseded narrow P1 sleeve cuff, and extended the wrap closer to the glove. The selected isolated Rook export now has 4,260 hand triangles and one primitive; Rook plus RPG is 7,536 triangles. The authored R/L wrap endpoints, own padded beige chart, actual exported projected coverage, original connected hand, rounded tips, all clips/sockets and production hashes pass `node --test tools/test-first-person-polish.mjs` 19/19 on a fresh isolated fixture. A restricted sandbox run initially produced three `EPERM` errors while creating test-owned directories/junctions; the identical suite passed with those lane fixture permissions. Crew corrected a missing `rgbDistance` helper in the new test without changing its threshold; the focused actual-export wrap test passed after that correction.

The current round-two export is in the standard ignored `art-build/first-person-p1/candidate/` review folder. Frozen SHA-256 values: Rook GLB `531be2b305d7fc9f24e5ddc4f27f699bf65068cc754c82e7c7561a01517af968`, generator `4fc7f58780a16717d983a8c627ddd7894c4f364fa1d99e4476f56339fd383937`, authored JSON `f4414178e37adeedf84dbeda9c611c8a7fa3b374c2aa4d64c13fd1f08430ae34`, Blender evidence manifest `b00f139a3105858e8387085fcc9cf0313d6745c056043575d226da7f0d470192`. Eight matched Blender poses were rendered from that export. The first source-module inspection shows the dashed sleeve stitch has been removed, the beige wrap is visible, and the skin-colored thumb wedge is smaller. These are not game likeness or frame scores. Career owns the private game capture and sheet; hold further model edits until that actual round-two comparison is reviewed.

## Round-three changed method, proposed before code

The Director's actual round-two review still scores likeness 3, readability 4, grounding 3 and consistency 3; frame is unmeasured. The wrap is present but looks like a smooth rigid beige band, the sleeves remain narrow smooth tubes, and the leather reads flat ochre. Since two scored rounds did not raise likeness, the next round changes how the visible surfaces are made. This is a proposal pending Crew's independent round-two review, Director approval and red acceptance; the round-two candidate and runtime stay frozen.

**Sleeve cloth surface.** Replace the Rook-only sleeve's current `sleeve_folds` scalar/ring distortion with an authored cloth surface in the same skinned shell. Source JSON will carry separate R/L fold curves in sleeve coordinates `(t,angle)`, where `t` runs elbow-to-wrist. Each curve defines a crest and neighboring trough and terminates into the surrounding fabric rather than continuing around the whole circumference. Plan two unequal compression folds at `t≈.62–.79` near the wrap and one longer diagonal tension fold spanning `t≈.35–.66`; use widths around 20–55 mm, 6–12 mm raised crests and 3–6 mm recessed troughs in camera-local metres. Their exact positions are authored, side-specific and visibly checked against the RPG reference, not random sine-ring repetition. Integrate the fold control vertices into the existing sleeve faces, with shared boundary vertices and normalized weights; avoid separate floating ridges or closed torus rings. UV cloth direction must follow the actual forearm and crest positions. Reuse most of the current sleeve triangle allocation; no more than roughly 250 net triangles may be spent on targeted fold subdivisions, leaving the actual hand under 4,724 triangles with the unchanged 3,276-triangle RPG. Measure final GLB triangle count after export.

Freeze the initial authored source contract before Crew's new red: `sleeveFoldPaths:{R:[...],L:[...]}`, each item `{kind,stations:[[t,angleDegrees],...],widthMetres,crestMetres,troughMetres}`. `t` is the existing elbow-to-wrist interpolation, and angle zero points along generator authoring +X toward +Y around its forearm section. Angles unwrap across 360 degrees; the local curve is bounded to its stated stations, and height fades to zero at the width boundary. R: compression A `[[.59,120],[.67,103],[.76,90]], .035,+.010,-.004`; compression B `[[.65,180],[.73,160],[.80,138]], .024,+.007,-.003`; tension `[[.35,150],[.50,108],[.65,60]], .052,+.008,-.004`. L: compression A `[[.58,40],[.68,60],[.78,82]], .039,+.011,-.005`; compression B `[[.64,-35],[.72,-12],[.80,10]], .027,+.007,-.003`; tension `[[.34,104],[.49,76],[.64,44]], .049,+.009,-.004`. Lengths and heights are metres, so `.010` means 10 mm. These are **inferred material construction controls**, not pixel-traced body contours. The acceptance test should measure the exported localized crest/trough and diagonal direction against the source, with no periodic full-ring substitute. Before this correction, two proposed L compression paths and one R path lay on the back side of the actual Blender view: simple face-normal dot camera checks on their path stations were negative (-0.42 to -0.15). The revised station directions make all three main curves per sleeve face the actual camera (rough positive dot 0.047–0.379), without changing the amplitude or cost contract. This was a pre-code oracle correction, not a result-driven test change. If later tool intrusion is found, record the evidence and review a source-coordinate correction rather than weakening the physical test.

**Painted material source.** A third procedural color/radius pass is unlikely to fix the generic surfaces. Do not project the small lit RPG photo directly onto unseen sleeves. Make one reviewed, seamless material source image with three clearly separated swatches: weathered teal woven cloth with large wear/color fields and no regular white dash border; dark leather with grain, cut panels, sparse stitching and abrasion; layered beige wrap fabric with frayed seam edges and uneven overlap stains. The reference crop supplies palette/material direction, not an exact camera texture. Prefer a built-in generated paint source after the prompt and chart mapping receive independent review. It is an explicit `--p1-paint <path> --p1-paint-sha256 <hash>` opt-in input, never silently found by path; a no-input synthetic fixture remains only a test recipe and cannot claim to reproduce the selected paint. Bake the three swatches into the existing 1024 atlas's protected padded cloth/leather/wrap charts, plus matching roughness/normal relief, while retaining the skin chart and fingertip pixels. The selected GLB contains the runtime map; no extra runtime file, material, dependency or network request is added. Record the exact original source path/hash and prompt, the atlas extraction or explicit-source recovery command, and pixel correspondence from input to exported GLB before promoting any winner. If no verified source exists, keep R2 rather than claim its procedural atlas is the new method.

The reviewed trial source now exists at `C:/Users/kyleb/.codex/generated_images/01a0d53e-3baa-7c12-883d-38d2395c6099/exec-643b8830-8970-4809-a7a2-19004a01fa10.png`, SHA-256 `785c80bb03380c6454607e5fba687b633e47642cac0a1b58d7176afdf9a64b3b`, 1254×1254 RGB. The exact original prompt is:

> Use case: stylized-concept. Asset type: flat albedo material source for a gritty realtime 3D game, to paint first-person sleeves and fingerless gloves. Create one square opaque image divided exactly into THREE equal full-height vertical panels with straight boundaries, no gaps or labels. LEFT THIRD: faded desaturated dark teal heavyweight woven workwear canvas, coarse directional weave, broad irregular faded patches, subtle dusty abrasion, a few long gently diagonal stitch seams. MIDDLE THIRD: dark umber brown worn glove leather, supple fine grain, broad irregular wear zones, a few sparse curved stitched panel seams, rubbed edges slightly lighter but never orange. RIGHT THIRD: dusty beige woven bandage/wrist-wrap canvas laid flat, dense fibers, uneven horizontal overlapping strips and frayed edges with narrow soft dark overlap stains. Every panel fills its rectangle edge to edge. Material photographed/scanned perfectly straight-on under uniform neutral diffuse light, no directional cast shadows, no highlights, no perspective, no objects, no arms, no hands, no weapons, no logos, no text. Make material-scale details clear but not noisy; large wear fields visible when downsampled to 240 square pixels. Painted photorealistic game texture quality. No rendered cylinders or sculpted folds: actual fold volume is supplied by the 3D mesh. All three panels opaque.

The source consists of three exactly 418-pixel-wide vertical panels. Use square native-pixel crops with no aspect distortion: cloth `[0,0,418,418]` (teal field and one angled seam), leather `[418,90,836,508]` (dark grain and one curved seam), wrap `[836,250,1254,668]` (several frayed overlaps). Crop coordinates are `[x0,y0,x1,y1]` in top-origin pixels with exclusive upper bounds; each is 418×418. Source JSON will record these exact rectangles under `paintSource.crops` and the source SHA above. Each crop is resized with fixed box filtering into the declared 240×240 chart rectangle `[8,248]` on each axis, while actual geometry UVs sample only the inner 224×224 pixels after their additional eight-pixel margin. The whole chart rect gets paint; its outer eight pixels act as bleed rather than another crop. Tests should compare decoded embedded chart pixels to this independently computed crop/downsample, allowing only encoded PNG rounding, and verify protected skin chart pixels exactly against the R2 baseline. Treat source channels as already in the intended PNG color space: do not double-apply gamma while baking. The lane copy is ignored and may be consumed; preserve the original generated-image path. A clean rebuild currently requires explicit `--p1-paint` with this original exact hash. No embedded-atlas extraction/reapplication command is implemented or claimed. Default procedural output is a synthetic fixture, not the selected painted result.

**Proof and reversal.** Crew should first add an actual-export red that distinguishes long, localized sleeve crest/trough geometry from the current smooth R2 surface, checks skin/finger topology and tool clearances, and ensures the paint input is explicit, hashed and isolated. A second own-build test should decode embedded PNG charts, compare protected skin/tool pixels and the selected source-to-atlas bake, and prove no default production overwrite or ignored-image dependency. The same private memory-only Rook fetch and matched Blender/High/Performance poses decide visual likeness; continuous grip/contact and full frame cost remain separate gates. If the painted/folded R3 still scores below four, retain the old runtime Rook GLB and re-slice the residual art work with these source trials and reviews. No history rewrite is part of this proposal.

## R2 contact diagnostic: measured selector limit

The independent contact helper tests are green 5/5 after red-first fixes for active-tool-only snapshots, realpath confinement, finite nested phase labels, per-tool bind poses, face-centroid region selection, the real reload `.999` boundary, and 0.1 mm UV-seam adjacency. The last weld test failed before its fix on six physical positions split across exported UV vertex IDs. These tests validate the measurement method and synthetic fixtures; they do not prove game contact.

The private production-render diagnostic used the frozen R2 Rook candidate SHA-256 `531be2b305d7fc9f24e5ddc4f27f699bf65068cc754c82e7c7561a01517af968`, unchanged RPG `241c03b027360e66424445cbaa6104a6b52faff4fdbe4ab74300f88ee23464a5`, and wrench `82aa9fb0b17a5fefe8127c205cce3e7795ee980e4ca6c828aabbcb39de8f9489`. It read actual skinned hand vertices and mounted tool triangles in camera-local coordinates after the production renderer updated. The authored `R:palm` 15 mm hint includes 12 face-centroid hits. Its nearest connected contact-facing part has five exported IDs but only **four distinct 0.1 mm spatial positions**, at a 5.6 mm minimum tool gap. A separate six-position part is 40.5 mm from the tool. The required six-distinct-position fixed contact patch cannot be formed inside that hint, so the run stops before the ordered 14-pose assessment. This is **insufficient local contact evidence**, not a clearance pass or a measured continuous-contact failure. Diagnostic reports are in ignored `.evidence/2026-09-25/first-person-p1/contact-r2-1` through `contact-r2-7`; their failed attempts trace the centroid, per-tool bind and seam-weld corrections. No contact limit or runtime asset changed. A future reviewed method may sample the actual triangle surface or grow a bounded patch along palm topology; this card does not broaden the region just to obtain a pass.

The other R compression path and L tension path were already front-facing. I rotated them as part of the same pre-code revision to separate the three fold tracks on each visible sleeve; this is a spacing and direction choice, not a hidden-face repair. All six final numeric paths above are the test oracle.

P1 face UVs for cloth/leather currently reach 10.24 pixels inside a 256-pixel tile, whereas this R3 design requires an additional eight pixels inside each declared `[8,248]` chart, namely a `[16,240]` safe range. Before the paint bake, inset only Rook P1 cloth/leather/wrap face UVs to that safe range, keeping the existing circumferential U and axial V order and preserving the already verified skin tile and terminal-tip UVs. Crew's exported UV test should check actual face corners, not the chart declaration alone.

The explicit paint CLI verifies the supplied file against its supplied SHA. Source JSON stores the reviewed artwork's `paintSource.selectedSha256`, crops and method; it does not forbid a test-owned synthetic triptych with a different correct explicit hash. The export manifest records the actual input path/hash/crops/method and `selectedArtwork`, true only when its hash equals the reviewed original's SHA. Normal tests can thus build fresh ignored fixtures without requiring the external generated image, while the final selected art proof must use the original exact hash.

## Removed

The P1 evidence override contract now permits only a resolved dedicated subfolder under the candidate output or the repository's ignored `.evidence` folder. Reject public, outside, junction escapes and the bare `.evidence` root before writes. GLB, blend, and texture output remains in the selected candidate subtree. The first path-guard source edit preceded this added bare-root/junction test request; record that chronology honestly. The existing public/outside override red was recorded before the guard.

Nothing removed in the design step. A later accepted runtime replacement would replace the superseded Rook binary in place; ordinary Git text/binary history remains unless Kyle separately approves a history rewrite. Rejected candidates stay ignored and are consumed after review.
