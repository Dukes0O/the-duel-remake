# ART-W: wall, gate and tool references

status: merged in c736150 after EGG-01

Three original opaque PNG references guide the upcoming Blender work:
Rustwall/gate, RPG and repair wrench. The existing art-direction and crew
sheets informed the prompts. Every full prompt, source filename, hash,
dimension and canonical-view decision is in docs/WASTELAND_ART.md; credits
are beside the images. Originals are retained outside the repository.

All three are 1536 by 1024. PNG structural validation passed, with no alpha,
for a combined 7,221,395 bytes. Repository copies preserve generated bytes.
No runtime code, dependency, save, reward or existing assertion changed.

The existing checkArtIntake function also passed with a catalog containing
these three references: each uses the wide-image rule, opaque pixels,
8,000,000-byte individual limit and the shared reference credits. The combined
limit was 12,000,000 bytes. It found all three files, no missing planned items
and zero failures. An initial provenance-format failure was corrected by using
the checker's canonical date line and a separate timezone line; the checker
and its assertions were not changed. All edited text files use LF.

Independent art review accepts all three as useful modeling references.
Rustwall's apparent height is too small: construct the main wall at 35 m,
span at least 400 m, and the gate opening separately at 9 by 7 m. Add height
above the opening, preserving human/car scale; do not uniformly scale the
image. Upper-left RPG side and leftmost wrench front are canonical shapes.
Minor multi-view details must be resolved in one coherent Blender model.

These are references, not completed runtime assets or fidelity rounds.
No beta claim is made. No world signature or replay fingerprint changed.
Merge must follow EGG-01.

## Independent gate

Clean candidate b3fcd5668a2b5244983be216195d88dda3ef9eb0 passed the required
lane tier: 217 suites, zero failures and zero not run, 338.18 s runner time
(338.40 s command). All eight campaign shards passed; no skip environment
was set. The production build passed in 1.26 s command time with only the
existing chunk-size warning. HEAD and clean state stayed unchanged.
Logs remain in .qa-dist/artw-lane.log and .qa-dist/artw-build.log.
The first log setup found the directory absent and ran no tests; the runner
created that ignored directory and then completed the gate successfully.
This final note adds evidence only to the tested source.
